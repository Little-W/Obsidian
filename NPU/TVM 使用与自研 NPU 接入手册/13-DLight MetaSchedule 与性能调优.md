---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 13. DLight MetaSchedule 与性能调优

> [!abstract] 本章内容
> 对比规则调度与搜索调度，说明任务提取、候选构建、设备测量、数据库和结果复用。

## 一句话理解

DLight 快速给出合理方案，MetaSchedule 用更多编译与测量时间寻找更快方案；两者都建立在正确的 TensorIR 与可信测量之上。

## 核心要点

1. DLight 适合快速基线和未调优任务的默认处理。
2. MetaSchedule 由任务、TuneContext、空间生成器、搜索策略、代价模型、Builder、Runner 和数据库组成。
3. 数据库保存工作负载结构、调度 trace 与测量时间。
4. RPC Runner 可在远程设备执行候选，主机负责生成和编译。
5. 自研 NPU 若采用 BYOC 图编译，可在自研编译器内部调优，也可为 TIR 路线增加规则。

```mermaid
flowchart LR
    A["输入程序状态"] --> B["分析信息"]
    B --> C["本章所述处理"]
    C --> D["输出程序状态"]
    D --> E["日志与可复现记录"]
```

## 逐项解析

### 1. DLight 适合快速基线和未调优任务的默认处理。

DLight 适合快速基线和未调优任务的默认处理。

阅读时先定位产生该信息的函数，再查找消费它的下一阶段。把两处代码和中间 IR 放在一起观察，比单独记忆类名更容易理解。

**实现建议：** 先验证每个候选的数值，再纳入性能比较。

> [!warning] 本小节常见问题
> 候选在主机上编译成功不表示 NPU 固件接受命令。

### 2. MetaSchedule 由任务、TuneContext、空间生成器、搜索策略、代价模型、Builder、Runner 和数据库组成。

MetaSchedule 由任务、TuneContext、空间生成器、搜索策略、代价模型、Builder、Runner 和数据库组成。

把变换前后的 IR 并排比较，重点查看函数参数、调用、属性、StructInfo 和返回值。只记录最终 IR 会丢失变化发生的位置。

**实现建议：** 测量前预热并使用多次重复，保存分布而非单个最小值。

> [!warning] 本小节常见问题
> 使用旧数据库但不核对硬件版本会得到无效调度。

### 3. 数据库保存工作负载结构、调度 trace 与测量时间。

数据库保存工作负载结构、调度 trace 与测量时间。

实现时将硬件固定限制放入 Target 或能力文件，把用户可选策略放入编译配置；二者发生冲突时应报告具体字段。

**实现建议：** 数据库键加入硬件、固件、编译器和目标配置版本。

> [!warning] 本小节常见问题
> 测量时间包含日志打印时，排序结果可能反映日志量而非计算速度。

### 4. RPC Runner 可在远程设备执行候选

RPC Runner 可在远程设备执行候选，主机负责生成和编译。

测试至少包含一个正常样本和一个只改变单一条件的反向样本。这样，结构或结果变化时能直接找到对应规则。

**实现建议：** 对未找到记录的任务提供基础调度或主机处理。

> [!warning] 本小节常见问题
> 候选在主机上编译成功不表示 NPU 固件接受命令。

### 5. 自研 NPU 若采用 BYOC 图编译

自研 NPU 若采用 BYOC 图编译，可在自研编译器内部调优，也可为 TIR 路线增加规则。

保存可由工具读取的阶段报告，其中包含输入摘要、输出摘要、Pass 配置、Target、耗时和错误；文本日志用于辅助阅读。

**实现建议：** 把片上容量、指令限制和对齐要求写入候选生成。

> [!warning] 本小节常见问题
> 使用旧数据库但不核对硬件版本会得到无效调度。

## 实施清单

- [ ] 先验证每个候选的数值，再纳入性能比较。
- [ ] 测量前预热并使用多次重复，保存分布而非单个最小值。
- [ ] 数据库键加入硬件、固件、编译器和目标配置版本。
- [ ] 对未找到记录的任务提供基础调度或主机处理。
- [ ] 把片上容量、指令限制和对齐要求写入候选生成。

## 设计评审问题

1. 本阶段接收哪一种 IR，要求哪些属性已经存在？
2. 本阶段产生哪些新函数、属性、常量或模块？
3. 遇到不被支持的输入时，是保留给其他后端、报告编译错误，还是插入转换？
4. 哪些配置属于硬件固定限制，哪些配置允许自动搜索？
5. 如何证明重复执行本阶段不会产生额外变化？
6. 如何在日志中解释每个重要决定？

## 自测

> [!question] 请先独立回答
> 把一个两层 MLP 的 IR 在本章处理前后分别打印出来，指出函数数量、调用形式、张量类型和模块属性发生了什么变化。如果无法运行代码，可先画出预期结构，再与实际输出比较。

## 参考资料

| 资料 | 类型 |
| --- | --- |
| [MetaSchedule](https://tvm.apache.org/docs/deep_dive/tensor_ir/tutorials/meta_schedule.html) | 官方资料 |
| [自定义优化](https://tvm.apache.org/docs/how_to/tutorials/customize_opt.html) | 官方资料 |
| [交叉编译与 RPC](https://tvm.apache.org/docs/how_to/tutorials/cross_compilation_and_rpc.html) | 官方资料 |

## 章末小结

本章的重点不是记住所有类名，而是明确输入状态、处理动作、输出状态和失败处理。接入自研 NPU 时，每一层都应保留可检查的中间结果，使图分区、代码生成、设备提交和数值对照可以分别验证。
