---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 41. FuseOpsByPattern 与组合函数

> [!abstract] 本章内容
> 从 IR 形态出发解释模式命中、组合函数生成、常量绑定、后端标注、函数命名、元组输出和重复执行稳定性。

> [!note] 官方依据
> [External Library Dispatch 的 Partitioning](https://tvm.apache.org/docs/arch/external_library_dispatch.html)、[Operator Fusion](https://tvm.apache.org/docs/arch/fusion.html) 与 `test_transform_fuse_ops_by_pattern.py`。

## 本章使用说明

先执行或检查本章给出的最小示例，再按快速目录逐项核对。每一节都使用相同的七个观察角度：功能与适用范围、官方实现、自研方法、必查信息、拒绝与异常、测试、应保留证据。这种组织便于快速定位实现位置，也便于后端开发者把内容直接转换为开发任务与测试。

遇到 API 名称时保留源码拼写，例如 `FusionPattern`、`FuseOpsByPattern`、`RunCodegen` 和 `runtime.Module`；解释文字使用 [[33-术语与 API 速查]] 中的标准中文。若某个接口与本地构建不同，先确认 TVM 提交与 Python 加载的动态库，再查固定 tag 的源码和测试。

## 快速目录

- [[#41.1 分区前的规范化|41.1 分区前的规范化]]
- [[#41.2 Composite 内层函数|41.2 Composite 内层函数]]
- [[#41.3 带 Codegen 属性的外层函数|41.3 带 Codegen 属性的外层函数]]
- [[#41.4 bind_constants 的选择|41.4 bind_constants 的选择]]
- [[#41.5 annotate_codegen 的选择|41.5 annotate_codegen 的选择]]
- [[#41.6 多输出与元组|41.6 多输出与元组]]
- [[#41.7 函数命名与稳定性|41.7 函数命名与稳定性]]
- [[#41.8 重复运行 Pass 的稳定性|41.8 重复运行 Pass 的稳定性]]

## 41.0 两层函数分别负责什么

```text
外层函数
  Codegen = "acme_npu"
  global_symbol = "fused_..."
  负责成为 RunCodegen 的编译单位

内层函数
  Composite = "acme_npu.matmul_bias_relu"
  负责告诉后端内部是哪一种已识别组合
```

`annotate_codegen=True` 时，Pass 可以直接形成这两层结构。采用其他流程时，也可以先只建立 Composite 函数，再由 `MergeCompositeFunctions` 生成带 Codegen 的外层函数。

## 41.1 分区前的规范化

> [!abstract] 本节内容
> 保证同一计算不会因不同前端写法而产生完全不同的模式命中结果

### 功能与适用范围

保证同一计算不会因不同前端写法而产生完全不同的模式命中结果。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

FuseOpsByPattern 在 Relax IR 上工作，输入表达式形态会直接影响 DFPattern 是否匹配。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

固定在分区前运行的规范化、常量折叠和布局处理，并记录 Pass 顺序。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

算子注册名、属性默认值、元组拆取、别名写法、常量形式和数据流块。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

输入 IR 仍含后端不认识的等价写法时先执行正规化，不要无限扩张模式。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

同一模型的多种前端写法经过前置 Pass 后应得到相同关键子图。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

前置 Pass 清单、规范化前后 IR 和命中差异。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 41.2 Composite 内层函数

> [!abstract] 本节内容
> 理解内层函数是被识别组合的自包含表示，供代码生成器按名称分派

### 功能与适用范围

理解内层函数是被识别组合的自包含表示，供代码生成器按名称分派。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方输出会在私有 Relax 函数上写 `Composite` 属性，函数体保留原 Relax 运算。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

检查函数体只依赖参数和内部绑定，组合名称与代码生成访问器一一对应。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

函数参数顺序、返回值、内部绑定、Composite 名称、纯函数属性和自由变量。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

函数捕获外部变量、组合名称没有访问器或返回信息不完整时停止代码生成。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

对每个 Composite 结构保存期望 IR，并用结构比较验证。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

组合函数 TVMScript、名称、参数表和代码生成处理器。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 41.3 带 Codegen 属性的外层函数

> [!abstract] 本节内容
> 理解外层函数是一个后端编译单位，可以包含一个或多个 Composite 调用

### 功能与适用范围

理解外层函数是一个后端编译单位，可以包含一个或多个 Composite 调用。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

RunCodegen 扫描带 `Codegen` 属性的 Relax 函数，并以该属性组成 `relax.ext.<backend>` 注册名。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

外层函数只暴露必要输入输出，写入稳定 global_symbol，并限制函数规模。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

Codegen、global_symbol、参数、返回值、内部组合数量、后端选项和 Span。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

后端名称与注册名不一致、符号重复或函数接口超出 ABI 时拒绝。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

多个外层函数同时存在时检查每个符号、模块数量和调用替换。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

外部函数目录、符号生成输入和唯一性检查结果。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 41.4 bind_constants 的选择

> [!abstract] 本节内容
> 决定权重是外部函数参数还是在分区时成为函数内部常量

### 功能与适用范围

决定权重是外部函数参数还是在分区时成为函数内部常量。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方 Example NPU 使用 False，由主机持有参数；当前 TensorRT 教程使用 True，把权重用于引擎构建。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

依据权重更新需求、引擎构建方式、模块体积和共享策略选择，并写入编译配置。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

常量大小、共享函数、更新频率、预处理布局、缓存键和模块格式。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

运行时需要更新权重却被固定进产物，或设备要求静态权重却仍作为普通参数时拒绝配置。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

同一模型分别测试两种模式，检查函数参数、常量清单、导出大小和重复加载。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

选择理由、常量所有权、产物清单和内存占用。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 41.5 annotate_codegen 的选择

> [!abstract] 本节内容
> 明确是在 FuseOpsByPattern 中直接建立后端外层函数，还是稍后统一分组

### 功能与适用范围

明确是在 FuseOpsByPattern 中直接建立后端外层函数，还是稍后统一分组。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方 Example NPU 教程设置 True，文档也说明设置 False 后可由 MergeCompositeFunctions 添加后端属性。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

需要跨多个组合做较大分组时可先 False；只需一个组合一个调用时可直接 True。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

Pass 次序、预期函数层数、是否调用 MergeCompositeFunctions 和最终外部函数数量。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

RunCodegen 前仍没有 Codegen 属性时不得继续执行。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

为两种配置保存 IR 样本，确认最终外部调用符合设计。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

配置值、函数层次图、Pass 输出和外部函数数量。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 41.6 多输出与元组

> [!abstract] 本节内容
> 处理组合运算返回多个张量以及主函数使用其中部分结果的情况

### 功能与适用范围

处理组合运算返回多个张量以及主函数使用其中部分结果的情况。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

Relax 用 TupleStructInfo 描述多输出，JSONSerializer 会把元组字段展平为多个节点入口。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

产物 ABI 显式列出每个输出的顺序、形状、数据类型和是否可省略。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

元组字段数量、TupleGetItem 使用、输出顺序、未使用输出和缓冲区分配。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

运行时模块不支持多输出或只计算部分字段会改变行为时拒绝组合。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

构造双输出函数，分别使用全部、只使用第一项和交叉使用，检查分区和运行。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

返回 StructInfo、输出目录、节点 entry 编号和调用参数。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 41.7 函数命名与稳定性

> [!abstract] 本节内容
> 使自动生成的 global_symbol 在一次编译中唯一，并能关联源模型位置

### 功能与适用范围

使自动生成的 global_symbol 在一次编译中唯一，并能关联源模型位置。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方 Pass 会生成包含算子和后端信息的函数名，RunCodegen 通过 GetExtSymbol 读取外部符号。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

对外产物使用确定性符号，必要时追加结构散列短值，避免依赖对象地址。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

入口函数、模式名称、出现序号、结构散列、字符限制和符号表限制。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

重复符号、超长名称、非法字符或结构相同但规格不同共用符号时停止编译。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

相同模块多次编译比较符号列表，不同模块并入同一包时检查冲突。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

符号生成规则、输入字段、最终符号表和冲突检查。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 41.8 重复运行 Pass 的稳定性

> [!abstract] 本节内容
> 确认分区 Pass 被意外执行两次时不会继续套入函数或改变已有外部函数

### 功能与适用范围

确认分区 Pass 被意外执行两次时不会继续套入函数或改变已有外部函数。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

TVM 变换通常在已规范化输入上使用，项目流程仍应明确哪些 Pass 允许重复执行。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

为分区入口加入状态检查或通过结构测试证明第二次执行没有额外变化。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

已有 Composite、已有 Codegen、函数数量、属性、模式命中和 Pass 配置。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

第二次执行增加函数层次、改变符号或重新绑定常量时视为流程错误。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

对一次和两次执行结果做结构比较，并测试先运行其他后端分区的情况。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

Pass 次序、结构散列和重复执行差异。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 本章检查清单

- [ ] 能用自己的话说明本章每个 TVM 属性或 API 的作用；
- [ ] 能在变换前后 IR 中找到对应结构；
- [ ] 能指出 Example NPU 中仅仅用于说明机制的部分；
- [ ] 能写出自研后端的输入、输出、失败原因和测试；
- [ ] 能保存足以在另一进程或另一台开发机复现的证据；
- [ ] 能说明本章内容与前后章节的关系。

## 继续阅读

本章属于 BYOC 专题。建议同时参照 [[15-Relax DPL 子图识别与分区]]、[[16-BYOC 代码生成器]]、[[17-NPU 运行时与驱动适配]]、[[26-端到端参考实现骨架]] 和 [[29-官方资料与源码导航]]。

## 实现说明与验证步骤

> [!note] 依据与适用范围
> 本节依据所列 Apache TVM 官方资料和 v0.24.0 源码整理，给出输入条件、操作步骤、预期结果、异常处理和自研 NPU 适配要求。接口细节以固定版本源码与测试为准，在线页面用于补充说明。

### 41.A 目标与适用范围

对 MatMul+ReLU 运行 `FuseOpsByPattern`，分别比较 `annotate_codegen=True` 与 `False` 的模块结构。

参考样本保持输入规模较小，以便直接检查对象、字段和输出。首次执行时不要同时加入图改写、外部代码生成和真实设备执行；应先确认当前阶段的输入与输出，再增加后续处理。建议为该项验证建立独立目录，保存命令、标准输出、IR、编译产物摘要和环境信息。

### 41.B 参考实现

**官方依据：** [External Library Dispatch](https://tvm.apache.org/docs/arch/external_library_dispatch.html)。

**v0.24.0 源码位置：** `src/relax/transform/fuse_ops.cc`、`tests/python/relax/test_transform_fuse_ops_by_pattern.py`。

```python
from tvm.relax.transform import FuseOpsByPattern

def fuse(mod, patterns, annotate):
    return FuseOpsByPattern(
        patterns,
        bind_constants=False,
        annotate_codegen=annotate,
    )(mod)

# inner_only = fuse(mod, patterns, False)
# external = fuse(mod, patterns, True)
```

按以下顺序执行：

1. 在新进程中确认 `tvm.__file__`、动态库路径和 TVM 提交，避免受到旧进程注册状态影响；
2. 将参考实现保存到单独文件，只补充本节明确要求的输入对象，不先加入额外优化；
3. 执行后保存完整输出；若生成 IR，同时保存 `script(show_meta=True)` 的文本；
4. 把输出与下面的预期现象逐项比较，不以“没有抛出异常”代替功能检查；
5. 重复执行一次并比较主要产物，确认结果没有依赖临时全局状态或未固定的遍历顺序。

> [!success] 预期现象
> `False` 主要产生带 `Composite` 的内部函数，后续可由合并 Pass 组成外部函数；`True` 会建立带 `Codegen` 的外层函数。两种结果适合不同的分组策略。

> [!tip] 输出检查顺序
> 先看对象类型和函数数量，再看属性、调用形式、形状与数据类型，最后看运行结果或编译产物。若前一项不符合预期，先停止后续步骤。这样能把问题限定在最早出现差异的阶段。

### 41.C 单项条件验证

把 `bind_constants` 改为 `True`，观察权重是否进入函数正文以及外部函数参数数量如何变化。大型权重的复制和缓存策略会因此改变。

执行条件变化样本时，复制参考实现的输入与环境，只修改上文指出的一项。对两个输出进行结构比较，并记录以下四项：

1. 第一次出现差异的是哪一个阶段？
2. 差异表现为函数、属性、形状、数据类型、编译产物还是运行结果？
3. 当前行为是明确拒绝、交给其他后端执行，还是编译错误？
4. 日志是否足以让另一位开发者不查看本次进程状态也能复现？

如果同时观察到多个变化，应继续缩小条件变化样本。参考实现用于确认正常处理，单项条件验证用于确认系统为何接受、拒绝或转交当前输入。

### 41.D 自研 NPU 适配要求

为自研后端固定一种常量策略并测试导出加载。若允许两种策略，模块格式必须区分嵌入常量与外部参数，运行时不可按参数位置猜测。

改造时建议保留三份相互独立的输入：最小 Relax 或 TensorIR、设备编译器输入、运行时调用输入。三份输入使用同一个样本编号，并记录转换前后的关键字段。这样可以分别测试 TVM 变换、NPU 编译器和驱动，不需要每次都运行完整模型。

至少补充以下样本：

- 一个完全满足能力要求的正向样本；
- 一个只改变数据类型的反向样本；
- 一个只改变形状或属性的反向样本；
- 一个包含主机与 NPU 混合执行的样本；
- 一个导出后在新进程加载的样本；
- 若支持动态尺寸，再增加最小值、常用值和最大值附近的样本。

### 41.E 源码定位

从上文列出的源码位置选择一个公开 API，依次找到 Python 调用、FFI 注册、C++ 实现和测试。记录函数签名、主要输入、主要输出、会写入的模块属性以及失败方式。若名称只在文档出现而源码中找不到，继续搜索注册字符串、属性键或错误文本。

> [!important] 验证项目
> 1. 执行前记录参考实现的对象关系；2. 执行后用实际输出修正记录；3. 增加一个会被明确拒绝的输入；4. 标明自研 NPU 接入需要修改的层次与保持不变的层次；5. 把结果整理成可由自动测试检查的断言。

### 41.F 检查清单

- [ ] 示例可在固定 v0.24.0 环境重复执行，或已明确列出所需可选依赖；
- [ ] 预期现象包含可检查的结构、字段或数值，不只是“运行成功”；
- [ ] 单条件变化能触发预期的拒绝、转交或结构差异；
- [ ] 能从官方页面定位到固定版本源码和测试；
- [ ] NPU 改造说明包含编译阶段、运行阶段与部署阶段；
- [ ] 失败时保留最小输入、环境、阶段输出和第一个错误。
