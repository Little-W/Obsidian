---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 44. JSONSerializer 与编译产物

> [!abstract] 本章内容
> 结合 ExampleNPUJSONSerializer 说明 Relax 函数如何转换为 JSON 图，详细讨论节点、入口、输出、属性、常量、形状、数据类型以及从教学格式升级为真实 NPU 产物的方法。

> [!note] 官方依据
> `src/relax/backend/contrib/codegen_json/codegen_json.h`、`src/relax/backend/contrib/example_npu/codegen.cc` 与JSON 运行时源码。

## 本章阅读方法

先运行或阅读本章给出的最小示例，再按快速目录逐项检查。每一节都使用相同的七个观察角度：要解决的问题、官方实现、自研方法、必查信息、拒绝与异常、测试、应保留证据。这样的组织便于初学者从一个可见结果出发，也便于后端开发者直接把内容转成任务与测试。

遇到 API 名称时保留源码拼写，例如 `FusionPattern`、`FuseOpsByPattern`、`RunCodegen` 和 `runtime.Module`；解释文字使用 [[33-术语与 API 速查]] 中的标准中文。若某个接口与本地构建不同，先确认 TVM 提交与 Python 加载的动态库，再查固定 tag 的源码和测试。

## 快速目录

- [[#44.1 AnalyzeVar2Value 与局部函数|44.1 AnalyzeVar2Value 与局部函数]]
- [[#44.2 Composite 访问器|44.2 Composite 访问器]]
- [[#44.3 JSON 节点与 NodeEntries|44.3 JSON 节点与 NodeEntries]]
- [[#44.4 形状与数据类型写入|44.4 形状与数据类型写入]]
- [[#44.5 算子属性提取|44.5 算子属性提取]]
- [[#44.6 常量节点|44.6 常量节点]]
- [[#44.7 JSON 格式的限制|44.7 JSON 格式的限制]]
- [[#44.8 真实 NPU 产物包|44.8 真实 NPU 产物包]]

## 44.0 Example NPU 编译器做了什么

```cpp
ffi::Array<ffi::Module> ExampleNPUCompiler(
    ffi::Array<Function> functions,
    ffi::Map<ffi::String, ffi::Any> options,
    ffi::Map<Constant, ffi::String> constant_names) {
  ffi::Array<ffi::Module> compiled_functions;
  const auto create = tvm::ffi::Function::GetGlobalRequired(
      "runtime.ExampleNPUJSONRuntimeCreate");

  for (const auto& func : functions) {
    ExampleNPUJSONSerializer serializer(
        constant_names, AnalyzeVar2Value(func));
    serializer.serialize(func);
    auto graph_json = serializer.GetJSON();
    auto const_names = serializer.GetConstantNames();
    auto func_name = GetExtSymbol(func);
    compiled_functions.push_back(
        create(func_name, graph_json, const_names).cast<ffi::Module>());
  }
  return compiled_functions;
}
```

Example NPU 不会在这里生成真实机器命令。它把组合函数写成 JSON，再立即创建一个JSON 运行时模块。真实后端可以保留这套组织方式，也可以把 JSON 换成设备编译器产生的二进制，只要返回值仍是可导出、可加载并能提供外部符号的 `runtime.Module`。

## 44.1 AnalyzeVar2Value 与局部函数

> [!abstract] 本节目标
> 理解代码生成器为何需要从变量找到其绑定的组合函数

### 先理解要解决的问题

理解代码生成器为何需要从变量找到其绑定的组合函数。对初学者而言，可以把这一项看成一次责任交接：前一阶段必须把后续阶段需要的信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息工作，不能猜测模型作者的意图。阅读打印出来的 IR 时，应先找负责交接的函数、属性和调用，再观察普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

ExampleNPUJSONSerializer 构造时接收 `AnalyzeVar2Value(func)` 的结果，VisitExpr_ 由 Call 的 Var 取得 Function。本手册以 v0.24.0 源码、对应测试和当前在线教程共同核对。若在线教程与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，教程用于理解整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

在外部函数进入代码生成前检查所有局部函数调用都有绑定，禁止依赖模块外未传入定义。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

调用 op 类型、Var、绑定表达式、Function、Composite 属性和作用域。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

callee 不是 Var、找不到绑定或绑定不是 Function 时作为后端输入不合法报告。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

测试一个、多个和嵌套局部函数，并为缺失绑定构造开发期异常样本。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

变量绑定表、函数属性、调用位置和失败节点。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 44.2 Composite 访问器

> [!abstract] 本节目标
> 把不同 Composite 名称转换为后端节点或设备编译器操作

### 先理解要解决的问题

把不同 Composite 名称转换为后端节点或设备编译器操作。对初学者而言，可以把这一项看成一次责任交接：前一阶段必须把后续阶段需要的信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息工作，不能猜测模型作者的意图。阅读打印出来的 IR 时，应先找负责交接的函数、属性和调用，再观察普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

Example NPU 读取局部函数的 Composite 属性，直接把名称作为 JSON kernel 节点的 op_name。本手册以 v0.24.0 源码、对应测试和当前在线教程共同核对。若在线教程与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，教程用于理解整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

真实后端建立显式访问器表，为每个组合解析属性、验证输入并生成内部操作。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

Composite 名称、处理函数、输入角色、输出数量、属性和最低设备版本。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

没有处理函数、名称版本过新或输出数量不一致时停止代码生成。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

逐个遍历已注册模式，确认每个可接纳 Composite 都有代码生成处理器。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

模式到处理器对应表、处理器版本和产物节点摘要。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 44.3 JSON 节点与 NodeEntries

> [!abstract] 本节目标
> 理解一个 Relax 表达式如何产生一个或多个 JSON 图入口

### 先理解要解决的问题

理解一个 Relax 表达式如何产生一个或多个 JSON 图入口。对初学者而言，可以把这一项看成一次责任交接：前一阶段必须把后续阶段需要的信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息工作，不能猜测模型作者的意图。阅读打印出来的 IR 时，应先找负责交接的函数、属性和调用，再观察普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

JSONSerializer 用 JSONGraphNodeEntry 保存节点编号与输出编号，元组结果会展平为多个入口。本手册以 v0.24.0 源码、对应测试和当前在线教程共同核对。若在线教程与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，教程用于理解整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

内部图也采用稳定值编号，禁止使用原始指针或主机地址作为跨进程标识。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

node id、output index、输入入口、输出数量、节点类型和拓扑顺序。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

引用不存在节点、输出编号超范围或出现非拓扑顺序依赖时拒绝产物。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

解析生成 JSON，逐项检查入口编号和输出编号，并对多输出运算测试。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

节点表、入口表、输出表和拓扑检查结果。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 44.4 形状与数据类型写入

> [!abstract] 本节目标
> 保证运行时能够为输出分配正确缓冲区并检查输入

### 先理解要解决的问题

保证运行时能够为输出分配正确缓冲区并检查输入。对初学者而言，可以把这一项看成一次责任交接：前一阶段必须把后续阶段需要的信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息工作，不能猜测模型作者的意图。阅读打印出来的 IR 时，应先找负责交接的函数、属性和调用，再观察普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

AddNode 从 StructInfo 取得 TensorStructInfo；常用路径要求形状已定义，并写入形状与数据类型属性。本手册以 v0.24.0 源码、对应测试和当前在线教程共同核对。若在线教程与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，教程用于理解整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

除公开逻辑形状外，真实产物还记录设备布局、存储字节数、对齐和动态范围。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

逻辑形状、设备形状、数据类型、打包位宽、byte_offset、步长和输出数量。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

ShapeExpr 无法转为后端支持形式、数据类型未定义或字节数计算溢出时停止编译。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

用独立解析器复算每个输出字节数，覆盖标量、空尺寸、多输出和符号尺寸。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

StructInfo 摘要、序列化字段、复算结果和内存需求。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 44.5 算子属性提取

> [!abstract] 本节目标
> 把卷积、池化、转置等属性准确写入后端表示

### 先理解要解决的问题

把卷积、池化、转置等属性准确写入后端表示。对初学者而言，可以把这一项看成一次责任交接：前一阶段必须把后续阶段需要的信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息工作，不能猜测模型作者的意图。阅读打印出来的 IR 时，应先找负责交接的函数、属性和调用，再观察普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

OpAttrExtractor 通过 FFI 反射读取整数、浮点、布尔、字符串、数据类型和数组字段。本手册以 v0.24.0 源码、对应测试和当前在线教程共同核对。若在线教程与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，教程用于理解整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

只导出规格说明允许的字段，并在导出前把默认值补全为统一形式。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

字段名称、原始类型、序列化类型、列表元素类型、空值和缺省行为。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

不支持的字段类型、混合数组无法安全表示或未知属性会影响结果时停止代码生成。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

每种属性类型构造一个测试，JSON 往返后与正规化属性比较。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

属性表、类型表、JSON 片段和往返差异。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 44.6 常量节点

> [!abstract] 本节目标
> 区分普通输入节点与由 RunCodegen 命名的常量节点

### 先理解要解决的问题

区分普通输入节点与由 RunCodegen 命名的常量节点。对初学者而言，可以把这一项看成一次责任交接：前一阶段必须把后续阶段需要的信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息工作，不能猜测模型作者的意图。阅读打印出来的 IR 时，应先找负责交接的函数、属性和调用，再观察普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

JSONSerializer 使用 constant_names 查找 Constant，并记录实际使用的常量名称。本手册以 v0.24.0 源码、对应测试和当前在线教程共同核对。若在线教程与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，教程用于理解整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

产物只引用需要的权重，权重内容放在模块、独立权重包或初始化参数中。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

常量名称、使用节点、数据类型、形状、内容散列、设备布局和存储位置。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

常量没有稳定名称、名称重复、内容缺失或预处理版本不符时停止模块创建。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

含未使用权重、共享权重和同形不同值权重的模型分别测试。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

常量目录、引用计数、内容散列和加载来源。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 44.7 JSON 格式的限制

> [!abstract] 本节目标
> 认识教学 JSON 图在安全、体积、解析速度和版本管理方面的不足

### 先理解要解决的问题

认识教学 JSON 图在安全、体积、解析速度和版本管理方面的不足。对初学者而言，可以把这一项看成一次责任交接：前一阶段必须把后续阶段需要的信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息工作，不能猜测模型作者的意图。阅读打印出来的 IR 时，应先找负责交接的函数、属性和调用，再观察普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方 JSONRuntimeBase 适合库调用型后端和教学，但不自动提供真实 NPU 命令格式所需的全部字段。本手册以 v0.24.0 源码、对应测试和当前在线教程共同核对。若在线教程与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，教程用于理解整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

为 JSON 增加 schema_version、长度限制、未知字段策略和严格解析器，稳定后可转为紧凑二进制。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

格式版本、最大节点数、字符串长度、整数范围、可选字段、校验值和端序。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

超大长度、重复键、未知必需字段、整数溢出或截断输入必须安全失败。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

使用损坏、截断、超大和未知版本文件测试加载器，不只测试正常产物。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

格式说明、结构规范、解析错误、模糊测试结果和迁移工具。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 44.8 真实 NPU 产物包

> [!abstract] 本节目标
> 从单一 JSON 升级为包含命令、常量、重定位、函数目录和版本信息的部署包

### 先理解要解决的问题

从单一 JSON 升级为包含命令、常量、重定位、函数目录和版本信息的部署包。对初学者而言，可以把这一项看成一次责任交接：前一阶段必须把后续阶段需要的信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息工作，不能猜测模型作者的意图。阅读打印出来的 IR 时，应先找负责交接的函数、属性和调用，再观察普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

BYOC 只要求编译器返回 runtime.Module，并不限制模块内部使用 JSON、源码还是二进制。本手册以 v0.24.0 源码、对应测试和当前在线教程共同核对。若在线教程与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，教程用于理解整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

设计带文件头和分段目录的容器，元数据可读，命令与权重按对齐存放。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

magic、格式版本、函数、命令段、常量段、重定位、工作区、Target 和最低运行时版本。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

任何分段越界、重叠、校验失败或版本不兼容都应在设备资源申请前拒绝。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

独立打包器与解析器做往返测试，并在新进程及只运行环境加载。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

产物清单、分段表、散列值、加载报告和兼容表。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 本章完成检查

- [ ] 能用自己的话说明本章每个 TVM 属性或 API 的作用；
- [ ] 能在变换前后 IR 中找到对应结构；
- [ ] 能指出 Example NPU 中仅用于教学的部分；
- [ ] 能写出自研后端的输入、输出、失败原因和测试；
- [ ] 能保存足以在另一进程或另一台开发机复现的证据；
- [ ] 能说明本章内容与前后章节的关系。

## 继续阅读

本章属于 BYOC 专题。建议同时参照 [[15-Relax DPL 子图识别与分区]]、[[16-BYOC 代码生成器]]、[[17-NPU 运行时与驱动适配]]、[[26-端到端参考实现骨架]] 和 [[29-官方资料与源码导航]]。
