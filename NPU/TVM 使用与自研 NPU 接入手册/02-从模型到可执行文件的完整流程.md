---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 02. 从模型到可执行文件的完整流程

> [!abstract] 本章内容
> 从模型导入开始，解释图级变换、算子逐级转换、TensorIR、目标翻译、模块打包和运行。

## 一句话理解

IRModule 是贯穿流程的工程容器；Relax 函数描述图与控制，PrimFunc 描述具体张量程序，runtime.Module 保存可运行函数。

## 核心要点

1. 模型前端创建含 Relax 函数的 IRModule，并可把参数作为常量或函数参数。
2. 图级 Pass 完成常量折叠、布局调整、组合运算识别、未使用代码删除等工作。
3. `LegalizeOps` 把高层算子改写为 `call_tir` 或外部函数调用。
4. TensorIR 继续进行循环、缓冲区、线程和硬件指令相关处理。
5. 代码生成结果放入 runtime.Module，并与 VM 字节码和常量共同组成可执行对象。

```mermaid
flowchart LR
    A["输入程序状态"] --> B["分析信息"]
    B --> C["本章所述处理"]
    C --> D["输出程序状态"]
    D --> E["日志与可复现记录"]
```

## 逐项解析

### 1. 模型前端创建含 Relax 函数的 IRModule

模型前端创建含 Relax 函数的 IRModule，并可把参数作为常量或函数参数。

阅读时先定位产生该信息的函数，再查找消费它的下一阶段。把两处代码和中间 IR 放在一起观察，比单独记忆类名更容易理解。

**实现建议：** 在每个关键 Pass 后保存 `mod.script()`，用结构比较定位第一次出现差异的位置。

> [!warning] 本小节常见问题
> 把 Relax VM 当作数学计算引擎会造成错误理解；计算由 TIR 内核或外部函数完成。

### 2. 图级 Pass 完成常量折叠、布局调整、组合运算识别、未使用代码删除等工作。

图级 Pass 完成常量折叠、布局调整、组合运算识别、未使用代码删除等工作。

把变换前后的 IR 并排比较，重点查看函数参数、调用、属性、StructInfo 和返回值。只记录最终 IR 会丢失变化发生的位置。

**实现建议：** 把主机 Target 与设备 Target 分开描述，避免交叉编译时使用主机自动探测结果。

> [!warning] 本小节常见问题
> 只看最终汇编无法解释图分区问题，应从导入后的 Relax 开始检查。

### 3. `LegalizeOps` 把高层算子改写为 `call_tir` 或外部函数调用。

`LegalizeOps` 把高层算子改写为 `call_tir` 或外部函数调用。

实现时将硬件固定限制放入 Target 或能力文件，把用户可选策略放入编译配置；二者发生冲突时应报告具体字段。

**实现建议：** 在 NPU 分区前先完成必要的图规范化，使等价写法呈现为稳定结构。

> [!warning] 本小节常见问题
> 先做图融合再注册更具体的 NPU 组合运算，可能使所需结构消失。

### 4. TensorIR 继续进行循环、缓冲区、线程和硬件指令相关处理。

TensorIR 继续进行循环、缓冲区、线程和硬件指令相关处理。

测试至少包含一个正常样本和一个只改变单一条件的反向样本。这样，结构或结果变化时能直接找到对应规则。

**实现建议：** 将编译与执行分成两个测试，以确认导出文件没有依赖编译进程内的临时状态。

> [!warning] 本小节常见问题
> 把 Relax VM 当作数学计算引擎会造成错误理解；计算由 TIR 内核或外部函数完成。

### 5. 代码生成结果放入 runtime.Module

代码生成结果放入 runtime.Module，并与 VM 字节码和常量共同组成可执行对象。

保存可由工具读取的阶段报告，其中包含输入摘要、输出摘要、Pass 配置、Target、耗时和错误；文本日志用于辅助阅读。

**实现建议：** 在每个关键 Pass 后保存 `mod.script()`，用结构比较定位第一次出现差异的位置。

> [!warning] 本小节常见问题
> 只看最终汇编无法解释图分区问题，应从导入后的 Relax 开始检查。

## 实施清单

- [ ] 在每个关键 Pass 后保存 `mod.script()`，用结构比较定位第一次出现差异的位置。
- [ ] 把主机 Target 与设备 Target 分开描述，避免交叉编译时使用主机自动探测结果。
- [ ] 在 NPU 分区前先完成必要的图规范化，使等价写法呈现为稳定结构。
- [ ] 将编译与执行分成两个测试，以确认导出文件没有依赖编译进程内的临时状态。

## 设计评审问题

1. 本阶段接收哪一种 IR，要求哪些属性已经存在？
2. 本阶段产生哪些新函数、属性、常量或模块？
3. 遇到不被支持的输入时，是保留给其他后端、报告编译错误，还是插入转换？
4. 哪些配置属于硬件固定限制，哪些配置允许自动搜索？
5. 如何证明重复执行本阶段不会产生额外变化？
6. 如何在日志中解释每个重要决定？

## 自测

> [!question] 请先独立回答
> 把一个两层 MLP 的 IR 在本章处理前后分别打印出来，指出函数数量、调用形式、张量类型和模块属性发生了什么变化。如果无法运行代码，可先画出预期结构，再与实际输出比较。

## 参考资料

| 资料 | 类型 |
| --- | --- |
| [架构总览](https://tvm.apache.org/docs/arch/index.html) | 官方资料 |
| [快速开始](https://tvm.apache.org/docs/get_started/tutorials/quick_start.html) | 官方资料 |
| [代码生成](https://tvm.apache.org/docs/arch/codegen.html) | 官方资料 |

## 章末小结

本章的重点不是记住所有类名，而是明确输入状态、处理动作、输出状态和失败处理。接入自研 NPU 时，每一层都应保留可检查的中间结果，使图分区、代码生成、设备提交和数值对照可以分别验证。
