---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 03. IRModule 与统一对象系统

> [!abstract] 本章内容
> 说明 IRModule、GlobalVar、Function、StructInfo、结构相等、反射、序列化和 FFI 的关系。

## 一句话理解

IRModule 类似一个可被编译器反复改写的程序包，内部函数可以属于不同 IR，但通过统一的类型、属性和全局名称互相调用。

## 核心要点

1. `IRModule` 保存全局函数、类型定义、全局信息与模块属性。
2. `relax.Function` 与 `tirx.PrimFunc` 可以共存，调用约定让图级函数调用张量内核。
3. 结构相等比较程序结构而非 Python 对象地址，适合 Pass 单元测试。
4. Object 系统提供反射、属性访问、散列和跨语言传递。
5. 全局注册表把 Python、编译器 C++ 与运行时 C++ 的函数接在一起。

```mermaid
flowchart LR
    A["输入程序状态"] --> B["分析信息"]
    B --> C["本章所述处理"]
    C --> D["输出程序状态"]
    D --> E["日志与可复现记录"]
```

## 逐项解析

### 1. `IRModule` 保存全局函数、类型定义、全局信息与模块属性。

`IRModule` 保存全局函数、类型定义、全局信息与模块属性。 这一点之所以重要，是因为 TVM 的信息会在多个 IR 层次之间逐步变得具体。上一层没有保留下来的信息，下一层通常无法可靠恢复；下一层过早写死的选择，也会限制后续优化。初学时应在每次变换前后打印模块，找出函数、调用、属性、形状和 dtype 的变化。

实现时可执行的动作是：为每个外部函数设置稳定且唯一的 `global_symbol`。 建议同时保存输入 IR、输出 IR、Pass 配置、Target 和日志。若出现结果差异，先找到第一次发生结构变化的阶段，再向下检查，而不是从最终设备结果反推全部过程。

> [!warning] 本小节常见问题
> 打印结果相同不保证结构完全相同，隐藏属性可能影响后续 Pass。

### 2. `relax.Function` 与 `tirx.PrimFunc` 可以共存

`relax.Function` 与 `tirx.PrimFunc` 可以共存，调用约定让图级函数调用张量内核。 这一点之所以重要，是因为 TVM 的信息会在多个 IR 层次之间逐步变得具体。上一层没有保留下来的信息，下一层通常无法可靠恢复；下一层过早写死的选择，也会限制后续优化。初学时应在每次变换前后打印模块，找出函数、调用、属性、形状和 dtype 的变化。

实现时可执行的动作是：单元测试同时检查结构与数值，避免只比较打印文本。 建议同时保存输入 IR、输出 IR、Pass 配置、Target 和日志。若出现结果差异，先找到第一次发生结构变化的阶段，再向下检查，而不是从最终设备结果反推全部过程。

> [!warning] 本小节常见问题
> 全局符号重复会在打包或加载阶段才暴露，定位成本较高。

### 3. 结构相等比较程序结构而非 Python 对象地址

结构相等比较程序结构而非 Python 对象地址，适合 Pass 单元测试。 这一点之所以重要，是因为 TVM 的信息会在多个 IR 层次之间逐步变得具体。上一层没有保留下来的信息，下一层通常无法可靠恢复；下一层过早写死的选择，也会限制后续优化。初学时应在每次变换前后打印模块，找出函数、调用、属性、形状和 dtype 的变化。

实现时可执行的动作是：修改节点时使用 BlockBuilder、Mutator 或标准 Pass，不直接改不可变对象字段。 建议同时保存输入 IR、输出 IR、Pass 配置、Target 和日志。若出现结果差异，先找到第一次发生结构变化的阶段，再向下检查，而不是从最终设备结果反推全部过程。

> [!warning] 本小节常见问题
> 把 Python 字典当作稳定二进制协议，会造成版本兼容问题。

### 4. Object 系统提供反射、属性访问、散列和跨语言传递。

Object 系统提供反射、属性访问、散列和跨语言传递。 这一点之所以重要，是因为 TVM 的信息会在多个 IR 层次之间逐步变得具体。上一层没有保留下来的信息，下一层通常无法可靠恢复；下一层过早写死的选择，也会限制后续优化。初学时应在每次变换前后打印模块，找出函数、调用、属性、形状和 dtype 的变化。

实现时可执行的动作是：为自研属性设计清晰名称空间，防止与 TVM 内置属性重名。 建议同时保存输入 IR、输出 IR、Pass 配置、Target 和日志。若出现结果差异，先找到第一次发生结构变化的阶段，再向下检查，而不是从最终设备结果反推全部过程。

> [!warning] 本小节常见问题
> 打印结果相同不保证结构完全相同，隐藏属性可能影响后续 Pass。

### 5. 全局注册表把 Python、编译器 C++ 与运行时 C++ 的函数接在一起。

全局注册表把 Python、编译器 C++ 与运行时 C++ 的函数接在一起。 这一点之所以重要，是因为 TVM 的信息会在多个 IR 层次之间逐步变得具体。上一层没有保留下来的信息，下一层通常无法可靠恢复；下一层过早写死的选择，也会限制后续优化。初学时应在每次变换前后打印模块，找出函数、调用、属性、形状和 dtype 的变化。

实现时可执行的动作是：为每个外部函数设置稳定且唯一的 `global_symbol`。 建议同时保存输入 IR、输出 IR、Pass 配置、Target 和日志。若出现结果差异，先找到第一次发生结构变化的阶段，再向下检查，而不是从最终设备结果反推全部过程。

> [!warning] 本小节常见问题
> 全局符号重复会在打包或加载阶段才暴露，定位成本较高。

## 实施清单

- [ ] 为每个外部函数设置稳定且唯一的 `global_symbol`。
- [ ] 单元测试同时检查结构与数值，避免只比较打印文本。
- [ ] 修改节点时使用 BlockBuilder、Mutator 或标准 Pass，不直接改不可变对象字段。
- [ ] 为自研属性设计清晰名称空间，防止与 TVM 内置属性重名。

## 设计评审问题

1. 本阶段接收哪一种 IR，要求哪些属性已经存在？
2. 本阶段产生哪些新函数、属性、常量或模块？
3. 遇到不被支持的输入时，是保留给其他后端、报告编译错误，还是插入转换？
4. 哪些配置属于硬件固定限制，哪些配置允许自动搜索？
5. 如何证明重复执行本阶段不会产生额外变化？
6. 如何在日志中解释每个重要决定？

## 自测

> [!question] 请先独立回答
> 把一个两层 MLP 的 IR 在本章处理前后分别打印出来，指出函数数量、调用形式、张量类型和模块属性发生了什么变化。如果无法运行代码，可先画出预期结构，再与实际输出比较。

## 参考资料

| 资料 | 类型 |
| --- | --- |
| [IRModule 入门](https://tvm.apache.org/docs/get_started/tutorials/ir_module.html) | 官方资料 |
| [TVMScript 架构](https://tvm.apache.org/docs/arch/tvmscript.html) | 官方资料 |
| [运行时系统](https://tvm.apache.org/docs/arch/runtime.html) | 官方资料 |

## 章末小结

本章的重点不是记住所有类名，而是明确输入状态、处理动作、输出状态和失败处理。接入自研 NPU 时，每一层都应保留可检查的中间结果，使图分区、代码生成、设备提交和数值对照可以分别验证。
