---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 01. 版本基准与学习准备

> [!abstract] 本章内容
> 解释 v0.24.0 与旧教程的差异，建立可复现环境，并给出阅读源码的方法。

## 一句话理解

把 TVM 看成一组可组合的 IR 变换与运行时模块，而不是一个只能按固定命令使用的黑盒工具。

## 核心要点

1. 主线 IR 是 Relax 与 TensorIR；v0.24.0 将旧 `tir` 代码组织拆成 `tirx` 与 `s_tir`。
2. Python 用于组织编译流程，C++ 实现大量核心数据结构、变换、代码生成与运行时。
3. 编译主机与执行设备可以不同，因此所有硬件特征都应由 Target 或编译配置显式提供。
4. 版本号、提交号、LLVM 版本、构建开关和 Python 环境必须一起记录。

```mermaid
flowchart LR
    A["输入程序状态"] --> B["分析信息"]
    B --> C["本章所述处理"]
    C --> D["输出程序状态"]
    D --> E["日志与可复现记录"]
```

## 逐项解析

### 1. 主线 IR 是 Relax 与 TensorIR；v0.24.0 将旧 `tir` 代码组织拆成 `tirx` 与 `s_tir`。

主线 IR 是 Relax 与 TensorIR；v0.24.0 将旧 `tir` 代码组织拆成 `tirx` 与 `s_tir`。

阅读时先定位产生该信息的函数，再查找消费它的下一阶段。把两处代码和中间 IR 放在一起观察，比单独记忆类名更容易理解。

**实现建议：** 固定 TVM tag 或 commit，不在同一项目中混用 main 分支与发布版二进制。

> [!warning] 本小节常见问题
> 旧文章常用 `relay.build`、旧 Pass 名称或旧目录，不能据此判断当前源码缺少功能。

### 2. Python 用于组织编译流程

Python 用于组织编译流程，C++ 实现大量核心数据结构、变换、代码生成与运行时。

把变换前后的 IR 并排比较，重点查看函数参数、调用、属性、StructInfo 和返回值。只记录最终 IR 会丢失变化发生的位置。

**实现建议：** 保存 `tvm.support.libinfo()` 输出，确认 Python 包加载的是预期的动态库。

> [!warning] 本小节常见问题
> 只记录 `pip freeze` 不足以复现本地构建，CMake 开关和本机 LLVM 同样重要。

### 3. 编译主机与执行设备可以不同

编译主机与执行设备可以不同，因此所有硬件特征都应由 Target 或编译配置显式提供。

实现时将硬件固定限制放入 Target 或能力文件，把用户可选策略放入编译配置；二者发生冲突时应报告具体字段。

**实现建议：** 阅读 API 时先看当前版本文档，再用 `rg` 找注册名和测试。

> [!warning] 本小节常见问题
> 编译成功不等于运行设备可见；设备探测属于运行时检查。

### 4. 版本号、提交号、LLVM 版本、构建开关和 Python 环境必须一起记录。

版本号、提交号、LLVM 版本、构建开关和 Python 环境必须一起记录。

测试至少包含一个正常样本和一个只改变单一条件的反向样本。这样，结构或结果变化时能直接找到对应规则。

**实现建议：** 为自研后端建立最小模型集，避免首次联调就使用完整大模型。

> [!warning] 本小节常见问题
> 旧文章常用 `relay.build`、旧 Pass 名称或旧目录，不能据此判断当前源码缺少功能。

## 实施清单

- [ ] 固定 TVM tag 或 commit，不在同一项目中混用 main 分支与发布版二进制。
- [ ] 保存 `tvm.support.libinfo()` 输出，确认 Python 包加载的是预期的动态库。
- [ ] 阅读 API 时先看当前版本文档，再用 `rg` 找注册名和测试。
- [ ] 为自研后端建立最小模型集，避免首次联调就使用完整大模型。

## 设计评审问题

1. 本阶段接收哪一种 IR，要求哪些属性已经存在？
2. 本阶段产生哪些新函数、属性、常量或模块？
3. 遇到不被支持的输入时，是保留给其他后端、报告编译错误，还是插入转换？
4. 哪些配置属于硬件固定限制，哪些配置允许自动搜索？
5. 如何证明重复执行本阶段不会产生额外变化？
6. 如何在日志中解释每个重要决定？

## 自测

> [!question] 请先独立回答
> 把一个两层 MLP 的 IR 在本章处理前后分别打印出来，指出函数数量、调用形式、张量类型和模块属性发生了什么变化。如果无法运行代码，可先画出预期结构，再与实际输出比较。

## 参考资料

| 资料 | 类型 |
| --- | --- |
| [官方源码安装说明](https://tvm.apache.org/docs/install/from_source.html) | 官方资料 |
| [v0.24.0 发布源码](https://github.com/apache/tvm/releases/tag/v0.24.0) | 官方资料 |
| [设计与架构](https://tvm.apache.org/docs/arch/index.html) | 官方资料 |

## 章末小结

本章的重点不是记住所有类名，而是明确输入状态、处理动作、输出状态和失败处理。接入自研 NPU 时，每一层都应保留可检查的中间结果，使图分区、代码生成、设备提交和数值对照可以分别验证。
