---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 35. 编译流程逐阶段检查手册

> [!abstract] 用途
> 本章把一次完整编译和执行拆成可单独检查的阶段。每一阶段都给出输入条件、处理动作、必查内容、证据文件与停止条件，适合新成员学习、自动化编译服务和问题定位。

## 快速目录

- [[#35.1 环境确认|35.1 环境确认]]
- [[#35.2 模型冻结|35.2 模型冻结]]
- [[#35.3 前端导入|35.3 前端导入]]
- [[#35.4 参数分离|35.4 参数分离]]
- [[#35.5 基础规范化|35.5 基础规范化]]
- [[#35.6 常量处理|35.6 常量处理]]
- [[#35.7 低比特整数图准备|35.7 低比特整数图准备]]
- [[#35.8 候选模式发现|35.8 候选模式发现]]
- [[#35.9 能力检查|35.9 能力检查]]
- [[#35.10 模式融合|35.10 模式融合]]
- [[#35.11 外部函数整理|35.11 外部函数整理]]
- [[#35.12 NPU 外部代码生成|35.12 NPU 外部代码生成]]
- [[#35.13 剩余算子逐级转换|35.13 剩余算子逐级转换]]
- [[#35.14 普通算子融合|35.14 普通算子融合]]
- [[#35.15 TensorIR 调度|35.15 TensorIR 调度]]
- [[#35.16 TensorIR 逐级转换|35.16 TensorIR 逐级转换]]
- [[#35.17 目标代码生成|35.17 目标代码生成]]
- [[#35.18 VM 代码生成|35.18 VM 代码生成]]
- [[#35.19 模块链接|35.19 模块链接]]
- [[#35.20 导出|35.20 导出]]
- [[#35.21 独立加载|35.21 独立加载]]
- [[#35.22 常量初始化|35.22 常量初始化]]
- [[#35.23 输入绑定|35.23 输入绑定]]
- [[#35.24 设备执行|35.24 设备执行]]
- [[#35.25 结果核对|35.25 结果核对]]
- [[#35.26 性能测量|35.26 性能测量]]
- [[#35.27 发布归档|35.27 发布归档]]

## 总体状态变化

```mermaid
flowchart LR
    A["源模型"] --> B["Relax"]
    B --> C["NPU 分区"]
    C --> D["外部 Module + TIR"]
    D --> E["VMExecutable"]
    E --> F["部署文件"]
    F --> G["设备执行"]
    G --> H["结果与性能报告"]
```

## 35.1 环境确认

> [!abstract] 阶段目标
> 把“源码、动态库、Python 包与依赖”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：源码、动态库、Python 包与依赖。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

读取提交号、构建选项和加载路径。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

版本和路径完全一致。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `environment.txt 与 libinfo.txt`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

任何版本或路径无法解释时停止后续编译。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.2 模型冻结

> [!abstract] 阶段目标
> 把“源框架模型与示例输入”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：源框架模型与示例输入。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

切换推理状态，固定参数和随机种子。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

训练专用节点已消除或固定。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `model_hash.txt 与 source_output.npz`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

源框架结果不稳定时不进入 TVM。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.3 前端导入

> [!abstract] 阶段目标
> 把“ExportedProgram、ONNX 或 TFLite 对象”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：ExportedProgram、ONNX 或 TFLite 对象。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

调用对应 Relax 前端。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

函数参数、返回、形状和数据类型。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `00_imported.py`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

未被支持的源算子需要自定义转换或更换导出方式。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.4 参数分离

> [!abstract] 阶段目标
> 把“导入后的 IRModule”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：导入后的 IRModule。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

决定常量嵌入或 `detach_params`。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

参数名称、顺序、形状、数据类型和内容散列。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `01_params.json 与 params.npz`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

参数顺序不稳定时不能继续生成外部 ABI。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.5 基础规范化

> [!abstract] 阶段目标
> 把“带前端特征的 Relax”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：带前端特征的 Relax。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

运行规范化、绑定整理和必要的形状推断。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

等价表达式是否变成稳定形式。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `02_normalized.py`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

规范化后 StructInfo 缺失要先修复。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.6 常量处理

> [!abstract] 阶段目标
> 把“规范化模块”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：规范化模块。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

折叠与目标无关的常量计算。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

常量数量、体积与是否破坏组合模式。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `03_constants.py`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

不能把运行期输入错误折叠为常量。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.7 低比特整数图准备

> [!abstract] 阶段目标
> 把“浮点或已有整数模型”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：浮点或已有整数模型。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

按项目流程建立显式转换节点和数值参数。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

scale、zero point、参数维度和输出数据类型。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `04_integer_graph.py`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

数值参数缺失或公式版本不明时停止。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.8 候选模式发现

> [!abstract] 阶段目标
> 把“稳定 Relax 图”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：稳定 Relax 图。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

运行 DPL 匹配但暂不改变模块。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

每类候选数量与结构。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `05_candidates.json`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

候选为 0 时先检查真实图结构。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.9 能力检查

> [!abstract] 阶段目标
> 把“候选子图与 Target”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：候选子图与 Target。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

逐项检查形状、数据类型、属性、布局、工作区和收益。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

接纳结果与原因码。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `06_decisions.json`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

无法证明安全的候选改由其他后端执行。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.10 模式融合

> [!abstract] 阶段目标
> 把“候选与 FusionPattern”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：候选与 FusionPattern。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

执行 FuseOpsByPattern。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

Composite 函数数量、参数与返回。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `07_fused.py`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

具体模式被普通模式抢占时调整优先级。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.11 外部函数整理

> [!abstract] 阶段目标
> 把“带 Composite 的模块”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：带 Composite 的模块。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

执行 MergeCompositeFunctions。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

Codegen、global_symbol 与调用关系。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `08_merged.py`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

符号重复或函数接口不稳定时停止。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.12 NPU 外部代码生成

> [!abstract] 阶段目标
> 把“NPU 外部函数数组”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：NPU 外部函数数组。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

执行 RunCodegen 与自研编译器。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

模块 type key、产物版本、常量目录和命令摘要。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `09_codegen.py、artifact.bin、artifact.json`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

任何静态地址或命令检查失败都阻止输出。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.13 剩余算子逐级转换

> [!abstract] 阶段目标
> 把“NPU 节点已替换的 Relax”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：NPU 节点已替换的 Relax。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

对剩余节点运行 LegalizeOps。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

call_tir、外部函数和未处理节点。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `10_legalized.py`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

主机 Target 缺少实现时补充实现或改变分区。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.14 普通算子融合

> [!abstract] 阶段目标
> 把“包含 call_tir 的模块”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：包含 call_tir 的模块。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

执行 AnnotateTIROpPattern、FuseOps 与 FuseTIR。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

剩余计算内核数量和函数参数。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `11_host_fused.py`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

不得跨 NPU 外部函数错误融合。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.15 TensorIR 调度

> [!abstract] 阶段目标
> 把“未调度 PrimFunc”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：未调度 PrimFunc。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

应用 DLight、MetaSchedule 记录或自定义规则。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

循环、缓冲区、内存作用域和调度轨迹。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `12_scheduled.py 与 schedule_trace.json`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

没有记录的任务必须有基础调度。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.16 TensorIR 逐级转换

> [!abstract] 阶段目标
> 把“已调度 PrimFunc”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：已调度 PrimFunc。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

运行目标所需低层 Pass。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

内建函数、缓冲区展平、主机与设备标记。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `13_tir_lowered.py`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

未被支持的内建函数应在代码生成前被发现。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.17 目标代码生成

> [!abstract] 阶段目标
> 把“低层 TIR IRModule 与 Target”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：低层 TIR IRModule 与 Target。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

调用目标构建函数。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

主机 Module、设备 Module 与导入关系。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `14_native_modules.txt`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

编译期不得查询本机实际 NPU。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.18 VM 代码生成

> [!abstract] 阶段目标
> 把“剩余 Relax 控制函数”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：剩余 Relax 控制函数。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

生成 VM 字节码或编译模式 TIR。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

Call、Ret、Goto、If 和函数目录。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `15_vm.astext`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

控制流与函数参数数量必须一致。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.19 模块链接

> [!abstract] 阶段目标
> 把“VM、主机模块、NPU 模块与常量”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：VM、主机模块、NPU 模块与常量。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

建立 VMExecutable。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

所有外部符号可查且导入关系完整。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `16_linked_modules.txt`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

任一函数缺失时不进入导出。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.20 导出

> [!abstract] 阶段目标
> 把“完整可执行对象”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：完整可执行对象。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

export_library 并保存参数。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

共享库、权重文件、元数据和内容散列。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `model.so、params.npz、manifest.json`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

外部 Module 不可保存时修复序列化。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.21 独立加载

> [!abstract] 阶段目标
> 把“部署文件与仅运行时环境”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：部署文件与仅运行时环境。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

在新进程 load_module。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

函数目录、模块版本和设备能力。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `17_load.log`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

依赖编译进程内全局状态的产物不合格。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.22 常量初始化

> [!abstract] 阶段目标
> 把“已加载模块与权重”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：已加载模块与权重。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

分配设备常量内存并上传。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

常量数量、散列、地址和引用计数。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `18_init.log`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

内容或目标不匹配时拒绝缓存。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.23 输入绑定

> [!abstract] 阶段目标
> 把“运行期 Tensor”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：运行期 Tensor。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

检查参数、形状、数据类型、步长、设备和对齐。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

动态版本、工作区和复制计划。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `19_bind.json`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

超范围输入走明确的其他后端或报告错误。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.24 设备执行

> [!abstract] 阶段目标
> 把“已重定位命令与实际 Tensor”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：已重定位命令与实际 Tensor。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

复制、提交、等待并读取状态。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

提交序号、事件、设备错误和输出。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `20_runtime.log 与 commands.disasm`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

超时后不得继续使用旧事件和地址。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.25 结果核对

> [!abstract] 阶段目标
> 把“源框架、CPU 和 NPU 输出”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：源框架、CPU 和 NPU 输出。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

按数值合同比较并定位第一个差异。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

位级结果或误差统计。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `21_compare.json 与 failing_case.npz`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

功能未通过时性能结果无效。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.26 性能测量

> [!abstract] 阶段目标
> 把“功能正确的稳定产物”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：功能正确的稳定产物。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

预热、多次重复并读取设备计数。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

复制、提交、执行、等待、带宽和利用率。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `22_perf.json`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

网络传输和日志输出不得混入设备执行时间。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 35.27 发布归档

> [!abstract] 阶段目标
> 把“通过回归的产物与报告”处理成下一阶段可以可靠接收的状态，并留下足够证据。

### 输入条件

本阶段输入是：通过回归的产物与报告。进入前先确认上一阶段的错误已经处理，文件内容散列和配置仍与记录一致。若输入来自缓存，要验证缓存键包含 TVM、后端、Target、模型和数值配置版本。

### 处理动作

生成兼容表、已知限制和重建信息。执行动作时使用单独的 Pass 或函数，避免在多个阶段共享不可见的全局可变状态。编译服务并行处理多个模型时，每个任务拥有独立上下文。

### 必查内容

所有版本与散列可追溯。检查既包含“存在什么”，也包含“不应存在什么”。例如外部代码生成后应出现 NPU Module，但原组合函数不应继续作为未处理调用残留。

### 证据文件

保存 `release_manifest.yaml`。文本证据使用稳定排序，二进制证据同时保存内容散列和解析摘要。IR 证据优先保存 `mod.script()`，结构测试使用结构相等 API。

### 失败处理

缺少任一关键版本时不作为正式发布。失败时保持原始输入、阶段配置、异常栈和部分输出；不要用下一阶段的自动修复掩盖本阶段错误。

### 评审问题

1. 本阶段是否读取了未记录的环境变量？
2. 相同输入重复执行是否得到结构相同的输出？
3. 缓存失效条件是否完整？
4. 错误能否指向函数、节点、字段或命令？
5. 本阶段输出是否能被一个独立小测试消费？

> [!tip] 初学者检查法
> 第一次执行时同时保存处理前后文本，用差异工具只看本阶段变化。若一次出现大量不相关变化，说明阶段职责过大，应继续拆分。

## 证据目录建议

```text
build-report/
├── environment/
├── ir/
├── partition/
├── npu-artifact/
├── native-modules/
├── executable/
├── runtime/
├── compare/
└── performance/
```
