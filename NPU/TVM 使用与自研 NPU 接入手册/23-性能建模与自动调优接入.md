---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 23. 性能建模与自动调优接入

> [!abstract] 本章内容
> 建立计算、片外访问、片上搬运、提交和同步的时间模型，并说明如何把 NPU 测量接入搜索。

## 一句话理解

端到端时延由多个部分组成，单看运算次数或峰值吞吐不能解释真实表现。

## 核心要点

1. 屋顶线模型可帮助判断任务主要受算力还是带宽限制。
2. 分块选择会同时影响复用、片上占用、尾部开销和并行度。
3. 组合运算减少中间写回，但也可能增加片上同时存活数据。
4. 异步执行需要分别测量提交、排队、设备执行和等待。
5. 搜索 Runner 应返回稳定时间与明确错误类别。

```mermaid
flowchart LR
    A["输入程序状态"] --> B["分析信息"]
    B --> C["本章所述处理"]
    C --> D["输出程序状态"]
    D --> E["日志与可复现记录"]
```

## 逐项解析

### 1. 屋顶线模型可帮助判断任务主要受算力还是带宽限制。

屋顶线模型可帮助判断任务主要受算力还是带宽限制。

阅读时先定位产生该信息的函数，再查找消费它的下一阶段。把两处代码和中间 IR 放在一起观察，比单独记忆类名更容易理解。

**实现建议：** 为每个外部函数统计输入输出字节、MAC 数、命令数和估计分块数。

> [!warning] 本小节常见问题
> 只报告 TOPS 利用率会忽略外部函数之间的数据搬运。

### 2. 分块选择会同时影响复用、片上占用、尾部开销和并行度。

分块选择会同时影响复用、片上占用、尾部开销和并行度。

把变换前后的 IR 并排比较，重点查看函数参数、调用、属性、StructInfo 和返回值。只记录最终 IR 会丢失变化发生的位置。

**实现建议：** 从设备计数器读取周期、忙碌周期、读写字节和停顿原因。

> [!warning] 本小节常见问题
> 预热次数不足会把频率提升和缓存填充算入结果。

### 3. 组合运算减少中间写回

组合运算减少中间写回，但也可能增加片上同时存活数据。

实现时将硬件固定限制放入 Target 或能力文件，把用户可选策略放入编译配置；二者发生冲突时应报告具体字段。

**实现建议：** 分别建立冷启动与稳态测试。

> [!warning] 本小节常见问题
> 把失败候选记成极大时间而不保存原因，会掩盖生成器缺陷。

### 4. 异步执行需要分别测量提交、排队、设备执行和等待。

异步执行需要分别测量提交、排队、设备执行和等待。

测试至少包含一个正常样本和一个只改变单一条件的反向样本。这样，结构或结果变化时能直接找到对应规则。

**实现建议：** 用真实常见尺寸训练代价模型，不只用规则方阵。

> [!warning] 本小节常见问题
> 只报告 TOPS 利用率会忽略外部函数之间的数据搬运。

### 5. 搜索 Runner 应返回稳定时间与明确错误类别。

搜索 Runner 应返回稳定时间与明确错误类别。

保存可由工具读取的阶段报告，其中包含输入摘要、输出摘要、Pass 配置、Target、耗时和错误；文本日志用于辅助阅读。

**实现建议：** 保留基础方案，调优记录不可用时仍能编译。

> [!warning] 本小节常见问题
> 预热次数不足会把频率提升和缓存填充算入结果。

## 实施清单

- [ ] 为每个外部函数统计输入输出字节、MAC 数、命令数和估计分块数。
- [ ] 从设备计数器读取周期、忙碌周期、读写字节和停顿原因。
- [ ] 分别建立冷启动与稳态测试。
- [ ] 用真实常见尺寸训练代价模型，不只用规则方阵。
- [ ] 保留基础方案，调优记录不可用时仍能编译。

## 设计评审问题

1. 本阶段接收哪一种 IR，要求哪些属性已经存在？
2. 本阶段产生哪些新函数、属性、常量或模块？
3. 遇到不被支持的输入时，是保留给其他后端、报告编译错误，还是插入转换？
4. 哪些配置属于硬件固定限制，哪些配置允许自动搜索？
5. 如何证明重复执行本阶段不会产生额外变化？
6. 如何在日志中解释每个重要决定？

## 自测

> [!question] 请先独立回答
> 把一个两层 MLP 的 IR 在本章处理前后分别打印出来，指出函数数量、调用形式、张量类型和模块属性发生了什么变化。如果无法运行代码，可先画出预期结构，再与实际输出比较。

## 参考资料

| 资料 | 类型 |
| --- | --- |
| [MetaSchedule](https://tvm.apache.org/docs/deep_dive/tensor_ir/tutorials/meta_schedule.html) | 官方资料 |
| [自定义优化](https://tvm.apache.org/docs/how_to/tutorials/customize_opt.html) | 官方资料 |
| [RPC 教程](https://tvm.apache.org/docs/how_to/tutorials/cross_compilation_and_rpc.html) | 官方资料 |

## 章末小结

本章的重点不是记住所有类名，而是明确输入状态、处理动作、输出状态和失败处理。接入自研 NPU 时，每一层都应保留可检查的中间结果，使图分区、代码生成、设备提交和数值对照可以分别验证。
