---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 28. 常见问题

> [!abstract] 本章内容
> 集中回答安装、IR、分区、外部代码生成、运行时、动态尺寸、数值和性能方面的常见疑问。

## 一句话理解

先按阶段分类，再依据 IR、模块和设备证据回答；不要把所有问题都归因于硬件或 TVM。

## 核心要点

1. 模型能在 CPU 运行但 NPU 无子图，通常先检查图结构与接纳函数。
2. 分区成功但 RunCodegen 找不到函数，通常检查注册名和构建开关。
3. 内存内运行成功但导出失败，通常检查 Module 保存与加载注册。
4. 小尺寸更慢，可能是提交和复制固定开销占比过高。
5. 动态尺寸不进入 NPU，可能是后端没有声明可接受范围或运行时缺少形状参数。

```mermaid
flowchart LR
    A["输入程序状态"] --> B["分析信息"]
    B --> C["本章所述处理"]
    C --> D["输出程序状态"]
    D --> E["日志与可复现记录"]
```

## 逐项解析

### 1. 模型能在 CPU 运行但 NPU 无子图

模型能在 CPU 运行但 NPU 无子图，通常先检查图结构与接纳函数。

阅读时先定位产生该信息的函数，再查找消费它的下一阶段。把两处代码和中间 IR 放在一起观察，比单独记忆类名更容易理解。

**实现建议：** 按 [[32-故障速查表]] 收集最小证据。

> [!warning] 本小节常见问题
> 只提供最终异常栈而没有中间 IR，通常无法判断分区阶段问题。

### 2. 分区成功但 RunCodegen 找不到函数

分区成功但 RunCodegen 找不到函数，通常检查注册名和构建开关。

把变换前后的 IR 并排比较，重点查看函数参数、调用、属性、StructInfo 和返回值。只记录最终 IR 会丢失变化发生的位置。

**实现建议：** 检查 `get_global_func` 和 `tvm.support.libinfo()`。

> [!warning] 本小节常见问题
> 只用一个随机输入对照，不能覆盖极值与尾部。

### 3. 内存内运行成功但导出失败

内存内运行成功但导出失败，通常检查 Module 保存与加载注册。

实现时将硬件固定限制放入 Target 或能力文件，把用户可选策略放入编译配置；二者发生冲突时应报告具体字段。

**实现建议：** 分别测试分区、代码生成、模块加载与设备提交。

> [!warning] 本小节常见问题
> 忽略版本信息会让相同代码在不同构建中表现不同。

### 4. 小尺寸更慢

小尺寸更慢，可能是提交和复制固定开销占比过高。

测试至少包含一个正常样本和一个只改变单一条件的反向样本。这样，结构或结果变化时能直接找到对应规则。

**实现建议：** 把不被接纳原因打印成结构化日志。

> [!warning] 本小节常见问题
> 只提供最终异常栈而没有中间 IR，通常无法判断分区阶段问题。

### 5. 动态尺寸不进入 NPU

动态尺寸不进入 NPU，可能是后端没有声明可接受范围或运行时缺少形状参数。

保存可由工具读取的阶段报告，其中包含输入摘要、输出摘要、Pass 配置、Target、耗时和错误；文本日志用于辅助阅读。

**实现建议：** 对照 [[30-自研 NPU 算子接入分册]] 的运算合同。

> [!warning] 本小节常见问题
> 只用一个随机输入对照，不能覆盖极值与尾部。

## 实施清单

- [ ] 按 [[32-故障速查表]] 收集最小证据。
- [ ] 检查 `get_global_func` 和 `tvm.support.libinfo()`。
- [ ] 分别测试分区、代码生成、模块加载与设备提交。
- [ ] 把不被接纳原因打印成结构化日志。
- [ ] 对照 [[30-自研 NPU 算子接入分册]] 的运算合同。

## 设计评审问题

1. 本阶段接收哪一种 IR，要求哪些属性已经存在？
2. 本阶段产生哪些新函数、属性、常量或模块？
3. 遇到不被支持的输入时，是保留给其他后端、报告编译错误，还是插入转换？
4. 哪些配置属于硬件固定限制，哪些配置允许自动搜索？
5. 如何证明重复执行本阶段不会产生额外变化？
6. 如何在日志中解释每个重要决定？

## 自测

> [!question] 请先独立回答
> 把一个两层 MLP 的 IR 在本章处理前后分别打印出来，指出函数数量、调用形式、张量类型和模块属性发生了什么变化。如果无法运行代码，可先画出预期结构，再与实际输出比较。

## 参考资料

| 资料 | 类型 |
| --- | --- |
| [BYOC 教程](https://tvm.apache.org/docs/how_to/tutorials/bring_your_own_codegen.html) | 官方资料 |
| [Relax VM](https://tvm.apache.org/docs/arch/relax_vm.html) | 官方资料 |
| [Target 与 Device](https://tvm.apache.org/docs/arch/device_target_interactions.html) | 官方资料 |

## 章末小结

本章的重点不是记住所有类名，而是明确输入状态、处理动作、输出状态和失败处理。接入自研 NPU 时，每一层都应保留可检查的中间结果，使图分区、代码生成、设备提交和数值对照可以分别验证。
