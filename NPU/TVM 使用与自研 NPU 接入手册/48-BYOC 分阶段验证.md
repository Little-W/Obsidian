---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 48. BYOC 分阶段验证

> [!abstract] 本章内容
> 提供从纯 Python 模式检查到真实 NPU 空 SDK 适配的连续验证步骤，每一步都列出命令、预期 IR、检查点、常见错误和保存材料。

> [!note] 官方依据
> [官方 BYOC 可运行教程](https://tvm.apache.org/docs/how_to/tutorials/bring_your_own_codegen.html) 与 `tests/python/contrib/test_example_npu.py`。

## 本章使用说明

先执行或检查本章给出的最小示例，再按快速目录逐项核对。每一节都使用相同的七个观察角度：功能与适用范围、官方实现、自研方法、必查信息、拒绝与异常、测试、应保留证据。这种组织便于快速定位实现位置，也便于后端开发者把内容直接转换为开发任务与测试。

遇到 API 名称时保留源码拼写，例如 `FusionPattern`、`FuseOpsByPattern`、`RunCodegen` 和 `runtime.Module`；解释文字使用 [[33-术语与 API 速查]] 中的标准中文。若某个接口与本地构建不同，先确认 TVM 提交与 Python 加载的动态库，再查固定 tag 的源码和测试。

## 快速目录

- [[#48.1 验证一：查询模式注册表|48.1 验证一：查询模式注册表]]
- [[#48.2 验证二：打印原始 Relax|48.2 验证二：打印原始 Relax]]
- [[#48.3 验证三：只执行 FuseOpsByPattern|48.3 验证三：只执行 FuseOpsByPattern]]
- [[#48.4 验证四：加入 MergeCompositeFunctions|48.4 验证四：加入 MergeCompositeFunctions]]
- [[#48.5 验证五：执行 RunCodegen|48.5 验证五：执行 RunCodegen]]
- [[#48.6 验证六：Example NPU 运行时|48.6 验证六：Example NPU 运行时]]
- [[#48.7 验证七：最小自研运行时|48.7 验证七：最小自研运行时]]
- [[#48.8 验证八：导出与新进程加载|48.8 验证八：导出与新进程加载]]

## 48.0 验证环境

```bash
git clone --depth 1 --branch v0.24.0 \
  git@github.com:apache/tvm.git tvm-v0.24.0
cd tvm-v0.24.0
cp cmake/config.cmake .
```

在 `config.cmake` 中打开 Example NPU 编译器、运行时与 LLVM，然后按 [[11-源码构建与第一个程序]] 完成构建。每项验证在新 Python 进程运行，先打印 `tvm.support.libinfo()` 并保存当前提交号。

> [!tip] 前六项验证不要求真实设备
> 模式注册和分区是纯 Python 变换。代码生成和 Example NPU 运行时需要相应 CMake 开关，但不需要真实 NPU。只有替换为自研 SDK 后的设备执行验证需要驱动与硬件。

## 48.1 验证一：查询模式注册表

> [!abstract] 本节内容
> 导入 Example NPU 并打印按优先级排列的模式名称

### 功能与适用范围

导入 Example NPU 并打印按优先级排列的模式名称。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方教程第一步导入注册模块，再调用 `get_patterns_with_prefix`。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

复制脚本改为自研前缀，输出核心模式、可选模式和版本。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

Python 环境、导入路径、模式数量、名称、顺序和检查函数存在性。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

列表为空时先检查加载的 TVM 源码和 Python 包，不继续分区。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

断言 MatMul、Conv2D 和融合模式存在，断言融合模式优先。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

终端输出、模式清单 JSON、TVM 提交和 Python 路径。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 48.2 验证二：打印原始 Relax

> [!abstract] 本节内容
> 用 TVMScript 建立 MatMul+ReLU 并熟悉函数、数据流块与 StructInfo

### 功能与适用范围

用 TVMScript 建立 MatMul+ReLU 并熟悉函数、数据流块与 StructInfo。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方教程使用固定形状 `float32` 的两输入模块。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

再增加一个只改变数据类型的模块，为后续检查函数准备对照。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

main 参数、矩阵维度、返回形状、数据流输出和默认属性。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

原始模块自身无效时不得用分区结果掩盖错误。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

CPU 构建并与 NumPy 比较，保存 `mod.script()`。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

TVMScript、打印 IR、输入、输出和结构散列。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 48.3 验证三：只执行 FuseOpsByPattern

> [!abstract] 本节内容
> 观察 Composite 与 Codegen 属性出现的位置

### 功能与适用范围

观察 Composite 与 Codegen 属性出现的位置。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方教程设置 `bind_constants=False` 与 `annotate_codegen=True`。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

分别改变两个标志，整理四种输出的函数层次和参数差异。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

函数数量、属性、常量、参数、外部符号和主函数调用。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

没有任何命中时不调用 RunCodegen，先查看模式和检查结果。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

对四种配置做结构快照，确认理解每个标志。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

四份 IR、配置表、命中模式和函数数量。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 48.4 验证四：加入 MergeCompositeFunctions

> [!abstract] 本节内容
> 比较加入分组 Pass 前后的外部函数数量和依赖

### 功能与适用范围

比较加入分组 Pass 前后的外部函数数量和依赖。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方流程在 FuseOpsByPattern 后执行 MergeCompositeFunctions。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

构造两个连续 Composite、一个分叉和一个跨后端示例。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

组数量、每组 Composite、输入输出、依赖和符号。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

输出形成循环或丢失组外使用值时视为测试失败。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

结构比较加上自定义分组报告，逐图核对。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

合并前后 IR、分组图、依赖表和原因。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 48.5 验证五：执行 RunCodegen

> [!abstract] 本节内容
> 查看调用变成 call_dps_packed，并确认 external_mods 出现

### 功能与适用范围

查看调用变成 call_dps_packed，并确认 external_mods 出现。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方教程仅在两个全局函数均存在时执行此步。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

在执行前打印注册查询；执行后遍历模块属性和导入模块。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

ExternFunc、外部符号、参数、返回 StructInfo、模块 type key 和常量表。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

缺少构建开关时应跳过并给出重建提示，不应把导入错误当模式错误。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

断言外部调用存在、模块数大于零、原 Codegen 函数已清理。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

RunCodegen 前后 IR、模块属性和注册查询。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 48.6 验证六：Example NPU 运行时

> [!abstract] 本节内容
> 运行 Example NPU，阅读执行单元、内存层次和融合日志，同时牢记结果数值不可信

### 功能与适用范围

运行 Example NPU，阅读执行单元、内存层次和融合日志，同时牢记结果数值不可信。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方教程只断言输出形状，因为 Run 没有真实计算。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

把日志关联到 Composite 名称和 JSON 节点，理解真正运行时需替换的位置。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

输入形状、日志节点数、执行单元、融合提示、输出形状和初始化信息。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

不能用示例运行时的数组内容进行误差比较或作为参考数据。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

运行 MatMul+ReLU 与 Conv2D+ReLU，确认日志中的融合名称。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

完整日志、JSON 图、输出形状和模块 type key。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 48.7 验证七：最小自研运行时

> [!abstract] 本节内容
> 把示例 Run 替换为一个可验证的 CPU 参考实现或 SDK 空适配层

### 功能与适用范围

把示例 Run 替换为一个可验证的 CPU 参考实现或 SDK 空适配层。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方建议替换 example_npu_runtime.cc 中的硬件调用位置。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

第一步可在 C++ 中执行 MatMul 参考计算，随后只替换提交函数为 SDK。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

Tensor ABI、函数分派、输入输出地址、错误码、同步和资源清理。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

仍没有写输出却开始比较数值，或 CPU 参考与设备路径无法切换时停止扩展。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

与 NumPy 位级或容差比较，测试错误参数和重复运行。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

实现模式、结果对照、SDK 调用日志和错误测试。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 48.8 验证八：导出与新进程加载

> [!abstract] 本节内容
> 证明产物不依赖编译进程中的 Python 对象和后端编译器

### 功能与适用范围

证明产物不依赖编译进程中的 Python 对象和后端编译器。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方真实部署教程用 `export_library` 保存，再用 `tvm.runtime.load_module` 加载。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

建立两个脚本：一个只编译导出，一个只链接运行时并加载执行。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

共享库、导入模块、权重文件、RPATH、运行时注册、设备和入口符号。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

加载进程仍需源码框架、编译器 SDK 或临时调试文件时说明部署包不完整。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

在干净环境执行加载脚本，重复运行并比较参考结果。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

部署目录、依赖清单、加载日志、输出和文件散列。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 本章检查清单

- [ ] 能用自己的话说明本章每个 TVM 属性或 API 的作用；
- [ ] 能在变换前后 IR 中找到对应结构；
- [ ] 能指出 Example NPU 中仅仅用于说明机制的部分；
- [ ] 能写出自研后端的输入、输出、失败原因和测试；
- [ ] 能保存足以在另一进程或另一台开发机复现的证据；
- [ ] 能说明本章内容与前后章节的关系。

## 继续阅读

本章属于 BYOC 专题。建议同时参照 [[15-Relax DPL 子图识别与分区]]、[[16-BYOC 代码生成器]]、[[17-NPU 运行时与驱动适配]]、[[26-端到端参考实现骨架]] 和 [[29-官方资料与源码导航]]。

## 实现说明与验证步骤

> [!note] 依据与适用范围
> 本节依据所列 Apache TVM 官方资料和 v0.24.0 源码整理，给出输入条件、操作步骤、预期结果、异常处理和自研 NPU 适配要求。接口细节以固定版本源码与测试为准，在线页面用于补充说明。

### 48.A 目标与适用范围

把 BYOC 分成可独立执行的七个实验，每次检查一个新结构，避免一开始就执行完整模型。

参考样本保持输入规模较小，以便直接检查对象、字段和输出。首次执行时不要同时加入图改写、外部代码生成和真实设备执行；应先确认当前阶段的输入与输出，再增加后续处理。建议为该项验证建立独立目录，保存命令、标准输出、IR、编译产物摘要和环境信息。

### 48.B 参考实现

**官方依据：** [BYOC 教程](https://tvm.apache.org/docs/how_to/tutorials/bring_your_own_codegen.html) 与 [External Library Dispatch](https://tvm.apache.org/docs/arch/external_library_dispatch.html)。

**v0.24.0 源码位置：** `docs/how_to/tutorials/bring_your_own_codegen.py`、BYOC 相关 Python 与 C++ 测试。

```text
E1 import backend and list patterns
E2 match one pattern without rewriting
E3 FuseOpsByPattern and inspect Composite
E4 MergeCompositeFunctions and inspect Codegen
E5 RunCodegen and inspect external_mods
E6 export and reload in a new process
E7 execute simulator, then real device
```

按以下顺序执行：

1. 在新进程中确认 `tvm.__file__`、动态库路径和 TVM 提交，避免受到旧进程注册状态影响；
2. 将参考实现保存到单独文件，只补充本节明确要求的输入对象，不先加入额外优化；
3. 执行后保存完整输出；若生成 IR，同时保存 `script(show_meta=True)` 的文本；
4. 把输出与下面的预期现象逐项比较，不以“没有抛出异常”代替功能检查；
5. 重复执行一次并比较主要产物，确认结果没有依赖临时全局状态或未固定的遍历顺序。

> [!success] 预期现象
> 每个实验的输入都来自前一实验的已保存产物，同时也可独立重建。结构断言比全文字符串比较更能容忍无关打印变化。

> [!tip] 输出检查顺序
> 先看对象类型和函数数量，再看属性、调用形式、形状与数据类型，最后看运行结果或编译产物。若前一项不符合预期，先停止后续步骤。这样能把问题限定在最早出现差异的阶段。

### 48.C 单项条件验证

故意让 E4 不执行，直接运行 E5。观察代码生成器得到的函数分组是否符合预期，并理解合并 Pass 何时可选、何时是项目设计的一部分。

执行条件变化样本时，复制参考实现的输入与环境，只修改上文指出的一项。对两个输出进行结构比较，并记录以下四项：

1. 第一次出现差异的是哪一个阶段？
2. 差异表现为函数、属性、形状、数据类型、编译产物还是运行结果？
3. 当前行为是明确拒绝、交给其他后端执行，还是编译错误？
4. 日志是否足以让另一位开发者不查看本次进程状态也能复现？

如果同时观察到多个变化，应继续缩小条件变化样本。参考实现用于确认正常处理，单项条件验证用于确认系统为何接受、拒绝或转交当前输入。

### 48.D 自研 NPU 适配要求

真实后端增加 E8：混合主机与 NPU 子图；E9：动态尺寸；E10：并发与异常注入。每个实验保留设备端任务编号。

改造时建议保留三份相互独立的输入：最小 Relax 或 TensorIR、设备编译器输入、运行时调用输入。三份输入使用同一个样本编号，并记录转换前后的关键字段。这样可以分别测试 TVM 变换、NPU 编译器和驱动，不需要每次都运行完整模型。

至少补充以下样本：

- 一个完全满足能力要求的正向样本；
- 一个只改变数据类型的反向样本；
- 一个只改变形状或属性的反向样本；
- 一个包含主机与 NPU 混合执行的样本；
- 一个导出后在新进程加载的样本；
- 若支持动态尺寸，再增加最小值、常用值和最大值附近的样本。

### 48.E 源码定位

从上文列出的源码位置选择一个公开 API，依次找到 Python 调用、FFI 注册、C++ 实现和测试。记录函数签名、主要输入、主要输出、会写入的模块属性以及失败方式。若名称只在文档出现而源码中找不到，继续搜索注册字符串、属性键或错误文本。

> [!important] 验证项目
> 1. 执行前记录参考实现的对象关系；2. 执行后用实际输出修正记录；3. 增加一个会被明确拒绝的输入；4. 标明自研 NPU 接入需要修改的层次与保持不变的层次；5. 把结果整理成可由自动测试检查的断言。

### 48.F 检查清单

- [ ] 示例可在固定 v0.24.0 环境重复执行，或已明确列出所需可选依赖；
- [ ] 预期现象包含可检查的结构、字段或数值，不只是“运行成功”；
- [ ] 单条件变化能触发预期的拒绝、转交或结构差异；
- [ ] 能从官方页面定位到固定版本源码和测试；
- [ ] NPU 改造说明包含编译阶段、运行阶段与部署阶段；
- [ ] 失败时保留最小输入、环境、阶段输出和第一个错误。
