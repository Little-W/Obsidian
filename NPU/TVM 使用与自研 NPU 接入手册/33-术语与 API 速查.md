---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 33. 术语与 API 速查

> [!important] 中文术语约定
> 本手册使用“批次”“批次大小”“批次维度”“形状”“秩”“维度编号”“数据布局”“分块”。代码参数 `axis` 保留英文；正文写“沿第 k 维”“归约维度”或“维度编号”，不使用含糊缩写。

## 33.1 采用这些写法的依据

- PaddlePaddle 的 [take_along_axis API](https://www.paddlepaddle.org.cn/documentation/docs/zh/api/paddle/take_along_axis_cn.html) 将 `axis` 说明为“指定沿着哪个维度”。
- PaddlePaddle 的 [数据加载文档](https://www.paddlepaddle.org.cn/documentation/docs/zh/2.3/guides/beginner/data_load_cn.html) 使用“批次大小”和“每批次读取样本数”。
- 昇腾 CANN 的 [Gather 文档](https://www.hiascend.com/document/detail/zh/canncommercial/800/apiref/aicpuopapi/atlaste_07_0062.html) 在参数说明中把 `axis` 解释为“指定切片数据所在的维度”，并把 `batch_dims` 解释为“批处理的维度数”。
- Apache TVM 的 [TensorIR 文档](https://tvm.apache.org/docs/deep_dive/tensor_ir/index.html) 与 [Relax 文档](https://tvm.apache.org/docs/deep_dive/relax/index.html) 用于确认英文 API 与源码概念。

不同厂商文档偶尔使用不同中文。本手册优先选择能直接说明张量位置或大小的完整表达，并在代码处保留英文参数名。

## 33.2 禁止混用的例子

| 不推荐 | 推荐 | 原因 |
| --- | --- | --- |
| 批次维度的简称 | 批次维度 | 不使用不规范缩写 |
| 英文参数与中文概念混写 | 第 k 维 / 维度编号 | 区分参数名与概念 |
| 形状大小 | 形状 / 某维度大小 | 区分整个形状与单一数值 |
| rank 大小 | 秩 | rank 表示维数 |
| 数据布局格式 | 数据布局 | 与数据类型、文件格式区分 |
| 英文术语直接作动词 | 分块 | 使用明确中文动作 |
| op | 算子 | 非代码处使用完整名称 |
| 英文术语直接代替处理说明 | 改由主机或其他后端执行 | 说明实际行为 |

## 33.3 快速表

| 英文或 API | 本手册中文 | 定义 |
| --- | --- | --- |
| `Tensor` | 张量 | 具有统一数据类型的多维数据对象；由形状、数据类型、设备和可选步长描述。 |
| `Scalar` | 标量 | 不带张量维度的单个数值。 |
| `Vector` | 向量 | 一维张量；在硬件语境中也可指一次并行处理的多个元素。 |
| `Matrix` | 矩阵 | 二维张量；批次矩阵则带有一个或多个前导批次维度。 |
| `Dimension` | 维度 | 张量形状中的一个位置，不等同于该位置的大小。 |
| `Dimension size` | 维度大小 | 某一维度包含的元素数量，例如形状 `[2,3]` 的第 1 维大小为 3。 |
| `Axis parameter` | 维度编号参数 | API 中的 `axis`；正文写成“沿第 k 维”或“归约维度”，保留代码里的参数名。 |
| `Rank / ndim` | 秩 / 维数 | 张量拥有的维度数量；形状 `[2,3,4]` 的秩为 3。 |
| `Shape` | 形状 | 按顺序列出的各维度大小，例如 `[batch, sequence, hidden]`。 |
| `Dynamic shape` | 动态形状 | 至少一个维度大小在编译期不是固定常量。 |
| `Static shape` | 静态形状 | 每个维度大小在编译期已经确定。 |
| `Symbolic shape` | 符号形状 | 用符号变量和表达式表示维度大小及其关系。 |
| `Batch` | 批次 | 一次共同处理的一组样本或请求。 |
| `Batch size` | 批次大小 | 一个批次包含的样本或请求数量。 |
| `Batch dimension` | 批次维度 | 形状中代表批次的维度；避免使用缩写式中文。 |
| `Batch dimensions` | 批处理维度数 | Gather 等 API 的 `batch_dims`，表示有多少个前导维度按批次独立处理。 |
| `Sequence length` | 序列长度 | 序列包含的词元、时间步或采样点数量。 |
| `Channel` | 通道 | 特征图或向量的一类特征维度。 |
| `Hidden size` | 隐藏层维度大小 | Transformer 等模型中隐藏表示最后一维的大小。 |
| `Head` | 注意力头 | 多头注意力中独立计算的一组 Q、K、V 子空间。 |
| `Head dimension` | 单头维度大小 | 每个注意力头的特征维度大小。 |
| `Data type / dtype` | 数据类型 | 元素编码与位宽，例如 float32、int8。API 字段保留 `dtype`。 |
| `Layout` | 数据布局 | 逻辑维度到物理存储次序、分块和填充的完整组织方式。 |
| `Data format` | 数据格式 | 常用于 NCHW、NHWC 等公开布局名称，也可包含数据类型约定。 |
| `Stride (buffer)` | 缓冲区步长 | 逻辑索引在某一维增加 1 时，物理地址前进的元素数或字节数。 |
| `Stride (convolution)` | 卷积步幅 | 卷积窗口每次在空间维度移动的距离。 |
| `Padding` | 填充 | 在输入边缘补充元素，或把维度大小补到硬件所需倍数。 |
| `Dilation` | 膨胀系数 | 卷积核相邻采样点之间的间隔。 |
| `Groups` | 分组数 | 卷积输入与输出通道被分成多少组独立计算。 |
| `Kernel (convolution)` | 卷积核 | 卷积权重及其空间窗口。 |
| `Kernel (compiled)` | 计算内核 | 可被运行时调用的一段已编译设备程序。 |
| `Operator` | 算子 | 图 IR 中表示一种计算的节点或运算类别。 |
| `Operation` | 运算 | 一次具体数学或数据处理动作；与“算子定义”按上下文区分。 |
| `Elementwise` | 逐元素 | 对对应元素独立执行同一运算。 |
| `Broadcasting` | 广播 | 按规则扩展大小为 1 或缺省的前导维度，以参与逐元素运算。 |
| `Reduction` | 归约 | 沿一个或多个维度把多个元素合成较少元素，例如求和或最大值。 |
| `Permutation` | 维度重排 | 改变张量各维度的次序。 |
| `Transpose` | 转置 | 二维矩阵交换行列，或高维张量按给定维度次序重排。 |
| `Reshape` | 形状变换 | 在元素线性次序不变时改变形状；连续 Tensor 可仅修改元数据。 |
| `Slice` | 切片 | 从一个或多个维度取得范围子集。 |
| `Gather` | 索引收集 | 按照索引从指定维度取得元素或切片。 |
| `Scatter` | 索引写入 | 按照索引把更新值写入目标 Tensor。 |
| `Concatenate` | 拼接 | 沿指定维度连接多个 Tensor。 |
| `Split` | 拆分 | 沿指定维度把一个 Tensor 分为多个 Tensor。 |
| `Tiling` | 分块 | 把大计算和大 Tensor 划分为适合片上存储与执行单元的小块。 |
| `Tile` | 分块 | 分块处理产生的一个具体小块；代码标识符可保留 `tile`。 |
| `Tail` | 尾部 | 维度大小不能整除物理分块或向量宽度时剩余的有效元素。 |
| `Mask` | 掩码 | 标出有效元素、允许位置或需要忽略的位置。 |
| `Alignment` | 对齐 | 地址、步长或大小需要满足的整数倍要求。 |
| `Packing` | 打包 | 把多个低位宽元素放入一个字节或机器字。 |
| `Unpacking` | 解包 | 从打包存储中恢复独立元素。 |
| `Zero point` | 零点 | 整数存储值中对应实数 0 的值。 |
| `Scale` | 缩放系数 | 整数存储值与实数之间换算所用的正数系数。 |
| `Per-tensor` | 按张量 | 整个 Tensor 共用一组参数。 |
| `Per-channel` | 按通道 | 每个通道拥有独立参数。 |
| `Accumulator` | 累加器 | 保存乘加或归约中间结果的寄存器或缓冲区。 |
| `Rounding` | 舍入 | 把高精度数值转换为较低精度时选择相邻可表示值的规则。 |
| `Saturation` | 饱和 | 超出数据类型范围时限制到最小值或最大值。 |
| `Clipping` | 裁剪 | 把数值限制到指定区间。 |
| `Overflow` | 溢出 | 结果超过当前表示范围。 |
| `Underflow` | 下溢 | 数值绝对值太小，无法由当前格式正常表示。 |
| `IR` | 中间表示 | 编译器内部用于分析和变换程序的数据结构。 |
| `IRModule` | IR 模块 | 保存 Relax 函数、TensorIR PrimFunc、全局信息与模块属性的容器。 |
| `Relax` | Relax 图级 IR | TVM 中描述模型计算图、控制流、形状和高层调用的 IR。 |
| `TensorIR` | 张量程序 IR | TVM 中描述循环、缓冲区、计算块和低层张量程序的 IR。 |
| `PrimFunc` | 原始函数 | TensorIR 中可独立编译的低层函数。 |
| `SBlock` | 可调度计算块 | v0.24.0 TensorIR 中带读写区域和迭代变量信息的计算块。 |
| `StructInfo` | 结构信息 | Relax 表达式的形状、数据类型、设备等静态可知信息。 |
| `GlobalVar` | 全局变量 | IRModule 内引用全局函数的符号对象。 |
| `Dataflow block` | 数据流块 | Relax 中主要表达局部无副作用计算依赖的块。 |
| `TVMScript` | TVM 脚本语言 | 使用 Python 语法书写和打印 TVM IR 的领域专用语言。 |
| `Pass` | 编译变换 | 把一种程序状态转换为另一种程序状态的编译步骤。 |
| `PassContext` | 编译变换上下文 | 控制变换配置、优化级别、禁用项和插桩的上下文。 |
| `Pipeline` | 编译流程 | 按明确顺序组合的一组编译变换。 |
| `Normalization` | 规范化 | 把多种等价写法整理成后续分析更容易处理的形式。 |
| `Legalization` | 算子逐级转换 | 把高层算子转换成 TensorIR 调用或外部函数调用。 |
| `Lowering` | 逐级转换 | 逐步把高层程序转换成更接近目标代码的表示。 |
| `Fusion` | 融合 | 把多个算子组合成一个计算内核或外部函数，减少中间存储与调用。 |
| `Pattern` | 模式 | 用来描述需要在 IR 中识别的结构。 |
| `DPL` | 数据流模式语言 | Relax 用于模式识别与改写的 API 集合。 |
| `Composite function` | 组合函数 | 由模式融合得到、带 Composite 属性的 Relax 函数。 |
| `Partitioning` | 图分区 | 把模型图划分为由不同后端处理的子图。 |
| `BYOC` | 自定义代码生成接入机制 | 让外部硬件编译器或库接收部分 Relax 子图的 TVM 机制。 |
| `Codegen` | 代码生成 | 把低层 IR 或外部子图转换成目标可执行形式。 |
| `Target` | 编译目标 | 编译期使用的硬件能力和代码生成配置。 |
| `TargetKind` | 编译目标类别 | 注册目标名称、默认设备类型和合法属性的类别。 |
| `Device` | 设备 | 运行期实际执行计算或保存 Tensor 的硬件实例。 |
| `DeviceAPI` | 设备接口 | TVM 运行时中负责内存、复制、流、同步和设备属性的统一接口。 |
| `Host` | 主机 | 组织编译或执行控制的 CPU 环境；交叉编译时编译主机与执行主机还需区分。 |
| `Runtime` | 运行时 | 加载模块、管理 Tensor 与设备、调用已编译函数的部署组件。 |
| `PackedFunc` | 统一封装函数 | TVM 跨 Python、C++ 和运行时传递参数与调用函数的类型擦除接口。 |
| `Module` | 运行时模块 | 保存可按名称查找函数并可导入其他模块的运行时对象。 |
| `Tensor object` | 张量对象 | 运行时中带形状、数据类型、设备和内存引用的对象。 |
| `DLPack` | DLPack 张量交换协议 | 多个框架交换 Tensor 描述和所有权的开放协议。 |
| `Virtual machine` | 虚拟机 | 执行 Relax 控制流程并调用已编译内核或外部函数的运行时组件。 |
| `Bytecode` | 字节码 | Relax VM 默认模式使用的紧凑控制指令。 |
| `Serialization` | 序列化 | 把运行时模块、命令和元数据保存成可传输形式。 |
| `Deserialization` | 反序列化 | 从保存形式恢复运行时模块与导入关系。 |
| `ABI` | 应用二进制接口 | 模块、函数、驱动和固件之间的二进制调用与数据约定。 |
| `Artifact` | 编译产物 | 代码生成器输出的模块、命令、常量和元数据集合。 |
| `Relocation` | 地址重定位 | 加载或调用时把逻辑缓冲区引用转换为实际设备地址。 |
| `Workspace` | 工作区 | 调用期间供中间值和临时计算使用的内存。 |
| `Scratchpad` | 片上暂存存储器 | 由软件显式管理、靠近计算单元的片上存储。 |
| `Cache` | 缓存 | 由硬件或软件保存近期数据以减少下层访问的存储。 |
| `DMA` | 直接存储器访问 | 不由通用 CPU 逐元素参与的数据搬运引擎。 |
| `Command stream` | 命令流 | 按协议编码并提交给固件或硬件的一组命令。 |
| `Event` | 事件 | 表示异步任务完成、错误或任务间先后关系的对象或编号。 |
| `Stream / queue` | 流 / 队列 | 按顺序接收设备工作并允许多个工作序列并行的运行时对象。 |
| `Synchronization` | 同步 | 建立任务先后关系或等待设备完成。 |
| `Asynchronous execution` | 异步执行 | 提交函数先返回事件，设备工作可在主机继续执行时进行。 |
| `RPC` | 远程过程调用 | TVM 用于上传模块、在远程设备运行函数和测量的机制。 |
| `DLight` | DLight 规则调度 | 使用预定义规则快速生成 TensorIR 调度。 |
| `MetaSchedule` | MetaSchedule 搜索调度 | 生成候选、构建、设备测量并保存调度记录的自动调优框架。 |
| `Schedule` | 调度 | 在不改变计算结果的前提下组织循环、存储和执行方式。 |
| `Schedule trace` | 调度轨迹 | 可重放的一系列调度决定。 |
| `Tensor Intrin` | 张量内建函数 | 用小块计算描述与目标硬件实现配对，供 tensorize 使用。 |
| `Tensorize` | 张量指令替换 | 把匹配的小块 TensorIR 计算替换为张量内建函数实现。 |
| `Cost model` | 代价模型 | 预测候选执行成本或性能的模型。 |
| `Runner` | 运行测量器 | 在本地或远程设备执行候选并返回测量结果。 |
| `Builder` | 候选构建器 | 把调度候选编译成可运行模块。 |
| `Database` | 调优数据库 | 保存工作负载、调度轨迹和测量结果。 |
| `Fallback` | 改由其他后端执行 | 当前后端不接纳某个节点或子图时，保留给主机或另一设备。 |
| `Reference model` | 参考模型 | 用于给出预期数值或位级结果的独立实现。 |
| `Golden data` | 标准参考数据 | 经过确认、供自动测试比较的输入与预期输出。 |
| `Bit-exact` | 位级相同 | 输出每一位都与参考结果一致。 |
| `Tolerance` | 允许误差 | 近似计算结果相对参考值可接受的数值范围。 |
| `Throughput` | 吞吐率 | 单位时间完成的样本、词元、运算或数据量。 |
| `Latency` | 时延 | 从请求开始到指定完成点所需时间。 |
| `Bandwidth` | 带宽 | 单位时间可传输的数据量。 |
| `Utilization` | 利用率 | 执行单元忙碌时间或有效工作量占可用能力的比例。 |
| `Roofline model` | 屋顶线模型 | 用计算强度、峰值算力和内存带宽判断性能限制来源的模型。 |

## 33.4 逐项解释

### 33.4.1 Tensor：张量

具有统一数据类型的多维数据对象；由形状、数据类型、设备和可选步长描述。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“张量（`Tensor`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.2 Scalar：标量

不带张量维度的单个数值。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“标量（`Scalar`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.3 Vector：向量

一维张量；在硬件语境中也可指一次并行处理的多个元素。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“向量（`Vector`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.4 Matrix：矩阵

二维张量；批次矩阵则带有一个或多个前导批次维度。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“矩阵（`Matrix`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.5 Dimension：维度

张量形状中的一个位置，不等同于该位置的大小。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“维度（`Dimension`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.6 Dimension size：维度大小

某一维度包含的元素数量，例如形状 `[2,3]` 的第 1 维大小为 3。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“维度大小（`Dimension size`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.7 Axis parameter：维度编号参数

API 中的 `axis`；正文写成“沿第 k 维”或“归约维度”，保留代码里的参数名。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“维度编号参数（`Axis parameter`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.8 Rank / ndim：秩 / 维数

张量拥有的维度数量；形状 `[2,3,4]` 的秩为 3。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“秩 / 维数（`Rank / ndim`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.9 Shape：形状

按顺序列出的各维度大小，例如 `[batch, sequence, hidden]`。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“形状（`Shape`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.10 Dynamic 形状：动态形状

至少一个维度大小在编译期不是固定常量。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“动态形状（`Dynamic shape`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.11 Static 形状：静态形状

每个维度大小在编译期已经确定。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“静态形状（`Static shape`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.12 Symbolic 形状：符号形状

用符号变量和表达式表示维度大小及其关系。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“符号形状（`Symbolic shape`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.13 Batch：批次

一次共同处理的一组样本或请求。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“批次（`Batch`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.14 Batch size：批次大小

一个批次包含的样本或请求数量。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“批次大小（`Batch size`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.15 Batch dimension：批次维度

形状中代表批次的维度；避免使用缩写式中文。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“批次维度（`Batch dimension`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.16 Batch dimensions：批处理维度数

Gather 等 API 的 `batch_dims`，表示有多少个前导维度按批次独立处理。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“批处理维度数（`Batch dimensions`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.17 Sequence length：序列长度

序列包含的词元、时间步或采样点数量。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“序列长度（`Sequence length`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.18 Channel：通道

特征图或向量的一类特征维度。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“通道（`Channel`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.19 Hidden size：隐藏层维度大小

Transformer 等模型中隐藏表示最后一维的大小。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“隐藏层维度大小（`Hidden size`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.20 Head：注意力头

多头注意力中独立计算的一组 Q、K、V 子空间。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“注意力头（`Head`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.21 Head dimension：单头维度大小

每个注意力头的特征维度大小。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“单头维度大小（`Head dimension`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.22 Data type / 数据类型：数据类型

元素编码与位宽，例如 float32、int8。API 字段保留 `dtype`。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“数据类型（`Data type / dtype`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.23 Layout：数据布局

逻辑维度到物理存储次序、分块和填充的完整组织方式。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“数据布局（`Layout`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.24 Data format：数据格式

常用于 NCHW、NHWC 等公开布局名称，也可包含数据类型约定。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“数据格式（`Data format`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.25 Stride (buffer)：缓冲区步长

逻辑索引在某一维增加 1 时，物理地址前进的元素数或字节数。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“缓冲区步长（`Stride (buffer)`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.26 Stride (convolution)：卷积步幅

卷积窗口每次在空间维度移动的距离。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“卷积步幅（`Stride (convolution)`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.27 Padding：填充

在输入边缘补充元素，或把维度大小补到硬件所需倍数。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“填充（`Padding`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.28 Dilation：膨胀系数

卷积核相邻采样点之间的间隔。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“膨胀系数（`Dilation`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.29 Groups：分组数

卷积输入与输出通道被分成多少组独立计算。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“分组数（`Groups`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.30 Kernel (convolution)：卷积核

卷积权重及其空间窗口。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“卷积核（`Kernel (convolution)`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.31 Kernel (compiled)：计算内核

可被运行时调用的一段已编译设备程序。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“计算内核（`Kernel (compiled)`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.32 Operator：算子

图 IR 中表示一种计算的节点或运算类别。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“算子（`Operator`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.33 Operation：运算

一次具体数学或数据处理动作；与“算子定义”按上下文区分。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“运算（`Operation`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.34 Elementwise：逐元素

对对应元素独立执行同一运算。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“逐元素（`Elementwise`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.35 Broadcasting：广播

按规则扩展大小为 1 或缺省的前导维度，以参与逐元素运算。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“广播（`Broadcasting`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.36 Reduction：归约

沿一个或多个维度把多个元素合成较少元素，例如求和或最大值。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“归约（`Reduction`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.37 Permutation：维度重排

改变张量各维度的次序。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“维度重排（`Permutation`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.38 Transpose：转置

二维矩阵交换行列，或高维张量按给定维度次序重排。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“转置（`Transpose`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.39 Reshape：形状变换

在元素线性次序不变时改变形状；连续 Tensor 可仅修改元数据。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“形状变换（`Reshape`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.40 Slice：切片

从一个或多个维度取得范围子集。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“切片（`Slice`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.41 Gather：索引收集

按照索引从指定维度取得元素或切片。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“索引收集（`Gather`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.42 Scatter：索引写入

按照索引把更新值写入目标 Tensor。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“索引写入（`Scatter`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.43 Concatenate：拼接

沿指定维度连接多个 Tensor。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“拼接（`Concatenate`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.44 Split：拆分

沿指定维度把一个 Tensor 分为多个 Tensor。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“拆分（`Split`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.45 Tiling：分块

把大计算和大 Tensor 划分为适合片上存储与执行单元的小块。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“分块（`Tiling`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.46 Tile：分块

分块处理产生的一个具体小块；代码标识符可保留 `tile`。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“分块（`Tile`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.47 Tail：尾部

维度大小不能整除物理分块或向量宽度时剩余的有效元素。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“尾部（`Tail`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.48 Mask：掩码

标出有效元素、允许位置或需要忽略的位置。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“掩码（`Mask`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.49 Alignment：对齐

地址、步长或大小需要满足的整数倍要求。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“对齐（`Alignment`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.50 Packing：打包

把多个低位宽元素放入一个字节或机器字。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“打包（`Packing`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.51 Unpacking：解包

从打包存储中恢复独立元素。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“解包（`Unpacking`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.52 Zero point：零点

整数存储值中对应实数 0 的值。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“零点（`Zero point`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.53 Scale：缩放系数

整数存储值与实数之间换算所用的正数系数。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“缩放系数（`Scale`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.54 Per-tensor：按张量

整个 Tensor 共用一组参数。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“按张量（`Per-tensor`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.55 Per-channel：按通道

每个通道拥有独立参数。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“按通道（`Per-channel`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.56 Accumulator：累加器

保存乘加或归约中间结果的寄存器或缓冲区。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“累加器（`Accumulator`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.57 Rounding：舍入

把高精度数值转换为较低精度时选择相邻可表示值的规则。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“舍入（`Rounding`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.58 Saturation：饱和

超出数据类型范围时限制到最小值或最大值。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“饱和（`Saturation`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.59 Clipping：裁剪

把数值限制到指定区间。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“裁剪（`Clipping`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.60 Overflow：溢出

结果超过当前表示范围。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“溢出（`Overflow`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.61 Underflow：下溢

数值绝对值太小，无法由当前格式正常表示。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“下溢（`Underflow`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.62 IR：中间表示

编译器内部用于分析和变换程序的数据结构。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“中间表示（`IR`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.63 IRModule：IR 模块

保存 Relax 函数、TensorIR PrimFunc、全局信息与模块属性的容器。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“IR 模块（`IRModule`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.64 Relax：Relax 图级 IR

TVM 中描述模型计算图、控制流、形状和高层调用的 IR。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“Relax 图级 IR（`Relax`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.65 TensorIR：张量程序 IR

TVM 中描述循环、缓冲区、计算块和低层张量程序的 IR。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“张量程序 IR（`TensorIR`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.66 PrimFunc：原始函数

TensorIR 中可独立编译的低层函数。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“原始函数（`PrimFunc`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.67 SBlock：可调度计算块

v0.24.0 TensorIR 中带读写区域和迭代变量信息的计算块。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“可调度计算块（`SBlock`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.68 StructInfo：结构信息

Relax 表达式的形状、数据类型、设备等静态可知信息。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“结构信息（`StructInfo`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.69 GlobalVar：全局变量

IRModule 内引用全局函数的符号对象。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“全局变量（`GlobalVar`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.70 Dataflow block：数据流块

Relax 中主要表达局部无副作用计算依赖的块。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“数据流块（`Dataflow block`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.71 TVMScript：TVM 脚本语言

使用 Python 语法书写和打印 TVM IR 的领域专用语言。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“TVM 脚本语言（`TVMScript`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.72 Pass：编译变换

把一种程序状态转换为另一种程序状态的编译步骤。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“编译变换（`Pass`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.73 PassContext：编译变换上下文

控制变换配置、优化级别、禁用项和插桩的上下文。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“编译变换上下文（`PassContext`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.74 Pipeline：编译流程

按明确顺序组合的一组编译变换。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“编译流程（`Pipeline`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.75 Normalization：规范化

把多种等价写法整理成后续分析更容易处理的形式。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“规范化（`Normalization`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.76 Legalization：算子逐级转换

把高层算子转换成 TensorIR 调用或外部函数调用。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“算子逐级转换（`Legalization`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.77 Lowering：逐级转换

逐步把高层程序转换成更接近目标代码的表示。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“逐级转换（`Lowering`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.78 Fusion：融合

把多个算子组合成一个计算内核或外部函数，减少中间存储与调用。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“融合（`Fusion`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.79 Pattern：模式

用来描述需要在 IR 中识别的结构。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“模式（`Pattern`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.80 DPL：数据流模式语言

Relax 用于模式识别与改写的 API 集合。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“数据流模式语言（`DPL`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.81 Composite function：组合函数

由模式融合得到、带 Composite 属性的 Relax 函数。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“组合函数（`Composite function`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.82 Partitioning：图分区

把模型图划分为由不同后端处理的子图。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“图分区（`Partitioning`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.83 BYOC：自定义代码生成接入机制

让外部硬件编译器或库接收部分 Relax 子图的 TVM 机制。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“自定义代码生成接入机制（`BYOC`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.84 Codegen：代码生成

把低层 IR 或外部子图转换成目标可执行形式。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“代码生成（`Codegen`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.85 Target：编译目标

编译期使用的硬件能力和代码生成配置。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“编译目标（`Target`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.86 TargetKind：编译目标类别

注册目标名称、默认设备类型和合法属性的类别。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“编译目标类别（`TargetKind`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.87 Device：设备

运行期实际执行计算或保存 Tensor 的硬件实例。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“设备（`Device`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.88 DeviceAPI：设备接口

TVM 运行时中负责内存、复制、流、同步和设备属性的统一接口。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“设备接口（`DeviceAPI`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.89 Host：主机

组织编译或执行控制的 CPU 环境；交叉编译时编译主机与执行主机还需区分。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“主机（`Host`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.90 运行时：运行时

加载模块、管理 Tensor 与设备、调用已编译函数的部署组件。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“运行时（`Runtime`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.91 PackedFunc：统一封装函数

TVM 跨 Python、C++ 和运行时传递参数与调用函数的类型擦除接口。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“统一封装函数（`PackedFunc`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.92 Module：运行时模块

保存可按名称查找函数并可导入其他模块的运行时对象。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“运行时模块（`Module`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.93 Tensor object：张量对象

运行时中带形状、数据类型、设备和内存引用的对象。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“张量对象（`Tensor object`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.94 DLPack：DLPack 张量交换协议

多个框架交换 Tensor 描述和所有权的开放协议。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“DLPack 张量交换协议（`DLPack`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.95 Virtual machine：虚拟机

执行 Relax 控制流程并调用已编译内核或外部函数的运行时组件。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“虚拟机（`Virtual machine`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.96 Bytecode：字节码

Relax VM 默认模式使用的紧凑控制指令。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“字节码（`Bytecode`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.97 Serialization：序列化

把运行时模块、命令和元数据保存成可传输形式。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“序列化（`Serialization`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.98 Deserialization：反序列化

从保存形式恢复运行时模块与导入关系。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“反序列化（`Deserialization`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.99 ABI：应用二进制接口

模块、函数、驱动和固件之间的二进制调用与数据约定。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“应用二进制接口（`ABI`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.100 Artifact：编译产物

代码生成器输出的模块、命令、常量和元数据集合。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“编译产物（`Artifact`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.101 Relocation：地址重定位

加载或调用时把逻辑缓冲区引用转换为实际设备地址。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“地址重定位（`Relocation`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.102 Workspace：工作区

调用期间供中间值和临时计算使用的内存。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“工作区（`Workspace`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.103 Scratchpad：片上暂存存储器

由软件显式管理、靠近计算单元的片上存储。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“片上暂存存储器（`Scratchpad`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.104 Cache：缓存

由硬件或软件保存近期数据以减少下层访问的存储。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“缓存（`Cache`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.105 DMA：直接存储器访问

不由通用 CPU 逐元素参与的数据搬运引擎。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“直接存储器访问（`DMA`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.106 Command stream：命令流

按协议编码并提交给固件或硬件的一组命令。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“命令流（`Command stream`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.107 Event：事件

表示异步任务完成、错误或任务间先后关系的对象或编号。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“事件（`Event`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.108 Stream / queue：流 / 队列

按顺序接收设备工作并允许多个工作序列并行的运行时对象。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“流 / 队列（`Stream / queue`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.109 Synchronization：同步

建立任务先后关系或等待设备完成。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“同步（`Synchronization`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.110 Asynchronous execution：异步执行

提交函数先返回事件，设备工作可在主机继续执行时进行。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“异步执行（`Asynchronous execution`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.111 RPC：远程过程调用

TVM 用于上传模块、在远程设备运行函数和测量的机制。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“远程过程调用（`RPC`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.112 DLight：DLight 规则调度

使用预定义规则快速生成 TensorIR 调度。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“DLight 规则调度（`DLight`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.113 MetaSchedule：MetaSchedule 搜索调度

生成候选、构建、设备测量并保存调度记录的自动调优框架。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“MetaSchedule 搜索调度（`MetaSchedule`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.114 Schedule：调度

在不改变计算结果的前提下组织循环、存储和执行方式。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“调度（`Schedule`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.115 Schedule trace：调度轨迹

可重放的一系列调度决定。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“调度轨迹（`Schedule trace`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.116 Tensor Intrin：张量内建函数

用小块计算描述与目标硬件实现配对，供 tensorize 使用。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“张量内建函数（`Tensor Intrin`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.117 Tensorize：张量指令替换

把匹配的小块 TensorIR 计算替换为张量内建函数实现。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“张量指令替换（`Tensorize`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.118 Cost model：代价模型

预测候选执行成本或性能的模型。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“代价模型（`Cost model`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.119 Runner：运行测量器

在本地或远程设备执行候选并返回测量结果。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“运行测量器（`Runner`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.120 Builder：候选构建器

把调度候选编译成可运行模块。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“候选构建器（`Builder`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.121 Database：调优数据库

保存工作负载、调度轨迹和测量结果。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“调优数据库（`Database`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.122 Fallback：改由其他后端执行

当前后端不接纳某个节点或子图时，保留给主机或另一设备。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“改由其他后端执行（`Fallback`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.123 Reference model：参考模型

用于给出预期数值或位级结果的独立实现。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“参考模型（`Reference model`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.124 Golden data：标准参考数据

经过确认、供自动测试比较的输入与预期输出。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“标准参考数据（`Golden data`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.125 Bit-exact：位级相同

输出每一位都与参考结果一致。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“位级相同（`Bit-exact`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.126 Tolerance：允许误差

近似计算结果相对参考值可接受的数值范围。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“允许误差（`Tolerance`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.127 Throughput：吞吐率

单位时间完成的样本、词元、运算或数据量。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“吞吐率（`Throughput`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.128 Latency：时延

从请求开始到指定完成点所需时间。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“时延（`Latency`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.129 Bandwidth：带宽

单位时间可传输的数据量。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“带宽（`Bandwidth`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.130 Utilization：利用率

执行单元忙碌时间或有效工作量占可用能力的比例。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“利用率（`Utilization`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

### 33.4.131 Roofline model：屋顶线模型

用计算强度、峰值算力和内存带宽判断性能限制来源的模型。

在 TVM 与自研 NPU 接入文档中，首次出现建议写作“屋顶线模型（`Roofline model`）”，后续使用中文；源码标识符、API 参数、属性名和命令字段保持原文。这样既能与代码检索一致，也能避免把概念名称、参数名称和张量中的具体维度混在一起。

## 33.5 API 名称保留规则

下列内容保持源码拼写：

- `axis`、`axes`、`batch_dims`、`shape`、`dtype`、`layout`；
- `IRModule`、`Relax`、`TensorIR`、`PrimFunc`、`StructInfo`；
- `FuseOpsByPattern`、`MergeCompositeFunctions`、`RunCodegen`；
- `Target`、`TargetKind`、`DeviceAPI`；
- `PackedFunc`、`runtime.Module`、`VirtualMachine`；
- `relax.quantize`、`relax.dequantize` 等实际算子注册名。

正文解释这些 API 时使用上表中文，避免把代码标识符强行翻成无法检索的名称。

## 实现说明与验证步骤

> [!note] 依据与适用范围
> 本节依据所列 Apache TVM 官方资料和 v0.24.0 源码整理，给出输入条件、操作步骤、预期结果、异常处理和自研 NPU 适配要求。接口细节以固定版本源码与测试为准，在线页面用于补充说明。

### 33.A 目标与适用范围

查找一个术语时，同时记录中文解释、源码拼写、对象类型、所属阶段和最小使用例，避免只保存孤立翻译。

参考样本保持输入规模较小，以便直接检查对象、字段和输出。首次执行时不要同时加入图改写、外部代码生成和真实设备执行；应先确认当前阶段的输入与输出，再增加后续处理。建议为该项验证建立独立目录，保存命令、标准输出、IR、编译产物摘要和环境信息。

### 33.B 参考实现

**官方依据：** [Python API](https://tvm.apache.org/docs/reference/api/python/index.html) 与 [官方文档索引](https://tvm.apache.org/docs/genindex.html)。

**v0.24.0 源码位置：** `docs/reference/api/python/`、`python/tvm/`、`include/tvm/`。

```yaml
term: GlobalVar
chinese: 全局变量
object: tvm.ir.GlobalVar
stage: IRModule function reference
created_by:
  - TVMScript parser
  - IRModule.add
consumed_by:
  - Call
  - module lookup
  - transformation passes
inspect:
  - name_hint
  - checked_type
```

按以下顺序执行：

1. 在新进程中确认 `tvm.__file__`、动态库路径和 TVM 提交，避免受到旧进程注册状态影响；
2. 将参考实现保存到单独文件，只补充本节明确要求的输入对象，不先加入额外优化；
3. 执行后保存完整输出；若生成 IR，同时保存 `script(show_meta=True)` 的文本；
4. 把输出与下面的预期现象逐项比较，不以“没有抛出异常”代替功能检查；
5. 重复执行一次并比较主要产物，确认结果没有依赖临时全局状态或未固定的遍历顺序。

> [!success] 预期现象
> 同一术语在图 IR、张量程序和运行时可能属于不同对象。速查项给出类型和阶段后，读者可以定位到正确 API。

> [!tip] 输出检查顺序
> 先看对象类型和函数数量，再看属性、调用形式、形状与数据类型，最后看运行结果或编译产物。若前一项不符合预期，先停止后续步骤。这样能把问题限定在最早出现差异的阶段。

### 33.C 单项条件验证

只搜索中文名称，再尝试定位 C++ 实现。若难以找到，应从 Python 类的 `_ffi_api` 调用或注册字符串继续追踪。

执行条件变化样本时，复制参考实现的输入与环境，只修改上文指出的一项。对两个输出进行结构比较，并记录以下四项：

1. 第一次出现差异的是哪一个阶段？
2. 差异表现为函数、属性、形状、数据类型、编译产物还是运行结果？
3. 当前行为是明确拒绝、交给其他后端执行，还是编译错误？
4. 日志是否足以让另一位开发者不查看本次进程状态也能复现？

如果同时观察到多个变化，应继续缩小条件变化样本。参考实现用于确认正常处理，单项条件验证用于确认系统为何接受、拒绝或转交当前输入。

### 33.D 自研 NPU 适配要求

为后端专有对象沿用 TVM 命名风格，缩写首次出现时解释。固件字段与 TVM 属性若名称不同，在表中明确对应关系和转换位置。

改造时建议保留三份相互独立的输入：最小 Relax 或 TensorIR、设备编译器输入、运行时调用输入。三份输入使用同一个样本编号，并记录转换前后的关键字段。这样可以分别测试 TVM 变换、NPU 编译器和驱动，不需要每次都运行完整模型。

至少补充以下样本：

- 一个完全满足能力要求的正向样本；
- 一个只改变数据类型的反向样本；
- 一个只改变形状或属性的反向样本；
- 一个包含主机与 NPU 混合执行的样本；
- 一个导出后在新进程加载的样本；
- 若支持动态尺寸，再增加最小值、常用值和最大值附近的样本。

### 33.E 源码定位

从上文列出的源码位置选择一个公开 API，依次找到 Python 调用、FFI 注册、C++ 实现和测试。记录函数签名、主要输入、主要输出、会写入的模块属性以及失败方式。若名称只在文档出现而源码中找不到，继续搜索注册字符串、属性键或错误文本。

> [!important] 验证项目
> 1. 执行前记录参考实现的对象关系；2. 执行后用实际输出修正记录；3. 增加一个会被明确拒绝的输入；4. 标明自研 NPU 接入需要修改的层次与保持不变的层次；5. 把结果整理成可由自动测试检查的断言。

### 33.F 检查清单

- [ ] 示例可在固定 v0.24.0 环境重复执行，或已明确列出所需可选依赖；
- [ ] 预期现象包含可检查的结构、字段或数值，不只是“运行成功”；
- [ ] 单条件变化能触发预期的拒绝、转交或结构差异；
- [ ] 能从官方页面定位到固定版本源码和测试；
- [ ] NPU 改造说明包含编译阶段、运行阶段与部署阶段；
- [ ] 失败时保留最小输入、环境、阶段输出和第一个错误。

## 详细说明所在章节

| 对象类别 | 本手册位置 |
| --- | --- |
| 节点、表达式、变量、函数、IRModule、结构比较 | [[03-IRModule 与统一对象系统]] |
| Relax 表达式、绑定、数据流块、函数、结构信息 | [[04-Relax 图级 IR]] |
| TensorIR 基础表达式、语句、Buffer、SBlock、PrimFunc | [[05-TensorIR 程序表示]] |
| PackedFunc、Device、运行时 Tensor、运行时 Module | [[09-PackedFunc Module 与运行时]] |
| Executable、VirtualMachine、闭包、导出与加载 | [[10-Relax VM 导出与加载]] |
