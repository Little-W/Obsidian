---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 24. 调试 可观测性 与故障定位

> [!abstract] 本章内容
> 给出从导入、分区、代码生成、打包、加载、提交到设备完成的逐层定位方法。

## 一句话理解

先确定错误第一次出现在哪一层，再检查该层输入与输出；不要从最终错误码同时猜测所有阶段。

## 核心要点

1. 编译日志应列出候选子图、接纳结果和拒绝原因。
2. 代码生成日志应包含外部函数名、输入输出、数据布局、工作区和命令摘要。
3. 运行时日志应含模块版本、设备、提交序号、事件和错误寄存器。
4. IR 快照、命令反汇编与参考输出是三类互补证据。
5. 最小失败模型可显著缩短定位时间。

```mermaid
flowchart LR
    A["输入程序状态"] --> B["分析信息"]
    B --> C["本章所述处理"]
    C --> D["输出程序状态"]
    D --> E["日志与可复现记录"]
```

## 逐项解析

### 1. 编译日志应列出候选子图、接纳结果和拒绝原因。

编译日志应列出候选子图、接纳结果和拒绝原因。

阅读时先定位产生该信息的函数，再查找消费它的下一阶段。把两处代码和中间 IR 放在一起观察，比单独记忆类名更容易理解。

**实现建议：** 为每个编译阶段保存带序号的 IR 文件。

> [!warning] 本小节常见问题
> 日志只写成功或失败，无法解释图为何未进入 NPU。

### 2. 代码生成日志应包含外部函数名、输入输出、数据布局、工作区和命令摘要。

代码生成日志应包含外部函数名、输入输出、数据布局、工作区和命令摘要。

把变换前后的 IR 并排比较，重点查看函数参数、调用、属性、StructInfo 和返回值。只记录最终 IR 会丢失变化发生的位置。

**实现建议：** 给每个外部函数生成稳定散列值并贯穿日志。

> [!warning] 本小节常见问题
> 多个异步任务复用同一临时缓冲区会产生偶发错误。

### 3. 运行时日志应含模块版本、设备、提交序号、事件和错误寄存器。

运行时日志应含模块版本、设备、提交序号、事件和错误寄存器。

实现时将硬件固定限制放入 Target 或能力文件，把用户可选策略放入编译配置；二者发生冲突时应报告具体字段。

**实现建议：** 实现命令流离线解析器，禁止只以十六进制转储作为诊断。

> [!warning] 本小节常见问题
> 错误后继续读取已失效输出，会制造二次症状。

### 4. IR 快照、命令反汇编与参考输出是三类互补证据。

IR 快照、命令反汇编与参考输出是三类互补证据。

测试至少包含一个正常样本和一个只改变单一条件的反向样本。这样，结构或结果变化时能直接找到对应规则。

**实现建议：** 错误发生后保存第一个失败分块的输入、预期输出和实际输出。

> [!warning] 本小节常见问题
> 日志只写成功或失败，无法解释图为何未进入 NPU。

### 5. 最小失败模型可显著缩短定位时间。

最小失败模型可显著缩短定位时间。

保存可由工具读取的阶段报告，其中包含输入摘要、输出摘要、Pass 配置、Target、耗时和错误；文本日志用于辅助阅读。

**实现建议：** 为超时、设备复位和驱动返回码建立统一错误分类。

> [!warning] 本小节常见问题
> 多个异步任务复用同一临时缓冲区会产生偶发错误。

## 实施清单

- [ ] 为每个编译阶段保存带序号的 IR 文件。
- [ ] 给每个外部函数生成稳定散列值并贯穿日志。
- [ ] 实现命令流离线解析器，禁止只以十六进制转储作为诊断。
- [ ] 错误发生后保存第一个失败分块的输入、预期输出和实际输出。
- [ ] 为超时、设备复位和驱动返回码建立统一错误分类。

## 设计评审问题

1. 本阶段接收哪一种 IR，要求哪些属性已经存在？
2. 本阶段产生哪些新函数、属性、常量或模块？
3. 遇到不被支持的输入时，是保留给其他后端、报告编译错误，还是插入转换？
4. 哪些配置属于硬件固定限制，哪些配置允许自动搜索？
5. 如何证明重复执行本阶段不会产生额外变化？
6. 如何在日志中解释每个重要决定？

## 自测

> [!question] 请先独立回答
> 把一个两层 MLP 的 IR 在本章处理前后分别打印出来，指出函数数量、调用形式、张量类型和模块属性发生了什么变化。如果无法运行代码，可先画出预期结构，再与实际输出比较。

## 参考资料

| 资料 | 类型 |
| --- | --- |
| [TVM 错误指南](https://tvm.apache.org/docs/errors.html) | 官方资料 |
| [Pass 基础设施](https://tvm.apache.org/docs/arch/pass_infra.html) | 官方资料 |
| [运行时系统](https://tvm.apache.org/docs/arch/runtime.html) | 官方资料 |

## 章末小结

本章的重点不是记住所有类名，而是明确输入状态、处理动作、输出状态和失败处理。接入自研 NPU 时，每一层都应保留可检查的中间结果，使图分区、代码生成、设备提交和数值对照可以分别验证。
