---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 32. 故障速查表

> [!abstract] 使用顺序
> 先按现象找到条目，再收集该条目要求的证据。定位固定遵循模型前端、Relax、分区、代码生成、模块、运行时、驱动、固件、硬件和输出的次序。

## 快速目录

- [[#32.1 Python 导入到旧 TVM|32.1 Python 导入到旧 TVM]]
- [[#32.2 构建找不到 LLVM|32.2 构建找不到 LLVM]]
- [[#32.3 后端模式为空|32.3 后端模式为空]]
- [[#32.4 候选结构不匹配|32.4 候选结构不匹配]]
- [[#32.5 检查函数拒绝全部候选|32.5 检查函数拒绝全部候选]]
- [[#32.6 具体组合被普通模式抢先|32.6 具体组合被普通模式抢先]]
- [[#32.7 RunCodegen 找不到后端|32.7 RunCodegen 找不到后端]]
- [[#32.8 Runtime 创建函数缺失|32.8 Runtime 创建函数缺失]]
- [[#32.9 分区后剩余运算无法编译|32.9 分区后剩余运算无法编译]]
- [[#32.10 外部符号重复|32.10 外部符号重复]]
- [[#32.11 内存内执行成功但导出失败|32.11 内存内执行成功但导出失败]]
- [[#32.12 加载后找不到 NPU 函数|32.12 加载后找不到 NPU 函数]]
- [[#32.13 常量数量不一致|32.13 常量数量不一致]]
- [[#32.14 输入形状正确但设备地址错误|32.14 输入形状正确但设备地址错误]]
- [[#32.15 只在非整分块尺寸错误|32.15 只在非整分块尺寸错误]]
- [[#32.16 K 尾部结果偏大|32.16 K 尾部结果偏大]]
- [[#32.17 负数结果差一|32.17 负数结果差一]]
- [[#32.18 极值出现翻转|32.18 极值出现翻转]]
- [[#32.19 多次运行第二次错误|32.19 多次运行第二次错误]]
- [[#32.20 并发运行偶发错误|32.20 并发运行偶发错误]]
- [[#32.21 设备超时后一直失败|32.21 设备超时后一直失败]]
- [[#32.22 小算子进入 NPU 反而更慢|32.22 小算子进入 NPU 反而更慢]]
- [[#32.23 调优记录变慢|32.23 调优记录变慢]]
- [[#32.24 RPC 测量异常大|32.24 RPC 测量异常大]]
- [[#32.25 动态形状选择错误版本|32.25 动态形状选择错误版本]]
- [[#32.26 模型精度下降但单算子通过|32.26 模型精度下降但单算子通过]]
- [[#32.27 权重缓存错误复用|32.27 权重缓存错误复用]]
- [[#32.28 部署库体积异常大|32.28 部署库体积异常大]]
- [[#32.29 设备计数器与命令不一致|32.29 设备计数器与命令不一致]]
- [[#32.30 版本升级结构测试失败|32.30 版本升级结构测试失败]]

## 最小证据包

```text
environment.txt
input_relax.py
partitioned_relax.py
target.json
backend_options.json
artifact_meta.json
commands.disasm
runtime.log
driver.log
reference_output.bin
actual_output.bin
```

## 32.1 Python 导入到旧 TVM

> [!warning] 现象
> 提交号正确但 API 缺失或行为像旧版

### 先收集

打印 `tvm.__file__`、已加载动态库路径和 `libinfo`。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

环境中残留旧 wheel，或 PYTHONPATH 顺序错误。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

先确认 Python 包，再确认动态库；两者都必须指向当前构建。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

隔离虚拟环境并在 CI 保存加载路径。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.2 构建找不到 LLVM

> [!warning] 现象
> CMake 报 LLVM 配置缺失

### 先收集

`llvm-config --version` 与路径。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

llvm-config 不在 PATH，或版本不被当前构建接受。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

在 config.cmake 写完整 llvm-config 命令并重新配置。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

构建镜像固定 LLVM 版本。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.3 后端模式为空

> [!warning] 现象
> `get_patterns_with_prefix` 返回空列表

### 先收集

检查 Python 后端模块是否被导入。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

`__init__.py` 未导入 patterns，或前缀拼写不同。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

显式导入后端包并列出注册模式。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

增加注册存在性单测。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.4 候选结构不匹配

> [!warning] 现象
> 模型含 MatMul 和 ReLU，但未形成组合

### 先收集

打印导入后的 Relax 绑定。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

中间存在 cast、reshape、bias 或运算名不同。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

根据真实结构扩展模式，先保持模式精确。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

为每种前端保存结构样本。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.5 检查函数拒绝全部候选

> [!warning] 现象
> 模式能找到，但外部函数数量为 0

### 先收集

输出每个检查字段与原因码。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

StructInfo 缺失、数据类型字符串或动态形状处理错误。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

把检查拆为纯函数并逐项单测。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

模型级统计拒绝原因。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.6 具体组合被普通模式抢先

> [!warning] 现象
> 只形成 MatMul，ReLU 留在外部

### 先收集

列出模式顺序与 Composite 名称。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

优先级顺序错误。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

把更具体组合放到更高优先级并写结构测试。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

每次新增模式运行优先级回归。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.7 RunCodegen 找不到后端

> [!warning] 现象
> 报缺少 `relax.ext.acme_npu`

### 先收集

`get_global_func`、CMake `libinfo`。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

代码生成源文件未进入编译器库，或注册名不一致。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

核对 Codegen 属性、注册字符串和构建开关。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

构建后自动检查全局函数。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.8 运行时创建函数缺失

> [!warning] 现象
> 代码生成器查找运行时 Create 失败

### 先收集

`get_global_func('runtime.AcmeNPURuntimeCreate', True)`。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

运行时源文件未编译，或仅打开代码生成开关。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

启用 runtime 开关并确认 CMake 模块。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

compiler 与 runtime-only 两套构建测试。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.9 分区后剩余运算无法编译

> [!warning] 现象
> NPU 子图成功，但主机构建失败

### 先收集

查看 RunCodegen 后 IR 与主机 Target。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

剩余运算未 Legalize，或主机缺少实现。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

给剩余节点配置完整主机编译流程。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

混合图必须进入提交级 CI。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.10 外部符号重复

> [!warning] 现象
> 链接或加载报告同名函数

### 先收集

列出所有 global_symbol。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

名称生成只使用算子名，未加入唯一编号。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

稳定散列加可读前缀生成符号。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

模块级唯一性断言。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.11 内存内执行成功但导出失败

> [!warning] 现象
> `export_library` 报 Module 不可保存

### 先收集

检查 type key、SaveToBinary 与加载注册。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

自定义 Module 只实现 GetFunction。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

实现保存和加载，并在新进程测试。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

所有外部模块都做导出回归。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.12 加载后找不到 NPU 函数

> [!warning] 现象
> 共享库可加载但外部调用缺失

### 先收集

查看被导入模块与函数目录。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

运行时模块未被打包或 type key 不一致。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

核对 Module 导入关系与加载函数。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

独立部署镜像测试。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.13 常量数量不一致

> [!warning] 现象
> 运行时 Init 报常量个数错误

### 先收集

打印 constant_names 与实际常量。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

代码生成阶段绑定常量策略与运行时不同。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

统一常量目录和顺序，使用名称而非遍历偶然顺序。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

常量清单散列进入模块。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.14 输入形状正确但设备地址错误

> [!warning] 现象
> 小输入崩溃或 DMA fault

### 先收集

打印逻辑缓冲区、重定位后地址和长度。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

把主机虚拟地址直接写入命令，或 byte_offset 未处理。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

所有命令只引用逻辑缓冲区，运行时重定位并检查。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

离线地址静态检查。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.15 只在非整分块尺寸错误

> [!warning] 现象
> 整块正确，block±1 错误

### 先收集

保存最后一块输入、命令与输出。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

掩码、填充、真实长度或输出写保护错误。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

逐维单独激活尾部，检查第一个失败维度。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

每个物理块维度固定三点测试。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.16 K 尾部结果偏大

> [!warning] 现象
> MatMul 只在 K 不整除时有额外贡献

### 先收集

检查填充值、zero point 与有效 K。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

填充值不是实数零对应的整数值。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

按输入 zero point 填充或使用硬件掩码。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

非零 zero point 尾部测试。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.17 负数结果差一

> [!warning] 现象
> 正数一致，负数在右移后差 1

### 先收集

记录乘数、移位前值、舍入偏置。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

负数舍入或算术右移规则不同。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

实现位准确 RoundShift 并与 RTL 共同测试。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

中点两侧固定向量。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.18 极值出现翻转

> [!warning] 现象
> 大正数变负数或相反

### 先收集

观察每个中间位宽。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

乘积或累加在饱和前已溢出。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

使用足够临时宽度并明确饱和位置。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

上界分析与极值测试。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.19 多次运行第二次错误

> [!warning] 现象
> 首次正确，后续结果受前次影响

### 先收集

清空输出、改变输入、检查工作区。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

部分和未清零、事件未等待或工作区过早复用。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

明确初始化命令与生命周期。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

交替输入长时间测试。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.20 并发运行偶发错误

> [!warning] 现象
> 单线程稳定，多线程失败

### 先收集

记录队列、提交序号、缓冲区和线程。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

共享可变工作区、常量加载竞态或事件复用。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

调用状态独立并使用线程安全缓存。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

并发压力与线程消毒工具。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.21 设备超时后一直失败

> [!warning] 现象
> 一次超时后所有任务失败

### 先收集

读取驱动和固件状态、事件表。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

失效队列、旧地址或未清事件继续使用。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

停止提交，收集状态，完整复位并重新加载。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

异常恢复测试。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.22 小算子进入 NPU 反而更慢

> [!warning] 现象
> 设备计算快但端到端慢

### 先收集

分解复制、提交、执行、等待。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

固定开销超过计算收益。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

在接纳检查加入成本阈值，或与相邻运算组合。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

按形状保存收益模型。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.23 调优记录变慢

> [!warning] 现象
> 应用数据库后性能下降

### 先收集

核对硬件、固件、编译器与 Target。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

旧记录被错误复用，或测量噪声选中异常值。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

版本化数据库并重新测量热点候选。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

记录完整环境与时间分布。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.24 RPC 测量异常大

> [!warning] 现象
> 远程结果包含网络时延

### 先收集

比较主机总时间与设备 time_evaluator。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

使用错误计时位置。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

用设备侧计时并把上传与初始化分开。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

性能脚本固定计时 API。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.25 动态形状选择错误版本

> [!warning] 现象
> 某些 sequence 运行崩溃或工作区不足

### 先收集

打印实际形状、版本范围与工作区公式。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

范围重叠、上限开闭错误或乘法溢出。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

排序版本并使用显式包含关系。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

范围端点测试。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.26 模型精度下降但单算子通过

> [!warning] 现象
> 每个基础运算正确，模型指标变化

### 先收集

保存组合运算中间输出与转换节点。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

组合后的舍入次数、公式版本或参数维度不同。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

逐层二分定位首次差异，比较拆分与组合。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

组合运算独立数值合同。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.27 权重缓存错误复用

> [!warning] 现象
> 换模型后结果像旧权重

### 先收集

核对源常量散列与缓存键。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

缓存只按形状或文件名。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

键包含内容散列、布局、数据类型、目标与编译器版本。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

交替加载同形不同值权重。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.28 部署库体积异常大

> [!warning] 现象
> runtime-only 仍包含编译器

### 先收集

检查链接依赖与 CMake 源文件列表。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

运行时引用编译器对象或开关组织错误。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

隔离公共格式代码，避免 runtime 依赖 IR。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

runtime-only 大小门限。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.29 设备计数器与命令不一致

> [!warning] 现象
> 报告的读写字节明显异常

### 先收集

比较命令静态估算与硬件计数。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

计数器范围、复位时机或多任务干扰。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

独占设备并在提交前后读取。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

计数器自测与空任务基线。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。

## 32.30 版本升级结构测试失败

> [!warning] 现象
> 新 TVM 生成不同 IR

### 先收集

比较首次差异 Pass 与 API 变更。同时保存 TVM 提交、动态库路径、Target、后端配置、模型或 IR 散列值、固件和设备版本。没有这些信息时，不应直接修改编译器或驱动。

### 常见原因

默认编译流程、打印形式或内部节点变化。该原因属于当前阶段的高概率项，但不是唯一可能。应通过中间 IR、产物、命令或设备状态确认，不能仅根据最终错误文本猜测。

### 定位与处理

先确认功能变化，再更新期望结构。处理后重跑最小样本，再运行相邻层测试与完整回归。若修复会改变外部函数 ABI、模块格式或数值规则，需要提升版本并拒绝旧产物。

### 防止再次出现

升级分支完整回归，不直接改发布分支。把本次最小样本、失败输入、期望结果和环境写入测试资产。

> [!tip] 停止条件
> 当已经找到第一次出现偏离的位置，并且有直接证据指向一个字段、一个 Pass、一个命令或一个运行时状态时，再实施修复；不要同时修改多个层次。
