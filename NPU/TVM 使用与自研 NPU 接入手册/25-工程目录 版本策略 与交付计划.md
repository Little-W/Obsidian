---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 25. 工程目录 版本策略 与交付计划

> [!abstract] 本章内容
> 给出后端源码布局、组件责任、ABI 版本、测试资产和分阶段交付建议。

## 一句话理解

可维护后端应把图规则、编译器、运行时、驱动协议和测试分开，同时使用一个明确版本集合确认相互兼容。

## 核心要点

1. Python 目录保存 Relax 组合运算与流水入口，C++ 编译器目录保存外部代码生成。
2. 运行时目录只依赖部署所需组件，不应带入模型前端与完整编译器。
3. 命令格式、模块二进制、驱动 ioctl 和固件能力都需要版本字段。
4. 测试资产应按单运算、组合运算、子图、模型和系统压力分层。
5. 每个阶段都应有可运行产物，不等待全部算子完成后才开始集成。

```mermaid
flowchart LR
    A["输入程序状态"] --> B["分析信息"]
    B --> C["本章所述处理"]
    C --> D["输出程序状态"]
    D --> E["日志与可复现记录"]
```

## 逐项解析

### 1. Python 目录保存 Relax 组合运算与流水入口

Python 目录保存 Relax 组合运算与流水入口，C++ 编译器目录保存外部代码生成。

阅读时先定位产生该信息的函数，再查找消费它的下一阶段。把两处代码和中间 IR 放在一起观察，比单独记忆类名更容易理解。

**实现建议：** 以 Example NPU 目录为起点复制结构，但重新实现空壳逻辑。

> [!warning] 本小节常见问题
> 编译器与固件各自升级但没有兼容表，会出现难以解释的设备拒绝。

### 2. 运行时目录只依赖部署所需组件

运行时目录只依赖部署所需组件，不应带入模型前端与完整编译器。

把变换前后的 IR 并排比较，重点查看函数参数、调用、属性、StructInfo 和返回值。只记录最终 IR 会丢失变化发生的位置。

**实现建议：** 为各组件指定维护人和公开输入输出协议。

> [!warning] 本小节常见问题
> 仅运行时构建意外链接完整编译器，会增大部署体积。

### 3. 命令格式、模块二进制、驱动 ioctl 和固件能力都需要版本字段。

命令格式、模块二进制、驱动 ioctl 和固件能力都需要版本字段。

实现时将硬件固定限制放入 Target 或能力文件，把用户可选策略放入编译配置；二者发生冲突时应报告具体字段。

**实现建议：** 发布产物附带源码提交、工具版本、能力表和已知限制。

> [!warning] 本小节常见问题
> 项目只有端到端模型测试时，任一失败都难以定位。

### 4. 测试资产应按单运算、组合运算、子图、模型和系统压力分层。

测试资产应按单运算、组合运算、子图、模型和系统压力分层。

测试至少包含一个正常样本和一个只改变单一条件的反向样本。这样，结构或结果变化时能直接找到对应规则。

**实现建议：** 第一版选择少量高收益组合运算并提供主机处理。

> [!warning] 本小节常见问题
> 编译器与固件各自升级但没有兼容表，会出现难以解释的设备拒绝。

### 5. 每个阶段都应有可运行产物

每个阶段都应有可运行产物，不等待全部算子完成后才开始集成。

保存可由工具读取的阶段报告，其中包含输入摘要、输出摘要、Pass 配置、Target、耗时和错误；文本日志用于辅助阅读。

**实现建议：** 在 CI 中同时构建编译器版与仅运行时版。

> [!warning] 本小节常见问题
> 仅运行时构建意外链接完整编译器，会增大部署体积。

## 实施清单

- [ ] 以 Example NPU 目录为起点复制结构，但重新实现空壳逻辑。
- [ ] 为各组件指定维护人和公开输入输出协议。
- [ ] 发布产物附带源码提交、工具版本、能力表和已知限制。
- [ ] 第一版选择少量高收益组合运算并提供主机处理。
- [ ] 在 CI 中同时构建编译器版与仅运行时版。

## 设计评审问题

1. 本阶段接收哪一种 IR，要求哪些属性已经存在？
2. 本阶段产生哪些新函数、属性、常量或模块？
3. 遇到不被支持的输入时，是保留给其他后端、报告编译错误，还是插入转换？
4. 哪些配置属于硬件固定限制，哪些配置允许自动搜索？
5. 如何证明重复执行本阶段不会产生额外变化？
6. 如何在日志中解释每个重要决定？

## 自测

> [!question] 请先独立回答
> 把一个两层 MLP 的 IR 在本章处理前后分别打印出来，指出函数数量、调用形式、张量类型和模块属性发生了什么变化。如果无法运行代码，可先画出预期结构，再与实际输出比较。

## 参考资料

| 资料 | 类型 |
| --- | --- |
| [Example NPU Python 目录](https://github.com/apache/tvm/tree/v0.24.0/python/tvm/relax/backend/contrib/example_npu) | 官方资料 |
| [Example NPU 编译器](https://github.com/apache/tvm/tree/v0.24.0/src/relax/backend/contrib/example_npu) | 官方资料 |
| [Example NPU 运行时](https://github.com/apache/tvm/tree/v0.24.0/src/runtime/contrib/example_npu) | 官方资料 |

## 章末小结

本章的重点不是记住所有类名，而是明确输入状态、处理动作、输出状态和失败处理。接入自研 NPU 时，每一层都应保留可检查的中间结果，使图分区、代码生成、设备提交和数值对照可以分别验证。
