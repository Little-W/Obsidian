---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 42. MergeCompositeFunctions 与外部函数分组

> [!abstract] 本章内容
> 解释组合函数的依赖分组、避免循环、后端一致性、分叉汇合、多入口、分组大小控制和设备驻留收益。

> [!note] 官方依据
> `src/relax/transform/merge_composite_functions.cc`、对应 Relax 变换测试与 External Library Dispatch 文档。

## 本章使用说明

先执行或检查本章给出的最小示例，再按快速目录逐项核对。每一节都使用相同的七个观察角度：功能与适用范围、官方实现、自研方法、必查信息、拒绝与异常、测试、应保留证据。这种组织便于快速定位实现位置，也便于后端开发者把内容直接转换为开发任务与测试。

遇到 API 名称时保留源码拼写，例如 `FusionPattern`、`FuseOpsByPattern`、`RunCodegen` 和 `runtime.Module`；解释文字使用 [[33-术语与 API 速查]] 中的标准中文。若某个接口与本地构建不同，先确认 TVM 提交与 Python 加载的动态库，再查固定 tag 的源码和测试。

## 快速目录

- [[#42.1 父组与当前组|42.1 父组与当前组]]
- [[#42.2 直接与间接依赖|42.2 直接与间接依赖]]
- [[#42.3 同后端限制|42.3 同后端限制]]
- [[#42.4 分叉图|42.4 分叉图]]
- [[#42.5 汇合图|42.5 汇合图]]
- [[#42.6 分组大小限制|42.6 分组大小限制]]
- [[#42.7 多入口函数|42.7 多入口函数]]
- [[#42.8 分组结果的收益复核|42.8 分组结果的收益复核]]

## 42.0 为什么“相邻且同后端”仍可能不能合并

设左侧 NPU 组的结果同时送入一个主机组和下方 NPU 组，而主机组的结果也送入下方 NPU 组。如果下方 NPU 组合并进左侧组，就会让一个组既依赖主机组，又被主机组依赖，形成循环。源码会传播直接和间接依赖，只有不会产生循环时才合并。

> [!note] 分组与运算融合不是同一件事
> Composite 表示已识别的运算组合；外层 Codegen 函数表示一次交给外部编译器的子图。一个外层函数可以含多个 Composite，外部编译器仍可在内部决定是否进一步融合。

## 42.1 父组与当前组

> [!abstract] 本节内容
> 理解当前组合调用可以尝试并入哪些输入来源组

### 功能与适用范围

理解当前组合调用可以尝试并入哪些输入来源组。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

源码为表达式分配 Group，遇到 Composite 调用时检查同后端父组。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

在调试报告中打印每个表达式的组编号、根组和后端名称。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

调用参数来源、父组根节点、Codegen 名称、节点数量和返回值。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

参数不是 Composite 结果或父组属于其他后端时新建组。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

直线形图验证所有同后端组合可按预期进入一个组。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

表达式到组的对应表、父组候选和合并结果。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 42.2 直接与间接依赖

> [!abstract] 本节内容
> 保证分组不会改变原图执行先后关系或形成循环

### 功能与适用范围

保证分组不会改变原图执行先后关系或形成循环。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

源码维护 group_deps，并传播参数组的间接依赖。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

若自研分组器增加成本规则，仍必须保留完整依赖传播。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

组依赖集合、传递依赖、拓扑顺序、跨组值和控制流位置。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

候选父组出现在其他父组依赖集合中时不得合并。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

使用菱形、交叉依赖和三层依赖图验证无环。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

依赖表、拓扑检查结果和未合并原因。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 42.3 同后端限制

> [!abstract] 本节内容
> 防止不同外部编译器的组合进入同一个外部函数

### 功能与适用范围

防止不同外部编译器的组合进入同一个外部函数。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

GetCodegenName 从 Composite 名称取得后端，MergeGroup 会检查两组后端相同。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

后端前缀必须稳定；共享硬件的两个编译器也应通过明确的上层编排合作。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

Composite 名称、后端前缀、目标选项、产物格式和运行时 type key。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

后端名称不同或运行时 ABI 不兼容时始终保持分开。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

交替放置两个后端的组合，检查外部函数没有混合。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

每组后端、组合名称列表和运行时要求。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 42.4 分叉图

> [!abstract] 本节内容
> 处理一个 NPU 结果被多个后继节点读取时的分组和输出设计

### 功能与适用范围

处理一个 NPU 结果被多个后继节点读取时的分组和输出设计。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

源码依据每个参数组及其依赖判断可合并集合，不会简单按文本相邻合并。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

评估把共享值作为外部函数内部值还是公开输出，结合后继后端与复制成本决定。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

共享值使用者、输出数量、驻留设备、主机读取、生命周期和复制次数。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

隐藏仍被组外使用的值或复制成本超过收益时不进行大组整合。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

构造一对多图，检查输出接口、结果一致性和数据复制记录。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

使用者列表、分组图、外部输出表和复制字节。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 42.5 汇合图

> [!abstract] 本节内容
> 处理多个父组结果共同进入一个后继组合的情况

### 功能与适用范围

处理多个父组结果共同进入一个后继组合的情况。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

MergeCompositeFunctions 会收集可合并父组，并检查父组之间的依赖。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

允许无相互依赖的同后端父组与当前组整合，但控制外部输入数。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

父组数量、相互依赖、输入参数、峰值内存、并行执行和组内调度。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

任何父组依赖另一个候选父组且整合会成环时保留独立组。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

使用两路并行计算后相加的模型，比较整合前后函数接口和结果。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

父组集合、依赖集合、合并顺序和峰值占用。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 42.6 分组大小限制

> [!abstract] 本节内容
> 避免合法但过大的外部函数超过编译时间、内存或固件命令上限

### 功能与适用范围

避免合法但过大的外部函数超过编译时间、内存或固件命令上限。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方 Pass 主要依据依赖和后端名称，项目可在之后增加拆分步骤或让后端编译器拒绝。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

设置最大 Composite 数、输入输出数、估计命令数和峰值工作区。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

组节点数、估计命令、常量字节、工作区、编译时间和设备上限。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

超过任一硬限制时按依赖安全位置拆分，不能任意截断。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

逐步增加重复层，确认达到阈值后分组数量按预期变化。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

阈值配置、拆分位置、拆分原因和资源估计。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 42.7 多入口函数

> [!abstract] 本节内容
> 确保模块包含多个公开函数时只处理可到达的外部组合并保持符号唯一

### 功能与适用范围

确保模块包含多个公开函数时只处理可到达的外部组合并保持符号唯一。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

RunCodegen 支持入口函数名称数组，并会补充外部可见且不属于特定后端的 Relax 函数。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

编译服务显式传入发布入口，内部调试函数不应进入最终包。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

入口列表、函数可见性、调用图、共享外部函数、符号和常量。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

入口不存在、私有函数被错误发布或同一外部函数重复编译时报告。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

包含两个入口和一个共享内部函数，检查最终模块与导出符号。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

入口清单、可达函数、被删除函数和导出目录。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 42.8 分组结果的收益复核

> [!abstract] 本节内容
> 在正确分组后再确认较大外部函数确实减少复制和提交

### 功能与适用范围

在正确分组后再确认较大外部函数确实减少复制和提交。该项位于两个编译阶段的责任交接处：前一阶段必须把后一阶段所需信息写入 IR、属性或结构化参数，后一阶段只能依据这些信息处理。检查打印的 IR 时，应先找到负责交接的函数、属性和调用，再确认普通 TVM 节点与外部后端节点分别保留在哪里。

### 官方实现中的具体行为

官方机制负责功能正确的外部函数组织，不承诺每次合并都带来目标设备性能提升。本手册以 v0.24.0 源码、对应测试和当前官方页面共同核对。若在线页面与固定版本存在目录或接口差异，项目代码以固定提交的函数签名为准，在线页面用于说明整体方法。检查源码时同时查看注册位置、调用位置和测试，不要只看一个帮助函数。

### 自研 NPU 中的实现方法

用成本报告比较整合前后提交数、复制字节、设备驻留和峰值内存。建议把这一项实现为独立函数或独立类，并把硬件固定限制从 Target 能力文件传入。实现函数返回结构化结果，其中至少包含成功状态、原因码、涉及的 Relax 节点、关键属性摘要和建议处理方式。这样，编译报告、单元测试和模型覆盖统计都能使用同一结果。

### 必须逐项核对的信息

外部调用数量、输入输出字节、工作区、设备执行时间和主机等待。核对时要记录数值单位、维度次序、数据类型、有符号性、内存类别和版本来源。形状中的符号变量要保留名称及约束，不能用一个示例值替换后继续编译。常量内容很大时可以只记录稳定名称、数据类型、形状和内容散列值，但调试工具应能找到原始权重。

### 拒绝与异常处理

整合造成峰值内存显著增加或阻止并行时，可依据明确策略重新拆分。拒绝不是失败，只表示该候选继续由 TVM 自带后端或另一外部后端处理。真正的编译错误只用于模型本身无效、产物内部不一致、所需运行时没有注册、或用户明确要求全部进入 NPU 但仍有节点未被接纳等情况。

### 测试方法

同一模型生成两种分组配置，在设备上测量并检查结果一致。每个正向样本至少再配一个只改变单一条件的反向样本，使测试失败时能立即知道是哪条规则失效。对 IR 变换使用结构比较，对编译产物使用字段解析和散列比较，对运行结果使用独立参考实现，对性能使用设备端计时与多次统计。

### 应保留的证据

配置、估计值、实测值和选择理由。按模型、外部函数和编译阶段归档，在总清单中保存内容散列值；推导数值同时保存原始输入与算法版本。

> [!important] 完成条件
> 能从变换前后 IR 追踪到外部产物和运行时调用；能说明接纳结果；能用最小测试复现，并能在新进程加载产物。

## 本章检查清单

- [ ] 能用自己的话说明本章每个 TVM 属性或 API 的作用；
- [ ] 能在变换前后 IR 中找到对应结构；
- [ ] 能指出 Example NPU 中仅仅用于说明机制的部分；
- [ ] 能写出自研后端的输入、输出、失败原因和测试；
- [ ] 能保存足以在另一进程或另一台开发机复现的证据；
- [ ] 能说明本章内容与前后章节的关系。

## 继续阅读

本章属于 BYOC 专题。建议同时参照 [[15-Relax DPL 子图识别与分区]]、[[16-BYOC 代码生成器]]、[[17-NPU 运行时与驱动适配]]、[[26-端到端参考实现骨架]] 和 [[29-官方资料与源码导航]]。

## 实现说明与验证步骤

> [!note] 依据与适用范围
> 本节依据所列 Apache TVM 官方资料和 v0.24.0 源码整理，给出输入条件、操作步骤、预期结果、异常处理和自研 NPU 适配要求。接口细节以固定版本源码与测试为准，在线页面用于补充说明。

### 42.A 目标与适用范围

两个连续组合函数属于同一后端，检查 `MergeCompositeFunctions` 能否把它们放入一个外部函数，同时不引入循环依赖。

参考样本保持输入规模较小，以便直接检查对象、字段和输出。首次执行时不要同时加入图改写、外部代码生成和真实设备执行；应先确认当前阶段的输入与输出，再增加后续处理。建议为该项验证建立独立目录，保存命令、标准输出、IR、编译产物摘要和环境信息。

### 42.B 参考实现

**官方依据：** [External Library Dispatch](https://tvm.apache.org/docs/arch/external_library_dispatch.html)。

**v0.24.0 源码位置：** `src/relax/transform/merge_composite_functions.cc` 与对应测试。

```python
from tvm.relax.transform import (
    FuseOpsByPattern,
    MergeCompositeFunctions,
)

def group_for_backend(mod, patterns):
    mod = FuseOpsByPattern(
        patterns,
        bind_constants=False,
        annotate_codegen=False,
    )(mod)
    return MergeCompositeFunctions()(mod)
```

按以下顺序执行：

1. 在新进程中确认 `tvm.__file__`、动态库路径和 TVM 提交，避免受到旧进程注册状态影响；
2. 将参考实现保存到单独文件，只补充本节明确要求的输入对象，不先加入额外优化；
3. 执行后保存完整输出；若生成 IR，同时保存 `script(show_meta=True)` 的文本；
4. 把输出与下面的预期现象逐项比较，不以“没有抛出异常”代替功能检查；
5. 重复执行一次并比较主要产物，确认结果没有依赖临时全局状态或未固定的遍历顺序。

> [!success] 预期现象
> 合并后的外层函数带 `Codegen` 与稳定全局名称，内部仍可保留多个 `Composite` 调用。函数参数只包含组外输入，返回值包含组外使用的结果。

> [!tip] 输出检查顺序
> 先看对象类型和函数数量，再看属性、调用形式、形状与数据类型，最后看运行结果或编译产物。若前一项不符合预期，先停止后续步骤。这样能把问题限定在最早出现差异的阶段。

### 42.C 单项条件验证

构造交叉依赖，使两个候选组若合并会出现循环。Pass 应保守保持分开，不能为了减少调用次数破坏依赖顺序。

执行条件变化样本时，复制参考实现的输入与环境，只修改上文指出的一项。对两个输出进行结构比较，并记录以下四项：

1. 第一次出现差异的是哪一个阶段？
2. 差异表现为函数、属性、形状、数据类型、编译产物还是运行结果？
3. 当前行为是明确拒绝、交给其他后端执行，还是编译错误？
4. 日志是否足以让另一位开发者不查看本次进程状态也能复现？

如果同时观察到多个变化，应继续缩小条件变化样本。参考实现用于确认正常处理，单项条件验证用于确认系统为何接受、拒绝或转交当前输入。

### 42.D 自研 NPU 适配要求

合并收益要与片上容量、并行机会和编译时间一起评估。外部函数过大可能降低缓存命中或让设备编译器难以处理，应提供最大节点数与资源估算。

改造时建议保留三份相互独立的输入：最小 Relax 或 TensorIR、设备编译器输入、运行时调用输入。三份输入使用同一个样本编号，并记录转换前后的关键字段。这样可以分别测试 TVM 变换、NPU 编译器和驱动，不需要每次都运行完整模型。

至少补充以下样本：

- 一个完全满足能力要求的正向样本；
- 一个只改变数据类型的反向样本；
- 一个只改变形状或属性的反向样本；
- 一个包含主机与 NPU 混合执行的样本；
- 一个导出后在新进程加载的样本；
- 若支持动态尺寸，再增加最小值、常用值和最大值附近的样本。

### 42.E 源码定位

从上文列出的源码位置选择一个公开 API，依次找到 Python 调用、FFI 注册、C++ 实现和测试。记录函数签名、主要输入、主要输出、会写入的模块属性以及失败方式。若名称只在文档出现而源码中找不到，继续搜索注册字符串、属性键或错误文本。

> [!important] 验证项目
> 1. 执行前记录参考实现的对象关系；2. 执行后用实际输出修正记录；3. 增加一个会被明确拒绝的输入；4. 标明自研 NPU 接入需要修改的层次与保持不变的层次；5. 把结果整理成可由自动测试检查的断言。

### 42.F 检查清单

- [ ] 示例可在固定 v0.24.0 环境重复执行，或已明确列出所需可选依赖；
- [ ] 预期现象包含可检查的结构、字段或数值，不只是“运行成功”；
- [ ] 单条件变化能触发预期的拒绝、转交或结构差异；
- [ ] 能从官方页面定位到固定版本源码和测试；
- [ ] NPU 改造说明包含编译阶段、运行阶段与部署阶段；
- [ ] 失败时保留最小输入、环境、阶段输出和第一个错误。
