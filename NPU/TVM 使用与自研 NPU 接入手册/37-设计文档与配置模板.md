---
tags:
  - TVM
  - NPU
  - 编译器
  - Relax
  - TensorIR
status: maintained
baseline: Apache TVM v0.24.0
updated: 2026-07-29
---

# 37. 设计文档与配置模板

> [!abstract] 用途
> 本章提供 Target、算子规格说明、接纳报告、编译产物、缓冲区、命令、数值、测试、性能、错误和版本记录模板。建议把模板转换成仓库内的结构规范与自动检查工具。

## 快速目录

- [[#37.1 Target 能力文件|37.1 Target 能力文件]]
- [[#37.2 算子规格说明|37.2 算子规格说明]]
- [[#37.3 模式接纳报告|37.3 模式接纳报告]]
- [[#37.4 编译流程清单|37.4 编译流程清单]]
- [[#37.5 编译产物清单|37.5 编译产物清单]]
- [[#37.6 逻辑缓冲区表|37.6 逻辑缓冲区表]]
- [[#37.7 命令目录|37.7 命令目录]]
- [[#37.8 数值规则表|37.8 数值规则表]]
- [[#37.9 测试向量清单|37.9 测试向量清单]]
- [[#37.10 性能报告|37.10 性能报告]]
- [[#37.11 错误报告|37.11 错误报告]]
- [[#37.12 版本兼容表|37.12 版本兼容表]]
- [[#37.13 模型发布记录|37.13 模型发布记录]]

## 编写原则

1. 机器可读字段与面向人的说明同时存在；
2. 每个数值带单位；
3. 每个文件带结构规范版本；
4. 每个产物带来源提交和内容散列；
5. 不使用无法从测试或工具确认的模糊描述；
6. 版本不兼容时默认拒绝，不静默忽略字段。

## 37.1 Target 能力文件

> [!abstract] 用途
> 给编译器提供可复现的硬件固定限制和默认参数，禁止从编译主机自动猜测。

### 必填内容

schema_version、arch、device_type、matrix_m/n/k、vector_bytes、sram_bytes、dma_alignment、max_commands、支持的数据类型、固件 ABI、可选功能位。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

字段类型与范围；未知字段处理；缺省值；硬件能力寄存器核对；内容散列；不同型号继承关系。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

模式检查、分块选择、代码生成、静态检查、运行时加载和性能报告。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "Target 能力文件"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.2 算子规格说明

> [!abstract] 用途
> 把“支持某算子”展开为前端结构、属性、形状、数据类型、数值、布局、尾部、工作区和错误处理。

### 必填内容

Relax 名称、组合名称、输入输出、属性集合、动态形状范围、布局、累加类型、舍入、饱和、工作区公式、设备命令和其他后端执行规则。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

每个限制有正反样本；公式可独立计算；前端不同写法；组合前后行为；版本变化。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

DPL 检查函数、自研内部 IR、参考模型、固件、RTL 和测试。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "算子规格说明"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.3 模式接纳报告

> [!abstract] 用途
> 记录每个候选子图被 NPU 接纳或改由其他后端执行的原因。

### 必填内容

模型函数、候选编号、模式名称、源节点、形状、数据类型、属性摘要、Target、结果、原因码、详细说明、估计收益。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

候选总数守恒；原因码稳定；没有空说明；同一输入重复编译结果一致；敏感模型信息可按需隐藏。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

编译日志、模型覆盖分析、能力规划、版本回归和性能优化。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "模式接纳报告"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.4 编译流程清单

> [!abstract] 用途
> 固定每个 Pass 的名称、顺序、配置、输入要求和输出证据。

### 必填内容

阶段编号、Pass 名、版本、配置、前置条件、产生属性、删除属性、缓存键、IR 文件名、耗时和错误。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

顺序变化有测试；默认编译流程也展开记录；Pass 重复执行稳定；失败停止位置明确。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

CLI、编译服务、CI、问题定位和版本升级。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "编译流程清单"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.5 编译产物清单

> [!abstract] 用途
> 描述一个可部署包中共享库、NPU 模块、权重、元数据和调试文件的关系。

### 必填内容

格式版本、每个文件名、字节数、内容散列、函数目录、目标、所需运行时、驱动与固件版本、创建时间和构建来源。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

文件完整；散列正确；未知版本拒绝；可在新进程加载；仅运行时环境依赖齐全。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

发布工具、运行时加载器、模型仓库和现场诊断。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "编译产物清单"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.6 逻辑缓冲区表

> [!abstract] 用途
> 把输入、输出、常量、工作区和片上暂存区用统一编号描述，供内存规划与重定位。

### 必填内容

buffer_id、名称、角色、形状、数据类型、布局、字节数、对齐、内存类别、生命周期、物理偏移、读写者。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

大小无溢出；同时存活区域不重叠；输入输出 ABI 一致；常量只读；异步生命周期正确。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

代码生成、运行时、命令反汇编、静态检查和内存报告。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "逻辑缓冲区表"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.7 命令目录

> [!abstract] 用途
> 让二进制命令可以被独立解析，并从设备错误返回到源节点。

### 必填内容

命令序号、opcode、执行单元、源节点、读取缓冲区、写入缓冲区、偏移、长度、等待事件、完成事件和字段摘要。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

地址在缓冲区内；事件生产者存在；依赖无环；命令数量合法；字段值属于固件版本。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

固件、模拟器、反汇编器、错误报告和性能分析。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "命令目录"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.8 数值规则表

> [!abstract] 用途
> 统一编译器、参考模型、固件和 RTL 对低比特整数计算的位级处理。

### 必填内容

输入输出数据类型、有符号性、scale、zero point、参数维度、乘积宽度、累加宽度、bias 域、整数乘数、移位、舍入、饱和和特殊函数版本。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

正负中点；最小最大值；临时宽度上界；per-channel 参数数量；INT4 打包；组合运算次序。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

图转换、代码生成、设备命令、参考模型、单算子测试和模型精度报告。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "数值规则表"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.9 测试向量清单

> [!abstract] 用途
> 让每个失败样本可以跨编译器、模拟器、固件和 RTL 重复。

### 必填内容

测试名称、随机种子、输入文件、常量文件、形状、数据类型、Target、编译选项、预期文件、允许误差和预期错误。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

文件散列；端序；打包；预处理；参考模型版本；极值与尾部覆盖；失败样本最小化。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

Python 测试、C++ 测试、板卡服务、RTL 环境和持续集成。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "测试向量清单"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.10 性能报告

> [!abstract] 用途
> 把端到端时延拆成可解释部分，并把结果与模型、产物和设备版本关联。

### 必填内容

预热、重复、时间分布、准备、复制、提交、排队、执行、等待、片外字节、命令数、MAC、峰值内存、各单元忙碌周期、温度和频率。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

功能先通过；设备独占；网络传输分开；计数器范围正确；冷启动与稳态分开；统计方法固定。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

调优器、架构分析、版本门限、模型发布和性能问题定位。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "性能报告"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.11 错误报告

> [!abstract] 用途
> 用统一结构记录编译、加载、复制、提交、执行和等待错误。

### 必填内容

阶段、主机错误码、驱动错误码、设备错误码、外部函数、提交序号、命令序号、执行单元、可恢复性、详细说明和证据目录。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

第一个错误不被后续错误覆盖；资源清理结果；设备状态；重试条件；敏感信息处理。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

运行时、编译服务、测试平台、现场日志和故障统计。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "错误报告"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.12 版本兼容表

> [!abstract] 用途
> 明确 TVM、后端编译器、模块格式、运行时、驱动、固件和硬件之间允许的组合。

### 必填内容

组件名称、版本范围、最低版本、最高已测版本、模块 ABI、固件 ABI、功能限制、升级步骤和回退版本。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

加载阶段执行检查；未知组合默认拒绝；兼容声明有自动测试；旧产物处理明确。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

构建系统、发布工具、运行时、部署平台和维护人员。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "版本兼容表"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 37.13 模型发布记录

> [!abstract] 用途
> 把模型来源、输入输出、编译配置、测试、性能和限制形成一个可重新构建的记录。

### 必填内容

模型名称、源文件散列、权重散列、前端版本、输入预处理、输出后处理、Target、流水、产物散列、测试报告、性能报告、已知限制和维护人。

字段使用稳定名称和明确单位。大小统一注明元素数还是字节数，时间统一注明秒、毫秒、微秒或周期，形状使用完整维度次序，地址说明是逻辑偏移、IOVA 还是其他设备地址。代码中的字段名保留英文，中文说明使用 [[33-术语与 API 速查]] 的写法。

### 核对方法

从记录可重新构建；部署文件与散列一致；主机执行节点清单存在；动态范围与状态说明完整。所有自动计算字段同时保存输入和算法版本，使另一工具能够复算。表内的“未设置”与数值 0 必须区分。

### 使用者

模型仓库、部署服务、应用团队、质量团队和版本升级。不同使用者读取同一份结构化数据，减少手工复制产生的不一致。若某个组件需要额外字段，应先提升结构规范版本并给出旧版本处理方法。

### YAML 骨架

```yaml
document:
  kind: "模型发布记录"
  schema_version: 1
  created_by:
  source_commit:
  target:
  inputs:
  outputs:
  checks:
  evidence:
  notes:
```

### 评审问题

1. 字段是否能由工具自动检查？
2. 是否有含糊单位或隐含默认值？
3. 版本变化后旧文件如何处理？
4. 失败时能否从本文件找到直接证据？
5. 是否存在由多个团队各自维护的重复定义？

> [!tip] 实施建议
> 先用 YAML 或 JSON 建立可读版本，再为性能和加载需求增加紧凑二进制。结构定义放在共享目录，生成 Python、C++ 和固件读取代码，避免三份手写结构逐渐不同。

## 模板自动检查

模板只有在进入自动工具后才真正发挥作用。建议为每一种文档建立 JSON Schema 或等价的类型定义，并在提交时检查必填字段、枚举值、单位、整数范围、文件散列和版本关系。算子规格说明中的形状限制可以生成 Python 检查函数，Target 能力文件可以生成 C++ 常量读取代码，命令目录可以生成反汇编字段表。这样，文档、编译器和固件不需要分别抄写相同参数。

自动检查工具应输出文件名、字段路径、期望类型、实际值和修复提示。例如 `targets/npu_v1.yaml` 的 `dma_alignment` 若不是 2 的幂，错误应直接指出字段，而不是等到设备提交后报告地址问题。模块格式和命令格式还应加入未知字段、缺失字段、截断输入、超大长度和整数溢出的测试。

对会影响数值的字段，要根据规格说明生成固定测试向量。修改舍入方式、累加宽度、特殊函数版本或 INT4 打包次序时，CI 自动选择相关单算子与组合算子测试。对会影响性能但不改变结果的字段，则运行代表尺寸的性能集合，并保留新旧时间分布。两类改动不能只用同一种门限。

模板升级采用“读取旧版本、写出新版本”的迁移工具。迁移前后保存内容散列和字段差异，禁止运行时在没有日志的情况下丢弃旧字段。若旧版本无法安全解释，新运行时应拒绝加载并给出所需工具版本。发布包同时携带结构规范、示例、迁移说明和最小读取器测试。

> [!important] 共同来源
> 硬件固定参数只能有一个受版本控制的共同来源。RTL 参数、固件能力表、编译器 Target 和文档表格由工具从该来源生成，或由自动检查证明一致。手工维护多份相同数值很容易在版本升级时产生差异。

## 建议的文件存放位置

```text
acme-npu/
├── schema/
├── targets/
├── operator-specs/
├── module-format/
├── command-format/
├── test-vectors/
├── performance/
└── releases/
```

这些目录应进入版本控制。二进制大文件可放入有内容寻址能力的制品库，仓库保留清单和散列值。
