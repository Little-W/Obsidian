# Alkaid RISC-V CPU 答辩指南

本指南面向设计答辩，回答重点放在 CPU 微架构、接口时序、冒险控制、存储访问、时序优化和验证结果。软件系统和 THP 应用只用于说明 SoC 的可运行性，不作为核心微架构结论的主要依据。

## 术语约定

| 代号或术语 | 本文含义 |
|:--|:--|
| P3 | 三级流水线配置：IFU 保留取指寄存器，译码、分发和短延迟执行共用级间边界。 |
| P4 | 四级流水线配置：IFU、译码与分发、执行、写回四个主要阶段。 |
| P5 | 五级流水线配置：IFU、IDU、Dispatch、EXU、WBU 分别形成独立阶段。 |
| RAW、WAW | 读后写（RAW）和写后写（WAW）数据冒险。 |
| HDU | 数据冒险检查单元，负责 RAW、WAW 和部分结构冲突检查。 |
| IFU | 取指单元，负责取指地址、指令响应、指令流整理和前端恢复。 |
| IDU | 指令译码单元，负责指令字段解析和控制信息生成。 |
| Dispatch | 分发单元，负责操作数选择、冒险检查、执行单元选择和部分地址预计算。 |
| EXU | 执行单元，包含算术逻辑单元（ALU）、分支执行单元（BRU）、加载存储单元（LSU）、乘法单元（MUL）、除法单元（DIV）和控制状态寄存器（CSR）执行单元。 |
| WBU | 写回单元，负责完成结果仲裁、GPR 写回和架构状态更新。 |
| GPR | 通用寄存器组。 |
| BPU | 分支预测单元。 |
| ISA、ABI | 指令集架构（ISA）和应用程序二进制接口（ABI）。 |
| XLEN | RISC-V 整数寄存器和基础整数数据通路的位宽。 |
| MMIO | 存储器编址输入输出。 |
| FIFO | 先进先出队列。 |
| CLINT、PLIC | 核心本地中断控制器（CLINT）和平台级中断控制器（PLIC）。 |
| BSP | 板级支持包。 |
| THP | 温湿度气压与 OLED 应用。 |
| MSH | RT-Thread 命令行组件。 |
| raw | 未应用配置级排除项的覆盖率统计。 |
| after-waive | 应用固定参数无法展开的合法排除项后的覆盖率统计。 |
| INSTS | 程序动态执行的指令总数。 |
| IPC | 每周期执行的动态指令数，计算为动态执行指令数除以总节拍数。 |

## 一、总体设计定位

### 1. 这款 CPU 主要解决什么问题？

回答：Alkaid 面向 FPGA 实现和 IP 核复用场景，目标是在有限硬件资源下提供可配置的 RISC-V CPU 与 SoC。设计同时处理三级、四级和五级流水线切换、RV32/RV64、分支预测、AXI4-Lite 存储访问、Cache、乒乓缓存和可裁剪外设等工程问题。重点不是某一个孤立单元，而是让不同配置保持一致的外部接口和架构结果。

### 2. 项目最核心的微架构特色是什么？

回答：核心特色是轻度乱序单发射和有限乱序完成。CPU 每周期最多发射一条指令，但短延迟指令可以在长延迟指令之前完成，WBU 依据 commit id 和寄存器状态决定是否允许写回。该结构比完整重排序缓冲区状态更少，仍能覆盖乘除、访存和普通运算重叠时最常见的完成冲突。

### 3. 为什么选择轻度乱序，而不是完整乱序执行？

回答：本设计的目标器件是 FPGA，完整乱序执行需要较大的重排序缓冲区、多个发射端口、复杂的唤醒选择逻辑和更大的旁路网络，会明显增加面积和时序压力。轻度乱序只保留一条发射通路和有限状态槽位，重点解决高频 RAW、WAW、长延迟完成和访存等待问题。这样可以在结构复杂度、性能和 FPGA 资源之间取得平衡。

### 4. Zbc 在设计中属于什么层次？

回答：Zbc 是 RISC-V 位操作指令集扩展，不属于核心微架构创新点。硬件实现上，Zbc 复用 MUL 的多周期执行资源，支持 `clmul`、`clmulh` 和 `clmulr`；软件只有在 `-march` 中打开 Zbc 时才会生成这些指令。Zbc 的加入说明 CPU 具有可扩展的指令执行结构，但不能把它描述成新的流水线组织方式。

### 5. 为什么同时支持 RV32 和 RV64？

回答：RV32 适合资源受限的 FPGA SoC，RV64 适合需要更宽整数运算和更大地址范围的应用。XLEN 控制通用寄存器、ALU、MUL、DIV、CSR 数据通路及软件 ABI 的宽度，软件分别使用 `ilp32` 和 `lp64`。两种配置共享同一套微架构控制思想，减少了维护两套独立 CPU 的成本。

### 6. P3、P4、P5 的主要差异是什么？

回答：P5 将取指、译码、分发、执行和写回分别设置为独立阶段；P4 去掉译码到分发之间的寄存边界，把两者合并；P3 进一步取消分发寄存边界，使译码、分发和短延迟执行在更紧凑的路径上工作。三种配置都保留 IFU 响应后的寄存器和 WBU 结果寄存器，因此外部接口、复位方式和有效暂停冲刷规则保持一致。

### 7. 流水线级数改变后，软件是否需要重新编译？

回答：仅改变 PIPELINE_STAGES 不改变 RISC-V 架构、ISA 或 ABI，理论上可以运行同一软件镜像。实际工程中仍建议按目标硬件重新生成镜像，以确保存储初始化内容、启动地址和当前配置保持一致。流水线级数改变的是内部延迟和性能，不改变程序可见的指令行为。

### 8. 设计中如何区分架构状态和流水线内部状态？

回答：GPR、CSR、程序计数器和存储器可观察内容属于架构状态，必须按照 RISC-V 规则更新。等待槽、局部旁路、Cache 标签、AXI 请求状态和 commit id 属于流水线内部状态，只用于控制完成和保持数据。发生冲刷、异常或中断时，内部状态可以清除或重建，但不能破坏已经确认的架构状态。

### 9. 单发射是否意味着每周期只能完成一条指令？

回答：单发射限制的是每周期进入执行通路的指令数，不等于所有指令都在一个周期完成。短延迟 ALU 指令可能连续完成，MUL、DIV、LSU 和 AXI 访问则可能跨越多个周期。有限乱序完成允许不同指令的完成时间重叠，但 WBU 仍需检查 commit id、目的寄存器和写回资格。

### 10. 设计的主要取舍是什么？

回答：主要取舍是用较小的控制状态和有限旁路换取高频依赖场景的性能，用参数化结构支持不同资源规模，用 Cache 和乒乓缓存改善同步存储器访问。代价是不能达到完整乱序处理器的峰值吞吐，覆盖率和边界测试也需要针对多种参数组合分别检查。该取舍符合 FPGA IP 核强调可配置、可验证和可实现的定位。

## 二、顶层接口与 SoC 组织

### 11. CPU 顶层最重要的外部接口有哪些？

回答：顶层接口包括 `clk`、`rst_n`、M0_AXI 只读主接口、M1_AXI 读写主接口、中断控制接口以及与 SoC 配置相关的参数接口。M0 由 IFU 使用，访问指令空间；M1 由 LSU 使用，访问数据空间和 MMIO。外部接口在 P3、P4、P5 中保持一致，便于比较不同流水线配置。

### 12. M0_AXI 和 M1_AXI 为什么要分开？

回答：M0_AXI 专门服务取指，只包含 AR 和 R 通道，结构简单且不会产生写事务。M1_AXI 服务数据访存和 MMIO，需要 AW、W、B、AR、R 五个通道。分开后可以分别设置取指和数据访问的缓存、乒乓缓存及仲裁策略，也便于观察取指暂停与数据访问等待的来源。

### 13. 为什么采用 AXI4-Lite，而不是 AXI4 Full？

回答：目标是连接片上 IMEM、DMEM 和 APB 外设，单次访问和低复杂度比突发传输更重要。AXI4-Lite 不使用 Burst、ID、USER、LAST 等扩展信号，协议状态更少，便于在 FPGA 上验证和时序优化。Cache、FIFO 和乒乓缓存负责改善连续访问效率，而不是依赖 AXI4 Full 突发功能。

### 14. AXI4-Lite 的握手规则如何保证正确？

回答：发送端只有在 `VALID` 置位且接收端 `READY` 置位的同一时钟沿完成传输。地址、写数据和响应信息在等待期间保持不变，接收端不能用短暂的 `READY` 脉冲改变未完成事务的内容。模块级 testbench 会检查 valid-ready 交互、地址和数据保持、响应顺序以及错误响应。

### 15. M1 同时有读写请求时如何处理？

回答：LSU 产生的读写请求先进入 M1 侧控制逻辑，再由 AXI 互连完成目标选择和返回管理。未完成请求状态保存请求类别、地址和必要的返回信息，响应到达后根据当前状态送回 LSU。对于需要保持次序的场景，DCache 响应 FIFO 和互连内部状态共同阻止后到响应覆盖先到请求。

### 16. IMEM、DMEM 和 MMIO 如何区分？

回答：地址译码在 AXI 互连中完成，IMEM 和 DMEM 是存储器目标，CLINT、PLIC 和 APB 外设使用各自的地址区间。M0 访问指令空间，M1 访问数据空间和 MMIO。软件通过链接脚本把代码、只读数据和可写数据分别放入对应存储区域，再通过固定 MMIO 地址访问控制器寄存器。

### 17. CLINT 在 CPU 中承担什么职责？

回答：CLINT 负责机器软件中断、机器定时器中断以及异常和中断入口地址管理。它接收当前指令、控制转移、异常条件和流水线暂停状态，输出陷入或 `mret` 返回地址。CPU 只根据 CLINT 给出的跳转有效和目标地址更新 IFU，不把定时器计数逻辑混入普通执行单元。

### 18. PLIC 的中断处理方式有什么特点？

回答：PLIC 管理 11 个外设中断源，为每个源提供使能位、优先级以及处理函数和参数。硬件直接选择最高优先级的有效中断，并把处理函数地址和参数送到软件入口；软件处理完成后清除设备本身的挂起状态。该实现没有采用通用 PLIC 的 claim/complete 寄存器形式，减少了本项目 SoC 的软件和硬件状态。

### 19. AXI2APB 模块为什么需要单独验证？

回答：AXI4-Lite 和 APB 的握手方式、等待方式和错误响应表达不同，转换器必须保持地址、写数据和读写方向的一致性。AXI2APB testbench 会覆盖 APB 等待、正常读写和错误响应，并检查 AXI 侧响应只在 APB 事务结束后产生。该模块错误会影响所有 UART、GPIO、SPI、I2C 和 Timer，因此需要独立模块级验证。

### 20. 如何保证硬件参数和 SoC 配置一致？

回答：配置值先在 SoC 系统层确定，再逐级传入 CPU、Cache、乒乓缓存、存储器和外设。XLEN、ISA 扩展、流水线级数、BPU、Cache 和存储初始化参数必须在 RTL、软件编译选项和 Vivado 图形化配置界面（GUI）中保持一致。验证任务会把流水线配置参数写入任务配置，并检查编译、仿真和结果摘要使用同一组输入。

## 三、取指、译码与分支预测

### 21. IFU 的主要职责是什么？

回答：IFU 负责维护取指地址、向 M0_AXI 发起读请求、接收指令数据、处理压缩指令流，并根据预测或执行结果更新下一取指地址。它还要响应暂停、冲刷、异常入口和 `mret` 返回。IFU 不负责普通整数运算，控制转移的最终正确性由 BRU 和 CLINT 共同确认。

### 22. C 扩展为什么会增加取指复杂度？

回答：C 扩展使用 16 位指令，32 位取指数据中可能同时包含两个 16 位指令，也可能包含一个 32 位指令的前半部分。`ifu_c_stream` 需要保存跨取指字边界的半字，并在下一次响应到达后完成拼接。冲刷时必须清除保存的半字，防止错误路径的残留数据再次进入译码。

### 23. 基础 BPU 包含哪些结构？

回答：基础 BPU 可以包括静态偏置、分支历史表（BHT）、循环预测器、相关方向预测、JALR 目标缓存和返回地址栈（RAS）。不同结构由参数独立生成，面积受限配置可以只保留较小的方向表和固定规则。基础 BPU 的目标是以较小资源减少明显的分支停顿，而不是追求大型处理器的预测规模。

### 24. 增强 BPU 比基础 BPU 多做了什么？

回答：增强 BPU 增加分支目标缓冲器（BTB）、GShare 方向预测器、增强 RAS 和更多预测状态字段，并可以继续启用基于多段历史的 TAGE 预测器。BTB 提供更早的目标地址，GShare 利用全局历史改善方向判断，RAS 处理返回地址，TAGE 用多级历史表提高复杂分支的预测能力。关闭增强 BPU 时，这些表项和控制逻辑不生成。

### 25. TAGE 为什么设计为独立可选项？

回答：TAGE 会增加表项存储、历史维护和查询选择逻辑，对面积和时序有直接影响。把它作为增强 BPU 内的独立参数，可以在资源受限配置中关闭，也可以在较高性能配置中打开。这样可以分别比较 BPU 结构的收益，而不必改变 IFU 与 BRU 的接口。

### 26. 分支预测错误后如何恢复？

回答：BRU 在 EXU 中计算实际条件、目标地址和下一取指地址，并把恢复信息送回 IFU。Ctrl 产生冲刷控制，清除错误路径上的取指、译码、分发和等待状态，同时禁止错误路径指令写回。恢复后的 IFU 从正确地址重新取指，异常和中断跳转则由 CLINT 提供更高优先级的目标地址。

### 27. JAL、JALR 和条件分支的处理有什么不同？

回答：JAL 的目标地址由当前 PC 与立即数计算，返回地址写入目的寄存器；JALR 的目标地址依赖寄存器值，需要等待操作数准备完成并清除最低位；条件分支要比较两个操作数，再依据条件选择目标地址或顺序地址。三类指令都由 BRU 最终确认，但 IFU 的预测输入字段和恢复条件不同。

### 28. FENCE.I 为什么需要前端恢复控制？

回答：FENCE.I 要求后续取指能够看到前面写入的指令内容，因此不能只当作普通 ALU 指令处理。执行到 FENCE.I 时，需要等待相关存储操作完成并清除前端已经取得但尚未使用的指令状态，再从正确地址重新取指。验证中会把 FENCE.I 与分支、写存储器及暂停组合，检查旧指令不会继续执行。

### 29. IFU 如何处理 AXI 读响应延迟？

回答：IFU 通过请求状态和指令响应寄存器保存未完成取指，响应未到达时保持当前取指状态。M0 侧乒乓缓存可以寄存 AXI 通道字段，ICache 命中时则不需要访问外部存储器。暂停、冲刷或异常到来时，控制逻辑会按照优先级取消或忽略不再属于当前路径的响应。

### 30. 取指错误如何避免扩散到后级？

回答：每一级流水寄存器都带有有效位和必要的控制字段，冲刷时先清除有效位，再更新程序计数器。IDU 不会把无效指令送入 Dispatch，Dispatch 不会为无效指令分配 commit id，WBU 也不会写回无效结果。异常、非法指令和错误响应在前端或 CLINT 处形成明确的控制状态。

## 四、Dispatch、HDU 与流水线冒险

### 31. HDU 是什么？

回答：HDU 是数据冒险检查单元，负责检查指令源寄存器与已分配指令之间的 RAW、WAW 以及部分资源冲突。它维护有限数量的 commit id 槽位和目的寄存器信息，并结合执行类别、旁路状态和等待状态决定当前指令是否可以发射。HDU 不是完整 ROB，而是针对单发射 CPU 的紧凑控制结构。

### 32. RAW 冒险如何检测？

回答：Dispatch 读取当前指令的 rs1、rs2，与 HDU 中仍未完成的目的寄存器逐项比较。如果结果已经在 ALU、LSU 或 MUL 的局部旁路中可用，则可以直接选择旁路数据；如果结果尚未准备好，则保持当前指令或进入等待槽。比较同时检查寄存器有效位，x0 不产生真正的写回依赖。

### 33. WAW 冒险为什么也需要检查？

回答：轻度乱序完成允许后发出的短延迟指令先完成，如果两个指令写同一个寄存器，后发指令可能先写回，早发指令随后写回会产生错误结果。HDU 保存目的寄存器和 commit id，WBU 依据当前寄存器对应的最新有效状态决定是否允许写入。这样既可以保持正确的架构结果，也能允许不冲突的指令继续完成。

### 34. 什么是结构冒险？

回答：结构冒险是多个操作同时需要同一硬件资源，例如乘法器正在处理上一条指令、LSU 正在等待 AXI 响应或写回队列暂时没有空间。HDU、Dispatch 和各执行单元分别提供忙状态或接收能力，当前指令只有在所需资源可用时才发射。结构检查与数据寄存器检查同时进行，避免只解决 RAW 而忽略资源冲突。

### 35. 等待槽为什么只保存有限数量指令？

回答：目标是解决最常见的短距离依赖和单个长延迟操作，而不是实现通用指令窗口。三级和五级配置可以保存一条暂时不能发射的非有序类指令，四级配置保持当前输入。有限等待槽能够降低比较器、选择器和状态寄存器数量，也使暂停和冲刷行为更容易验证。

### 36. Dispatch 为什么要预计算地址？

回答：加载、存储和 JALR 的地址通常依赖寄存器值，若把所有地址计算都推迟到 EXU，会增加跨级数据等待。Dispatch 使用 AGU 保存项和地址前递保持机制，尽早保存基址、立即数和访问属性；当源数据准备完成时即可形成有效地址。JALR 仍由 BRU 在执行阶段确认最终目标，以保证控制转移正确。

### 37. 局部旁路解决了哪些问题？

回答：ALU、LSU 和 MUL 结果先进入 EXU 局部旁路暂存区，后续指令可以在执行级直接选择结果，而不必等待完整写回路径。它主要覆盖连续 ALU、加载后分支、乘法结果作为存储数据和短距离乘法依赖。三级配置关闭 EXU 局部旁路，依靠更严格的 HDU 阻塞保持正确性。

### 38. 为什么 P3 要关闭 EXU 局部旁路？

回答：P3 将译码、分发和短延迟执行压缩在较紧凑的路径中，继续加入局部旁路会显著增加选择器和控制条件深度。P3 采用四项紧凑比较结构，对相关写入实施更严格的阻塞，牺牲部分依赖场景的性能换取更小的控制逻辑。这样可以保持 P3 的结构简单和实现稳定。

### 39. 如何防止等待槽产生死锁？

回答：等待槽只在指令被确认不能发射时保存，并持续观察源数据、资源状态和冲刷信号。执行单元完成结果后，HDU 清除对应 commit id，等待槽重新参与选择；异常、分支恢复或复位则清空相关状态。验证会专门检查长延迟 DIV、访存响应延迟和异常同时出现时是否仍能继续接收新指令。

### 40. HDU 如何处理 x0？

回答：RISC-V 的 x0 恒为零，任何写入 x0 的指令都不能产生可见寄存器状态。HDU 不为 x0 建立有效的写回依赖，GPR 读口对 x0 直接返回零，WBU 也抑制 x0 写入。x0 专项程序会检查写 x0、紧邻读取 x0 以及长流水线下 commit id 的变化。

## 五、EXU、长延迟单元与写回

### 41. EXU 包含哪些主要单元？

回答：EXU 包含 ALU、BRU、LSU、MUL、DIV 和 CSR 执行单元。ALU 处理整数运算和逻辑操作，BRU 处理控制转移，LSU 处理加载、存储和原子访问，MUL/DIV 处理长延迟整数运算，CSR 单元处理控制状态寄存器读写。各单元通过统一的请求、完成、暂停和结果字段与 WBU 连接。

### 42. ALU 的验证重点是什么？

回答：ALU 需要覆盖加减、逻辑、移位、比较、立即数处理和 RV32/RV64 字运算。除了结果值，还要检查操作类型、操作数保持、完成有效信号和 commit id 是否对应同一条指令。RV32 ALU 与地址前递波形会同时观察操作数、前递结果、比较结果和暂停控制。

### 43. BRU 如何保证控制转移正确？

回答：BRU 重新计算条件、目标地址和顺序地址，不直接相信预测结果。条件分支只有在条件成立且目标地址有效时才产生恢复请求；JAL 和 JALR 根据各自的立即数与寄存器规则生成目标。BRU 完成后把实际结果送往 IFU 和 CLINT，错误路径由冲刷控制清除。

### 44. MUL 为什么需要多周期状态？

回答：RV64 乘法器的完整结果和高位结果会增加组合逻辑规模，按周期迭代或分段处理可以降低单周期路径深度。MUL 保存操作数、部分结果、目的寄存器和 commit id，直到计算完成才产生有效结果。验证会检查输入接收、迭代保持、结果阻塞和恢复接收四种状态。

### 45. DIV 的主要风险是什么？

回答：DIV 的风险包括除零、有符号最小值除以负一、不同 XLEN 下的符号扩展以及多周期商和余数计算。DIV 状态机需要在输入阶段锁存操作数和操作类型，在每次迭代保持中间结果，并在完成时依据 RV32 或 RV64 规则形成结果。异常和冲刷到来时，未完成的 DIV 状态必须被清除。

### 46. CSR 执行单元如何与 CLINT 协同？

回答：CSR 执行单元负责普通 CSR 地址、读写使能、写数据和旧值返回；CLINT 负责异常入口、`mepc`、`mcause`、`mstatus`、`mie` 等与陷入控制相关的状态。二者通过明确的读写端口交互，避免普通 CSR 指令和异常硬件同时写同一状态时产生不确定结果。异常优先级和流水线暂停由 Ctrl 与 CLINT 统一处理。

### 47. Zbc 如何复用 MUL 资源？

回答：Zbc 的 `clmul` 类操作采用按片段迭代的无进位乘法算法，复用 MUL 的输入保持、部分结果累加和完成接口。译码生成独立的 Zbc 操作类型，MUL 根据该类型选择异或累加而不是普通整数乘法。这样避免新增一套完整乘法器，同时保留多周期结果保持和 WBU 接口。

### 48. 长延迟结果被阻塞时怎么办？

回答：MUL、DIV 或 LSU 的结果产生后，如果 WBU 暂时不能接收，执行单元保持 `valid`、结果、目的寄存器和 commit id 不变。接收端恢复后，结果在握手完成的周期释放。RV64 MUL 波形专门检查 `ctrl_ready_i` 变化时附属字段不改变，防止结果与目的寄存器错位。

### 49. WBU 如何仲裁不同执行单元的完成结果？

回答：WBU 接收 ALU、LSU、MUL、DIV 和 CSR 的完成信息，并检查有效位、目的寄存器、commit id、异常状态和写回资格。若多个结果同时到达，仲裁逻辑按照可写回条件和内部优先规则选择一个结果，其余结果保持有效等待下一次接收。对同一寄存器的写回还要结合 HDU 保存的最新 commit id。

### 50. 如何证明 WBU 没有错误写回？

回答：模块级 testbench 会直接驱动不同执行单元的完成输入，检查结果保持、仲裁、目的寄存器和 commit id。整核程序再通过 GPR 写回观察、程序结果校验字、tohost 和 Spike 对照检查架构结果。x0、WAW、长延迟结果和异常冲刷是专门的边界条件，能够覆盖错误写回的主要来源。

## 六、LSU、Cache 与乒乓缓存

### 51. ICache 和 DCache 在系统中的位置是什么？

回答：IFU 的 M0 读请求先经过 ICache，LSU 的 M1 访问先经过 DCache 或直通路径。ICache 只处理读请求，DCache 处理读写请求；未命中或不可缓存访问再进入 AXI4-Lite 乒乓缓存和 AXI 互连。Cache 负责本地命中判断和数据存储，乒乓缓存只负责通道寄存与等待状态。

### 52. 当前 ICache 和 DCache 的参数是什么？

回答：推荐 ICache 使用 2048 行、1 路结构，DCache 使用 1024 行、2 路结构，读未完成请求和响应位置的深度均为 2。ICache 面向指令读取，DCache 采用读分配和写通策略。参数由 SoC 层统一传入，不随 P3、P4、P5 的流水线级数改变。

### 53. 为什么 ICache 和 DCache 的组织不同？

回答：指令访问通常以读取为主，ICache 可以使用简单的一路结构降低面积和控制复杂度。数据访问同时包含读取、写入和 MMIO，地址重复与替换冲突更明显，因此 DCache 使用两路结构并支持写通。两者的控制器接口统一，但内部状态和写入策略不同。

### 54. Cache 未命中时发生什么？

回答：控制器先保存未命中地址、访问类型和必要的写数据，再通过下一级 AXI 访问取得数据或完成写操作。返回数据进入响应 FIFO，并按照请求状态送回 IFU 或 LSU；在等待期间，当前请求的附属字段保持不变。未命中状态还要响应冲刷、复位和错误响应，防止旧请求影响新路径。

### 55. 乒乓缓存解决什么问题？

回答：乒乓缓存对 AXI4-Lite 通道字段进行寄存，使请求端和目标端不必在同一周期完成组合连接。它可以在同步存储器、AXI 互连和 CPU 之间形成一级或两级寄存边界，改善接口时序并允许有限的请求重叠。它不做 Cache 命中判断，也不替代 Cache 的标签和有效位管理。

### 56. 为什么不能把乒乓缓存称为 Cache？

回答：乒乓缓存不保存按地址索引的数据副本，不比较标签，也不决定命中或替换。它保存的是 AXI 地址、数据、控制和响应状态，作用类似接口级的流水寄存和等待队列。把两者混称会误解命中率、容量和一致性行为，因此报告中分别说明。

### 57. AXI 响应顺序如何保证？

回答：Cache 控制器为未完成读请求和响应设置 FIFO，保存请求位置和返回状态。AXI 互连根据 M0/M1 端口和目标设备状态选择响应，返回后由对应 FIFO 恢复到 CPU 请求。模块级测试会让多个请求产生不同等待长度，检查响应数据、目的地址和请求次序不发生错位。

### 58. DCache 的写通策略有什么含义？

回答：DCache 写命中或写请求会把数据直接送往下一级存储器，同时按照控制器规则更新本地状态。写通不需要在 DCache 中保留脏数据，复位和替换控制更简单，适合本项目的片上存储器结构。代价是连续写操作可能产生更多外部 AXI 事务，需要结合乒乓缓存和写响应状态改善等待。

### 59. LSU L0 Cache 与 DCache 有什么区别？

回答：LSU L0 Cache 位于 Dispatch 和 EXU 的短距离数据通路中，采用较小的影子标签和数据暂存区，重点优化加载结果紧跟条件分支或存储数据的场景。DCache 位于 M1 访问路径，负责更大范围的指令或数据存储副本。前者解决执行级短距离等待，后者减少片外或下一级存储器访问。

### 60. 关闭 Cache 或乒乓缓存后，接口是否改变？

回答：关闭功能后不生成对应的数据阵列、标签阵列或寄存状态，但 CPU 顶层 AXI 接口和握手规则保持不变。直通路径仍需正确处理 AXI 等待、错误响应和冲刷。这样可以用相同的系统测试程序比较不同存储配置，不需要改变软件访问方式。

## 七、时序、面积与 FPGA 实现

### 61. 设计中最需要关注的关键路径有哪些？

回答：关键路径主要来自 Dispatch 的多项冒险比较、操作数选择、分支预测条件、AXI 响应选择、Cache 标签判断以及 WBU 的结果仲裁。高位宽 ALU、MUL 和地址计算也可能形成较长数据路径。时序优化的第一步是用 RTL structure 工具定位逻辑深度和扇出，再回到 RTL 判断是否需要增加寄存器或拆分选择条件。

### 62. RTL structure 工具的作用是什么？

回答：RTL structure 工具对综合前 RTL 进行结构分析，统计逻辑层级、选择器深度、控制条件深度和扇出等信息，用于提前发现可能影响时序的结构。它不能替代 Vivado 实现阶段的静态时序分析，也不能直接证明某条路径一定失败。正确使用方式是先用它定位高风险结构，再用综合和实现结果验证修改效果。

### 63. 为什么流水线寄存器能够改善时序？

回答：寄存器把一条长组合路径拆成多个时钟周期，使每个阶段只承担有限的逻辑深度。P5 的级间边界最完整，通常有利于更高目标频率；P3 的寄存器更少，控制和数据必须在较短周期内完成。增加寄存器会带来额外延迟、暂停和冲刷控制，因此不能只看逻辑深度，还要检查架构时序和资源开销。

### 64. P3、P4、P5 如何选择？

回答：资源受限且重视单位频率效率时可以选择 P3；希望在逻辑规模和频率之间取得平衡时选择 P4；更重视目标频率和长路径拆分时选择 P5。三者都必须通过相同的功能和边界条件验证。最终选择应结合 FPGA 资源、目标频率、CoreMark 执行时间和系统应用需求。

### 65. 为什么性能配置会占用更多查找表资源？

回答：性能配置通常打开增强 BPU、LSU L0 Cache、Cache、乒乓缓存和更多旁路结构，并可能启用 Zba、Zbb、Zbs、Zbc。额外表项、比较器、选择器和控制状态减少等待周期，但会增加查找表（LUT）、寄存器和块存储器（BRAM）使用量。报告中的性能比较必须同时列出运行时间、IPC 和资源占用，不能只看单一指标。

### 66. Vivado 的 WNS 和 TNS 分别说明什么？

回答：最差负时序余量（WNS）表示最差路径的时序余量，反映最紧张的一条路径；总负时序余量（TNS）是所有负时序余量的总和，反映整体违规规模。WNS 变为非负并不代表所有路径都已满足，还要检查 TNS、最差保持时序余量（WHS）、总保持时序余量（THS）以及路径报告。报告中的 RTL structure 分析只能提供早期方向，最终时序结论以实现阶段结果为准。

### 67. 如何验证一次时序优化确实有效？

回答：先固定器件、时钟约束、综合策略和实现策略，再记录修改前后的关键路径、WNS、TNS、资源和功能结果。修改后必须重新执行相同配置的综合和实现，并检查是否把问题从一条路径转移到另一条路径。功能测试、模块级断言和覆盖率也必须保持通过，不能用时序改善掩盖功能变化。

### 68. FPGA 实现为什么要比较六种配置？

回答：六种配置由 P3、P4、P5 与默认、性能两类功能开关组成，可以同时观察流水线级数和性能特性对资源、频率与执行时间的影响。六组结果使用统一器件、时钟和实现条件，才能进行横向比较。上板 CoreMark 还要给出迭代次数、运行时间、Iterations/Sec、IPC 和循环冗余校验值（CRC）。

### 69. CoreMark 结果如何避免误读？

回答：必须说明 RV32 或 RV64、100 MHz 条件、2K performance run、`ITERATIONS=1`、ISA、ABI 和编译选项。CoreMark 分数受动态指令数、IPC、分支预测、Cache 和编译器调度共同影响，不能简单归因于 XLEN。RV64 LP64 ABI 下同一 CoreMark 可能动态执行更多指令，这是 RV64 分数较低的重要原因。

### 70. 性能和面积哪个更重要？

回答：要由应用目标决定。资源受限的 SoC 可能更重视每千 LUT 的执行效率和可实现频率，计算密集型应用可能更重视总执行时间和 IPC。答辩时应同时展示资源、频率、节拍数、INSTS、IPC 和分支预测准确率，说明选择背后的取舍，而不是声称存在唯一最优配置。

## 八、验证平台与验证方法

### 71. 为什么需要两个系统级仿真平台？

回答：Verilator 平台负责运行官方 riscv-tests、CoreMark、排序程序等系统级功能程序，适合快速检查完整 CPU、存储器和 MMIO 协同工作。VCS 平台负责完整回归、模块级 testbench、riscv-dv 类别随机程序、Spike 对照、断言、波形和覆盖率统计。两者使用相同 RTL、ISA 配置、镜像格式和程序完成协议，但关注点不同。

### 72. Verilator 系统级测试的价值是什么？

回答：它可以较快运行完整 CPU 和 SoC，确认程序是否到达标准结束状态，检查程序结果、tohost、执行周期数、INSTS 和 IPC。它适合早期功能检查和软件程序验证，不承担 VCS 的完整覆盖率数据库合并。Verilator 测试失败时，可以用 VCS 定向 Case 和波形进一步定位。

### 73. VCS 平台主要检查什么？

回答：VCS 平台同时检查模块协议、整核架构结果、长延迟状态、异常恢复、随机程序和覆盖率。模块级 testbench 直接驱动被测设计（DUT）的公开端口，整核程序通过 monitor、tohost、程序结果校验字和 Spike 对照检查。VCS 还生成 line、condition、branch、toggle、FSM、assertion 和功能组统计。

### 74. 官方 riscv-tests 是什么？

回答：riscv-tests 是 RISC-V 社区提供的指令集标准自检程序集，按 RV32、RV64、用户态、机器态以及 I、M、A、C 等指令类别组织测试。程序内部执行预设结果比较，并通过标准结束状态报告结果。项目使用它检查基础整数、乘除、机器态、原子、压缩和位操作扩展，再用自研程序补充当前 CPU 特有的冒险和存储场景。

### 75. 模块级 testbench 为什么不只看最终结果？

回答：最终结果错误只能说明某个行为不正确，无法直接判断是握手、状态保持、暂停还是结果选择出了问题。模块级 testbench 会逐事务检查 `VALID`/`READY`、地址和数据保持、复位、暂停、冲刷、错误响应以及结果字段。这样可以把故障定位到具体模块、参数和状态变化。

### 76. 整核程序的通过条件是什么？

回答：程序内部失败数必须为零，程序必须成功写入 tohost，系统平台要输出 `SYSTEM_CHECK_PASS`，断言不能失败，程序计数器活性检查不能超时。适用程序还必须通过八个 32 位程序结果校验字比较和 Spike 对照。只有这些条件同时满足，任务才记为 PASS。

### 77. riscv-dv 在项目中如何使用？

回答：项目保留 riscv-dv 的测试分类和指令生成思想，由本地 Python 程序依据固定配置生成适用于当前 RTL、启动代码和存储布局的汇编。RV32 和 RV64 各配置 21 类程序，每类使用两个固定 seed，三种流水线配置合计 252 个随机程序。这样既保持类别覆盖，又避免生成当前硬件不支持的指令或运行环境。

### 78. 为什么要使用固定 seed？

回答：固定 seed 使随机程序可以重复生成，失败时能恢复相同指令序列和相同输入条件。任务摘要还会关联测试类别、ISA、ABI、编译结果和运行参数，便于判断失败来自硬件、程序还是配置变化。精简覆盖率回归只保留每类的第一个 seed，用于快速采集覆盖率，不替代双 seed 稳定性检查。

### 79. Spike 在验证中扮演什么角色？

回答：Spike 是顺序执行的 RISC-V 参考模型，使用与 RTL 相同的可执行与可链接格式文件（ELF）、ISA 和程序数据。它不模拟本项目的流水线时序，而是提供架构结果参考。比较程序提取 Spike 和 RTL 对同一程序结果区的写入，比较八个 32 位校验字，从而检查算术、控制转移、访存回读、异常返回和程序完成状态。

### 80. 多级流水线和 Spike 的时序如何对应？

回答：RTL monitor 不要求每条指令在同一时钟周期与 Spike 同时完成，而是在 RTL 的完成或写回事件发生时采集程序结果区写入和完成信息。Spike 只执行同一 ELF 并保存对应地址范围的结果，比较过程按地址和校验字进行，而不是强制逐周期同步。这样可以兼容多级流水线、长延迟单元和有限乱序完成。

## 九、边界条件、覆盖率与结果解释

### 81. 为什么边界条件测试比普通随机程序重要？

回答：随机程序可以扩大指令和状态组合，但不一定稳定触发某个特定周期边界。边界条件程序只改变一个关键变量，例如相邻指令间隔、访问宽度、异常位置或完成次序，并给出精确的期望结果。它们适合验证暂停、冲刷、长延迟返回和保护区未被改写等容易遗漏的状态变化。

### 82. 如何设计 RAW 边界条件？

回答：让一条指令产生结果，后续指令分别在相邻周期、间隔一个周期和间隔多个周期读取该结果，并覆盖 ALU、MUL、DIV、CSR 和 load 结果。程序同时检查寄存器结果、内存结果、完成信息和程序结果校验字。若缩短间隔会触发错误旧值读取，通常可以定位到 HDU 比较、局部旁路或等待槽释放问题。

### 83. 如何设计 WAW 边界条件？

回答：安排两条写同一目的寄存器的指令，让前一条使用长延迟单元，后一条使用短延迟单元，再改变两条指令之间的间隔。正确结果应符合程序顺序要求，而不是简单采用最先完成的值。测试同时观察 WBU 写回事件和最终 GPR 值，以区分写回抑制错误与执行结果错误。

### 84. 如何检测结构冒险？

回答：让多个指令连续请求同一个 MUL、DIV、LSU、AXI 通道或写回接收位置，并在请求之间插入不同长度的等待。期望结果包括请求不丢失、输入字段保持、完成数量正确和程序最终结束。若出现超时、重复完成或字段错位，可以进一步查看资源 busy、ready 和 valid 的波形。

### 85. 如何验证分支错误路径已经被清除？

回答：在 taken branch 或异常指令的错误路径上放置会修改寄存器和内存的指令，再在正确路径检查这些修改不应出现。测试还要覆盖错误路径中有 store、MUL、DIV 或 CSR 操作的情况。通过内存保护字、GPR 结果、异常字段和程序校验字同时检查，可以防止只看到程序跳回正确地址却遗漏错误路径副作用。

### 86. 如何设计访存边界条件？

回答：访存边界条件覆盖字节、半字、字、双字访问，地址对齐和非对齐访问，读后写、写后读、连续写以及不同 DTCM、M0、M1 延迟组合。有效数据两侧放置保护值，程序结束后检查保护值不变，并比较读取结果和 tohost 状态。对于 Cache 和乒乓缓存，还要让较早发出的请求延迟返回，观察响应次序。

### 87. 如何验证异常恢复？

回答：程序在非法指令、EBREAK、ECALL、非对齐访问和中断前后设置可检查的算术和访存操作。异常处理程序读取 `mcause`、`mepc` 和计数器，修正返回地址后执行 `mret`，主程序再检查异常前后状态是否符合预期。测试还要确认异常处理期间较早完成的 store 保留，错误路径操作被清除，返回后可以继续执行新指令。

### 88. 覆盖率 Feature List 和 Case 如何对应？

回答：每个 Feature 只描述一个明确功能点，并指定一个主模块 Case 或整核程序 Case。模块、访存参数、随机程序、边界条件和排序程序分别列出，避免把多个互不相关功能塞进一行。执行时通过 Case 状态、程序结果、断言和 coverage group 判断该 Feature 是否实际被激励。

### 89. `1309/1309` 全部通过说明什么？

回答：它说明完整覆盖率回归计划中的 1309 项任务均通过，包括 111 项模块任务、28 项访存参数任务、276 项系统任务、884 项程序任务、3 项 SoC 参考任务和 7 项合并任务。它证明当前任务集合的功能检查和合并步骤没有失败，但仍应结合表格中的代码覆盖率和功能组统计理解验证范围。任务全部通过不等于所有未执行 RTL 分支都已达到 100%。

### 90. raw 和 after-waive 应该如何解释？

回答：raw 是未应用配置级排除项的统计，after-waive 只排除因固定参数关闭而无法展开的 RTL 对象，不得用于隐藏可由软件或模块 Case 执行的功能。当前 P3、P4、P5 的 after-waive line 和 Module-definition line 均为 100%，SCORE 分别为 87.56%、88.24% 和 89.13%。line 表示 RTL 行是否执行，condition、branch、toggle、FSM 和 assertion 分别观察条件组合、控制分支、信号翻转、状态机和断言；SCORE 是代码指标汇总，不能代替功能组覆盖率。

## 十、软件系统与应用补充

### 91. 软件如何与硬件指令集配置保持一致？

回答：软件的 `-march` 必须由 XLEN 和 A、C、Zba、Zbb、Zbs、Zbc 等硬件开关确定，`-mabi` 在 RV32 下使用 `ilp32`，在 RV64 下使用 `lp64`。流水线级数、BPU、Cache 和乒乓缓存不改变 ISA 或 ABI，但会改变执行时间。硬件关闭某项扩展时，软件不能生成对应指令。

### 92. 裸机板级支持包的启动过程是什么？

回答：复位入口设置栈指针、清零 `.bss`、建立必要的 CSR 状态，再进入 `main` 或 RT-Thread Nano 启动入口。链接脚本把 `.text` 和初始化表放入 IMEM，把 `.rodata`、`.data`、`.bss`、堆和栈放入 DMEM。启动程序使用链接脚本定义的边界符号完成初始化，因此存储容量和区段地址必须与硬件一致。

### 93. 软件如何处理异常和中断？

回答：统一入口读取 `mcause`，先区分同步异常与异步中断，再选择对应处理函数。裸机入口保存调用 C 函数可能修改的通用寄存器和 `mepc`，处理完成后恢复状态并执行 `mret`。默认处理过程不打开中断嵌套，外设处理函数必须在返回前清除设备挂起状态。

### 94. RT-Thread Nano 移植包含哪些关键部分？

回答：移植内容包括 libcpu、线程栈初始化、上下文切换、机器模式异常入口、CLINT 系统节拍、机器软件中断、PLIC 外部中断、UART 控制台、系统堆和 MSH。线程切换保存线程栈指针和必要寄存器，中断中的切换在统一退出位置恢复目标线程。该部分证明 CPU 可以运行带调度、中断和动态内存的实时系统。

### 95. RT-Thread Nano 的系统节拍如何产生？

回答：CLINT 的 `mtime` 按系统时钟递增，软件把下一次节拍值写入 `mtimecmp`。定时器中断进入后先设置下一次比较值，再调用 `rt_tick_increase()`，防止处理期间旧比较条件持续成立。节拍周期由 `SOC_TIMER_FREQ/RT_TICK_PER_SECOND` 计算。

### 96. 软件如何使用 PLIC？

回答：初始化时关闭未配置中断源、设置优先级并安装处理函数和参数。机器外部中断进入后，软件读取 PLIC 给出的处理函数和参数并调用对应驱动。处理函数完成设备状态清除后返回，优先级数值越大的有效中断先被选择。

### 97. THP 应用为什么适合作为 SoC 展示程序？

回答：THP 应用同时使用 RT-Thread Nano、I2C、GPIO 引脚复用、UART、OLED 和多个环境传感器，可以检查从 CPU 指令执行到 MMIO、外设控制和线程调度的完整系统行为。应用把传感器采样、物理量计算、UART 输出和 OLED 刷新放在同一次处理流程中，便于观察软件与硬件是否协同工作。

### 98. 软件 I2C 和硬件 I2C 有什么区别？

回答：软件 I2C 使用 GPIO0[18] 和 GPIO0[19] 模拟开漏 SCL、SDA，通过方向切换和外部上拉产生高低电平。硬件 I2C 先配置 GPIO0.IOFCFG，再由 APB I2C0 控制器产生 START、WRITE、READ 和 STOP。两种实现对传感器和 OLED 提供相同的上层访问接口。

### 99. OLED 显示如何与传感器采样保持一致？

回答：后台线程和 MSH 命令共用同一个采样函数，函数先取得信号量，再读取在线传感器并形成一次样本快照，随后把同一份快照送到 UART 和 OLED。SSD1306 使用 128×64 帧缓冲区，刷新前先清空旧内容，再按照有效字段排版。这样可以避免终端显示和 OLED 使用不同轮次的数据。

### 100. 如果只用一句话总结设计和验证，应该怎么回答？

回答：Alkaid 是一款面向 FPGA 的可配置 RISC-V CPU，通过三级、四级和五级流水线、轻度乱序单发射、有限乱序完成、分支预测、Cache 和乒乓缓存实现性能与资源的可调节平衡，并通过 Verilator 系统测试、VCS 模块与整核回归、riscv-dv、Spike 对照、边界条件 Case 和覆盖率统计进行分层验证。当前完整覆盖率回归 1309/1309 全部通过，P3、P4、P5 的 after-waive line 和 Module-definition line 均达到 100%。
