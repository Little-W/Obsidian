下面给你一个**严格、可直接用于推导 / RTL / 论文的「对称 rounding（symmetric rounding）」完整公式说明**。我会按 **数学定义 → 整数等价形式 → CMSIS-NN 特化形式 → 直觉解释** 四层来写。

---

# 一、对称 rounding 的数学定义（最本质）

对任意实数 (x)，**对称 rounding（round-to-nearest, symmetric）** 定义为：

$$
\boxed{
\operatorname{round}_{sym}(x)
=============================

\begin{cases}
\lfloor x + 0.5 \rfloor, & x \ge 0 [6pt]
\lceil x - 0.5 \rceil, & x < 0
\end{cases}
}
$$

等价理解：

* **正数**：四舍五入
* **负数**：绝对值意义下四舍五入，再恢复符号
* rounding 结果 **关于 0 对称**
* 不会产生系统性正偏或负偏

---

# 二、用于“除以 (2^n)”时的对称 rounding（核心工程形式）

目标计算：

$$
y = \operatorname{round}_{sym}!\left( \frac{x}{2^n} \right)
$$

---

## 1️⃣ 先做算术右移（截断）

$$
q = x \gg n
$$

---

## 2️⃣ 计算余数（低 n 位）

$$
r = x \bmod 2^n
$$

在整数中等价于：

$$
r = x \& (2^n - 1)
$$

---

## 3️⃣ 定义对称 rounding 的阈值

$$
\boxed{
T =
\frac{2^n - 1}{2}
+
\mathbf{1}(x < 0)
}
$$

其中：

* (\mathbf{1}(x < 0)) 是指示函数

  * (x<0) → 1
  * 否则 → 0

---

## 4️⃣ 最终对称 rounding 公式（整数形式）

$$
\boxed{
y
=

q
+
\mathbf{1}(r > T)
}
$$

---

# 三、CMSIS-NN / RISC-V NN 使用的**完整整数公式**

把上面的步骤合并，CMSIS-NN 实际实现的是：

$$
\boxed{
\operatorname{round}_{sym}!\left(\frac{x}{2^n}\right)
=====================================================

(x \gg n)
+
\mathbf{1}!\left(
(x \& (2^n - 1))

>

\left(
\frac{2^n - 1}{2}
+ (x < 0)
\right)
\right)
}
$$

这就是你在 `riscv_nn_divide_by_power_of_two()` 中看到的全部数学含义。

---

# 四、为什么负数阈值要「+1」（关键直觉）

这是整个问题里**最容易出错、但最关键的一点**。

### 举例：(n = 3)，即除以 8

$$
2^n = 8,\quad \frac{2^n - 1}{2} = 3
$$

---

### 正数示例

$$
x = 13 \Rightarrow 13/8 = 1.625
$$

* (q = 1)
* (r = 5)
* (T = 3)
* (r > T) → rounding up
  → (y = 2)

✔ 正确

---

### 负数示例（关键）

$$
x = -13 \Rightarrow -13/8 = -1.625
$$

* (q = -2)（算术右移）
* (r = 3)
* (T = 3 + 1 = 4)
* (r > T) 为假
  → (y = -2)

✔ 这是 **(-1.625 \to -2)**
✔ 对称 rounding

---

### 如果你**不加这 +1**

* 阈值仍是 3
* (r = 3)
* 会被误判为 rounding up
  → 得到 (-1)

❌ **系统性偏大（绝对值变小）**
❌ 正是你之前看到的现象

---

# 五、和其他 rounding 方式的对比（非常重要）

| 方法              | 正数   | 负数      | 是否对称 | 是否有系统偏差 |
| --------------- | ---- | ------- | ---- | ------- |
| truncate        | 向 0  | 向 0     | ❌    | ✔       |
| floor           | 向 −∞ | 向 −∞    | ❌    | ✔       |
| round half up   | +0.5 | +0.5    | ❌    | ✔       |
| **对称 rounding** | 四舍五入 | 绝对值四舍五入 | ✅    | ❌       |

CMSIS-NN 选择的是 **最后一行**。

---

# 六、用于论文 / 设计说明的一句话公式版（可直接引用）

> 对称 rounding 定义为
> $$
> \operatorname{round}_{sym}(x) =
> \begin{cases}
> \lfloor x + 0.5 \rfloor, & x \ge 0 \\
> \lceil x - 0.5 \rceil, & x < 0
> \end{cases}
> $$
>
> 在整数除以 (2^n) 的实现中，等价为
> $$
> (x \gg n) + \mathbf{1}!\left(
> (x \bmod 2^n) >
> \frac{2^n-1}{2} + (x<0)
> \right)
> $$

---

如果你愿意，下一步我可以帮你：

* 📐 把 **对称 rounding** 和 **round-to-even** 做一个**量化误差对比**
* 🔧 给你一个 **完全 combinational / pipeline 的 RTL 实现模板**
* 🧪 推导 **为什么对称 rounding 在 NN 推理中 bias 最小**

你是打算 **写论文公式**，还是 **继续做 RTL 级优化**？
